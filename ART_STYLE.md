# MCTHUNDER 美术规范 · 025

使用原创低多边形造型，保留斜甲、分面炮塔、炮盾、轮组和悬挂的车型差异。1 单位为 1 米；Godot Y 向上、-Z 向前。外饰尺寸仍有估算，不把美术统一表述为史料精度提升。

`assets/art_palette.json` 是四车和两图的共用色板。主体橄榄绿、履带钢灰和橡胶深灰；村屋灰黄墙、土红瓦；工业设施灰绿和砖色。默认粗糙度 0.9，钢件金属度 0.2。无写实贴图、第三方游戏模型或游戏标志。共用日光角度 (-48,-32) 度、能量 0.85、环境光 0.42。主要面使用正确正面绕序和独立平面法线，禁止用双面材质掩盖错误法线。

四车可编辑 `.blend` 位于 `authoring/vehicles/`，运行资源为 `assets/vehicles/*.glb`。使用 Blender 5.2.1 LTS；游戏启动不依赖 Blender。先统一 M4 样件，再应用到 M24/M26/M36；调色脚本逐项证明顶点和对象局部矩阵未变。导出方式见该目录 README。

运行时装甲外皮按 hull/turret/barrel 合批，顶点直接来自真实装甲布局。GLB 外饰、炮口和后坐对象保持既有轴系；行驶碰撞包围盒和装甲数值保持原配置。`--geometry-overlay` 可叠加青色实际装甲线框；车库原有装甲/内构检查继续可用。炮塔脱离后，实际查询沿同一炮塔节点变换，旧位置不留下隐形炮塔装甲。

每车预算含动态履带和暂时隐藏的炮口火光、烟火效果：最多 25,000 三角形、45 个绘制面。确切数据以交付日志为准。小光学/帆布/暗色外饰 140 米淡出，履带小履片 100 米淡出，主装甲、炮管和履带大轮廓继续显示。合批按运动部件与建筑组件进行，不采用每个小块一个独立节点的实现。

房屋、仓库、墙、石堆、树和道路由可复用原创建模代码生成。房顶凸碰撞跟随可见屋顶；树、石堆有可见且匹配的实体碰撞，阻挡炮弹和视线。新增树石避开原道路、出生点、补给区和建筑。每图最多 65,000 静态三角形、150 个绘制面；八车真实窗口另外采样，不能用单模型预算代替整场性能。

每图外围有对称的两座木棚、两组三段砖墙；每段两次速度至少 30 m/s 的实际炮弹命中后倒塌。这是游戏规则初值，不是结构力学模拟。第一发出现损坏，第二发打开真实缺口；击毁建筑的那一发仍终止于建筑，后续弹丸和车辆可通过缺口，边缘残块保留碰撞。倒塌碎片 1.2 秒、尘土 1.5 秒后清理；不改动原永久掩体。

死亡视觉由真实死亡原因驱动：装有弹药的弹药架殉爆产生一次爆闪，历史四车型的真实炮塔抛出并碰撞落地；火灾致乘员不足留下燃烧残骸；普通乘员不足留下炭黑残骸。殉爆/火灾残骸火焰 45 秒、烟 60 秒后消散，残骸本身继续遵循既有数量与寿命清理规则。没有新增范围爆炸伤害。效果暂停、重置、结束与实体清理均受生命周期约束。

车辆作者、许可、生成方式、源文件/GLB 哈希记录在各车型 manifest；场景与效果登记在 `assets/world_art.manifest.json`，通过 `python authoring/update_world_manifest.py` 更新。登记使用工程相对路径，代码文本哈希统一 LF 换行。全部为项目原创生成资产，可随项目分发源文件；未引入须另购许可的素材。
