class_name CapturePointState
extends RefCounted
## Independent capture state; the legacy single-point match inherits the same fields.
var capture_progress := 0.0
var capture_owner := 0
var contested := false
