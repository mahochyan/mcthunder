# WT-010 共用静态弹道与有限平移接触增量

2026-09-12补充：APHE已新增可配置模拟延迟与车外破片，见WT013_DELAYED_FUZE.md；下方APHE每步末静态几何的边界仍有效，未启用其连续平移或旋转接触。

静态距离装定使用 `SightBallistics` 的恒重力数学，进入权威炮塔追瞄和真实炮口发射。真实 600m 静止目标检查记录见 `WT007_RANGE_AND_ZEROING.md`。后续AI已接入共用`BallisticIntercept`，相对静止情形复用`SightBallistics`；实际移动目标与机构跟踪验证见`WT010_AI_INTERCEPT.md`。下文平移接触边界保持原样，不因此扩展为旋转或有限弹体碰撞。

`TranslationSweep` 使用相邻权威物理帧的实际部件变换。每个炮弹分段的世界起止时间映射到目标部件的相对运动线段，再查询原装甲三角形和内构盒；没有扩大命中盒。真实命中世界点、飞行距离与冲击时刻回放几何保持对应。

准入边界是同实体、生命、代次、定义、布局与资源，时间间隔不超过 0.1s，单步平移不超过 8m，部件基底完全不变。旋转、身份变化、断帧或越界使用原静态末姿态路径并记录原因。静止车辆保持旧事件字段与回放缓存语义。等速退化的相对线段在明确盒外判错过、明确盒内保留持续区间，边界仍标未决。

本批只对动能点弹丸启用平移接触。APHE 的内部路径与破片仍依赖静态几何，因此 `internal_burst` 在入口冻结为同一末姿态，避免将新接触时刻与旧内部结算混用。旋转目标、有限弹体、穿后时变内构及 APHE 全程连续几何均未完成；WT-010 保持 `in_progress`。

工程证据：`logs/WT-parallel/c193693-integrated-wip2/20260912-150459` 中 67 项通过，覆盖真实阶段驱动下的薄目标横穿、同空间错时错过、墙遮挡、寿命/路程裁短、身份与间断隔离、等速退化、APHE 策略隔离及静态兼容。首次阶段夹具在手动调用之间跨过物理帧，失败证据保留在 integrated-wip1；修订后使用真实优先级阶段调度，并断言相邻采样 tick。检查通过不代表 WT-010—013 整体毁伤验收。
