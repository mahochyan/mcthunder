# 暂停的测距/装定实现草稿

2026-09-12，用户要求先规划全部未完成工作，开发暂停在 `5f24ae6`。

- `transport-unverified.patch`：暂停前的6个传输文件改动，依赖尚未实现的 AimIntent / FireControlState，不可直接应用为可运行功能。
- 两个 `.gd.txt`：恒重力瞄准解与测试草稿，未导入、未运行、未接生产路径。以文本保存，避免进入 Godot 脚本扫描。

已保存草稿后，仅将本轮自己的6个传输文件恢复到 HEAD。用户原有6个模型相关修改未动。当前主线命令协议仍v2、网络仍v3；这些草稿没有完成证据，不计入功能进度。后续先按全量执行计划核对既有弹道求解器及接口，再决定复用或改写，禁止因已存在草稿而倒推需求。
