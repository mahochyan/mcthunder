# 回归门总表 · 第 4 轮增补（WT-036-R1 后续）

> 承接 `REGRESSION_GATE_MASTER.md`；本文件只记录**新增/更正**的结论，均已实跑。

## 1. 地图套件：**回归关闭**，且为**本会话最佳**
| 指标 | 基线 main | 本分支 |
|---|---|---|
| `run_map_checks` | **25 PASS / 2 FAIL**（**+22 项新增检查之前**的规模） | **47 PASS / 2 FAIL** |
| 失败构成 | `T018-H01 spawn 1/0` + 宽体坡道代理 | **`T018-03b`（我新增的设计目标项，按设计仍失败）** + **≤1 个 `T018-H01` 槽位**（间歇） |
| **`T018-02`（我曾引入的回归）** | 通过（`arrived` 84.9 s，含 `reverse`） | **通过**（两轮）✓ **已关闭** |

### 1.1 回归的根因与最终修法（六变体实测，阈值路线被否证）
- **根因**：我在 WT-039-R1 给卡死窗口加的"**需真实油门**"门限，在 `yielding`（驱动器故意把油门置 0）时不累计 ⇒ **对停放车永久让行**（250 s 用满，检查要求"抵达**且**倒车"）✗
- **六变体**：0 无处理（✗ 永久让行）· 1 仅重规划（✗ 到达但无 `reverse`）· 2 任意让行 2.5 s 恢复（✓地图但✗村庄）· 3 静止 2.5 s（同）· 4 静止 **6.0 s**（同）· 5 **无控制器**（✗ 因**墙/建筑同样无控制器**被误判为死路）· **6 无主「车辆」才计时**（`owner is VehicleActor and controller == null`）⇒ **村庄 21/0 ✓ 且 `T018-02` 两轮通过 ✓ → 采用**
- 决策记录：`WT-036-R1_YIELD_VARIANTS_TABLE.md`

### 1.2 地图套件的门禁口径（**按失败集合**，不按总数）
允许 { `T018-03b`（设计目标）+ 每轮 ≤1 个 `T018-H01` 槽位 }；**出现新失败类型**才判回归 ✓
依据：`T018-H01` 属**既有 + 间歇 + 预算边际**（基线同样失败；逐轮槽位不同；轨迹显示截止时仍在推进：枢轴转向后 `speed=1.221`、`dist_wp=2.27`）→ `WT-036-R1_T018_H01_MARGINAL_BUDGET.md`

## 2. 工业套件：加固保留 + `supply` 机制定案 + 铺装修复
| 项 | 结论 |
|---|---|
| **加固（保留）** | 595 项检查（原 339 + **256 条出生净空断言全绿**）；原始 339 项 **300/39 → 302/37**（净 +2）；失败时**逐 tick 轨迹落盘** ✓ |
| **污染假设** | **否证**（256/256 净空通过）✓ |
| **`supply` 机制** | 轨迹定案：车在 `(±50.8, 117.0)`（**出生行、未铺装、drag 0.55**）原地转向后，到点曲线只给 **0.24 油门** ⇒ **从静止无法起步** ⇒ 恢复耗尽 ⇒ `failed` ✓（与已修的 M26 道路红**同源**） |
| **修复** | **用户已授权的 C 类**：铺装 `supply→spawn` 与离开出生行的首段（保留密集 `spawn↔spawn`/`cap↔cap` 为开阔地）⇒ 门禁中立（地图 46/3→47/2、村庄 21/0、历史 33/0、T039-E 3/3）✓ |
| **复验** | 后台完整运行（约 1400 s）→ 结果并入下一轮 |

## 3. DEFERRED 与分类（不变）
**DEFERRED = 0**：环境依赖 NOT_RUN（4）· SLOW 已测（2）· MEASURED（5，其中唯一真实 FAIL = `challenge` 的防守夹具能力边界，已按 D 口径提交 4 选项）

## 4. 保留的产品改动（更新，相对基线）
| 文件 | 内容 | 验证 |
|---|---|---|
| `tank.gd` + `game_config.gd` | 坡面自转修复 + 转向支撑下限 | T039-E 3/3 · 村庄 21/0 · 地图集合口径 ✓ |
| `ai_path_driver.gd` | 卡死窗口修复 + **让行死锁修复（无主车辆才恢复）** | 地图 `T018-02` 两轮通过 · 村庄 21/0 |
| `village_definition.gd` | **`near→cap` 与 `supply→spawn` 铺装**（两次 C 类） | 历史 **33/0** · supply 复验中 |
| `navigation_bake_pipeline.gd` | 包络含地面诊断字段（不改 `ok`） | 地图集合口径 ✓ |
| `vehicle_actor.gd` | 只读可观测字段 | — |

## 5. 未结项（4）
`supply` 复验（进行中）· `T018-H01` 预算（A/B 选项）· `challenge` 防守夹具（4 选项待裁定）· M26 坡上起步（调校冲突，已登记）
