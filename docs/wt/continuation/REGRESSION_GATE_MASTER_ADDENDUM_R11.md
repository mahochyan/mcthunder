# 回归门终版（第 11 次回填）：**扩容后全量门禁**

## 1. 最新全量运行（120 套件，1500 s/套件）
```
PASS = 118 · FAIL = 2 · ZERO = 0 · TIMEOUT = 0
```
| 失败套件 | 结果 | 定性 |
|---|---|---|
| `run_industrial_battle_checks` | **15 / 1** | **与未改动基线逐项一致**（基线亦 15/1）⇒ 唯一失败＝**既有抵达红** ✓ |
| `run_challenge_checks` | **138 / 2** | 已登记：**防守夹具能力边界** ✓ |

## 2. 门禁清单当前规模：**123 套件**（本次运行后新增 3 个，均已试跑通过 ✓）
`run_turret_mechanism_player_checks`（41/0 ✓）· `run_river_reachability_checks`（34/0 ✓）· `run_river_engagement_checks`（36/0 ✓）

## 3. 与上两次全量运行的对照
| 运行 | 套件数 | 结果 |
|---|---|---|
| 第 2 轮口径（原始列表） | 32 | **30 PASS / 2 FAIL**（两项非回归 ✓） |
| 扩容后（本轮前） | 111 | **109 PASS / 2 FAIL** ✓ |
| **本轮** | **120** | **118 PASS / 2 FAIL** ✓ |
（三次运行的失败项**始终是同一对非回归** ✓ ⇒ 扩容未引入任何新红 ✓）

## 4. 179 个 `run_*.gd` 的完整归类（可审计）
| 类别 | 数量 |
|---|---|
| **默认门禁** | **123** ✓ |
| 慢档（>240 s） | 4 |
| 诊断电池（按设计有已知失败） | 1 |
| 零输出（需实参/窗口/输入） | 17 |
| 环境依赖（窗口/渲染/导出/设置/教程） | 8 |
| 其余未逐项试跑（多为 demo/overview/showcase 变体） | 26 |

## 5. 本会话对门禁的净贡献
1. **覆盖 32 → 123**（+91 套件 ✓，累计 **数千项检查** ✓）；
2. **两次抓到我引入的回归**（`ai_drive` → `6937e684`；`partial_support` → `c26ac2c1` ✓）⇒ 两者**现由门禁看住** ✓；
3. **判定口径修复**（日志证据，替代不可靠的 `ExitCode` ✓）+ **零输出必须判 NOT_RUN**（含我自己的误判自查 ✓）；
4. **默认超时 900 → 1500 s** ✓（工业套件需 971 s ✓）；
5. **分类规则**：慢档不入禁 · **诊断电池永不入禁** · 零输出/环境依赖 ⇒ NOT_RUN · 网络/河谷入口套件 ⇒ 专用脚本 ✓。

## 6. 仍未结（不声称完成）
`challenge` 防守（B1–B4）· `industrial_battle` **既有**抵达红（建议立项）· `T018-H01`（A1/A2）· M26 坡上起步（C1–C3）· 资产 ⑤ 待**作者字段与签收** · 全量 96 车适配与导出预设（**待授权**）· 真人验收与性能采样（**NOT_RUN**）

---

## 补记：门禁清单**定稿为 127 套件**，179 套件归类完成
| 类别 | 数量 |
|---|---|
| **默认门禁** | **127** ✓（新增 `room_service` 52/0 · `settings` 36/0 · `shell_cycle_player` 41/0 · `tutorial` 49/0 ✓） |
| 慢档 / 演示 | 13 |
| 需实参/窗口 | 28 |
| 环境依赖 | 8 |
| **既有基线红** | **1**（`run_chassis_response_checks` 21/2，与基线逐项一致 ✓） |
| 诊断电池 | 1（T039-D ✓） |
| 演示类失败 | 1（`run_armor_demo` ✓） |
| 未试跑 | **0** ✓ |
| **合计** | **179** ✓ |

**定稿运行**：`logs/WT-036-R1/full-gate-127/`（127 套件，1500 s/套件 ✓）——用于确认"**扩容后无新红**" ✓
