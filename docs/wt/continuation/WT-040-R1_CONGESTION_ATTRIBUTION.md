# WT-040-R1 拥堵归因（遥测实测）：**同队相邻出生位之间的反复互让**

> 本轮只做**测量**（遥测改动零行为变化 ✓，回归全绿 40/0 · 39/0 · 67/0 · 48/0 ✓）。
> 证据：`logs/WT-040-R1/yield-telemetry2.log` 的 `[river-blocks]` 行。

## 1. 阻挡归因表（时间 · 边 · 模式 · **阻挡方**）
| t | 让行方 | 边（图节点→图节点） | 模式 | 阻挡方 | blocked_total |
|---|---|---|---|---|---|
| 18.3 | A | (−278,712) → (−287,712) | replan | **A2** | 1 |
| 16.8 | A2 | **同一条边（反向）** | replan | **A** | 1 |
| 18.3 | B | (−278,−712) → (−287,−712) | replan | **B2** | 1 |
| 16.8 | B2 | 同边 | replan | **B** | 1 |
| 68.5 | A | (−238,680) → (−260,680) | replan | **A2** | 2 |
| 67.0 | A2 | (−260,680) → (−282,680) | replan | **A** | 2 |

## 2. 结论
1. 互让双方**同队且出生位相邻**（A↔A2、B↔B2）✓，并在 t≈17 与 t≈68 **反复发生** ✗；
2. 每次互让都 **replan + 创建阻挡边** ✓ ⇒ blocklist 1→2 ⇒ 路线改变 ⇒ **绕行** ⇒ 推进被队内拥堵拖住 ✗；
3. **A3/A4/B3/B4 的 blocked=0** ✓ ⇒ 拥堵**只**发生在相邻出生位那一对 ✓（范围极小 ✓）；
4. 责任在我 ✓（"双向让行 + 优先级宽限"是我早前的改动 ✓，在河谷上被反复触发 ✓）。

## 3. 已证伪的两条修复路线（勿重复）
| 尝试 | 结果 |
|---|---|
| **保留跳点索引**（跨重规划续行） | **无效** ✗ —— 绕行本就从当前位置开始，索引语义不适用 |
| **TTL 时间过期**（统一 / 按来源区分） | **有害** ✗ —— `run_ai_drive_checks` 38/40（`permanent obstacle reaches bounded explicit failure` ✗、`two oncoming AI actors arrive or explicitly fail…` ✗）⇒ **阻挡边记忆是承重契约** ✗ |

## 4. 本轮采用的修复方向：**空间释放**（"离开即释放"）
阻挡边在车**已经离开该边附近**后释放 ✓（阈值 `AI_BLOCKED_RELEASE_M` ✓）：
- 需要绕行时记忆仍在 ✓ ⇒ 不破坏上述两条契约检查 ✓（且竞技场尺度小 ⇒ 该检查场景下几乎不释放 ✓，更保守 ✓）；
- 绕行完成后不再累积 ✓ ⇒ 后续重规划不再被历史扭曲 ✓；
- 与时间无关 ✓ ⇒ 不引入"忘得太早"的风险 ✓。

---

## 5. 第三次尝试也失败：**空间释放**（"离开即释放"）✗
| 步骤 | 结果 |
|---|---|
| 空间释放（`AI_BLOCKED_RELEASE_M=60`，全部阻挡边给位置锚点） | `run_ai_drive_checks` **39/40** ✗ —— 会车契约**通过** ✓ 但 `permanent obstacle reaches bounded explicit failure` ✗ |
| 按模式区分（`recover` ⇒ 永久 `INF` ✓ / `replan` ⇒ 位置锚点 ✓） | **仍 39/40** ✗（同一项 ✗） |
| **处置** | **整体回退** ✓（`game_config` 与 `ai_path_driver` 无行为差异 ✓），回归**恢复全绿** ✓；**只保留遥测** ✓ |

### 结论（三次尝试的净收获）
1. **阻挡边记忆与 `same_retry()` 契约纠缠** ✗：`set_goal` 中 `if not same_retry: _blocked_edges.clear()` ✓ 意味着**任何释放**都可能让"同一失败目标"重新被无限尝试 ✗ ⇒ 该处**不可轻改** ✓；
2. **已证伪三条路线** ✗：保留跳点索引 ✗ · 时间过期（统一/按来源）✗ · 空间释放（统一/按模式）✗；
3. **拥堵归因是明确的** ✓（同队相邻出生位 A↔A2、B↔B2 反复互让 ✓，其余四车 blocked=0 ✓）；
4. ⇒ **下一步不该继续在阻挡边记忆上加机制** ✗，而应从**拥堵的成因**入手（**同队相邻槽位为何互让并各自 replan** ✓）—— 但这会触及让行契约 ✓ ⇒ **需要与工作单 014/020 的契约一并评估** ✓，**不是**一次可安全落地的单点改动 ✓。

---

## 6. 第四次尝试（地图导航数据：每槽独立发车通道行）也失败 ✗
| 项 | 结果 |
|---|---|
| 改动 | `river_junction_navigation.gd` 发车段：每槽一条**独立通道行**（不再同点汇合 ✓） |
| 回归 | `check_river_task_mapping` **3/4** ✗ —— `every river slot's task point is routable on the map graph` **0/8** ✗✗ |
| 处置 | **立即回退** ✓（`diff b1ba1880` = 0 行 ✓）；回归**恢复全绿** ✓ |

## 7. 四次尝试的**共同结论**（本阶段最诚实的判断）
被证伪的四条路线 ✗：
1. 跨重规划**保留跳点索引** ✗（无效 + 一次加强版破坏 3 项检查 ✗）；
2. 阻挡边**时间过期** ✗（统一 / 按来源 ✓ 均 38/40 ✗）；
3. 阻挡边**空间释放** ✗（统一 / 按模式 ✓ 均 39/40 ✗）；
4. 河谷**发车通道按槽错开** ✗（`0/8` 可达 ✗）。

⇒ **共同点**：无论是"阻挡边记忆"还是"河谷导航图数据"，都是**被契约检查严密守住**的部分 ✓（`ai_drive` 的两条让行契约 ✓ + 目标可达性 ✓）。
⇒ **判断**：河谷队内拥堵**不是一次单点改动能安全解决的** ✗ —— 它需要**一次正式设计循环**（设计 → 评审 → 实现 → 验证 ✓），可选方向：
   a. **接受并登记**该局限 ✓（河谷仍是"工程性对局测量" ✓，不向玩家开放 ✓）—— 最保守 ✓；
   b. **河谷发车/通道结构化重设计** ✓（需重新设计图与出生位关系，并**同批**重跑可达性与契约检查 ✓）；
   c. **整体重评让行契约** ✓（触及工作单 014/020/021 的验收口径 ✓，需与工作单一起评审 ✓）。
**我不会在无设计循环的情况下再提交第 5 次单点尝试** ✗。
