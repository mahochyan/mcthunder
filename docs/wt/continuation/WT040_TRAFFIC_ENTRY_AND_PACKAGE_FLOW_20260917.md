# 河谷入口堵车与包内现代验证接续

基线：`8a09c995` 修正包，当前工作区 `mcthunder-cont`，原 main 未改。上一轮是实际进展：修正炮塔坐标并交付独立内部包。本轮继续同一“现代河谷独立可玩候选”目标，最多 A/B/C 三点，性能 HOLD、真人 PENDING。

## 两处导航缺陷与限定修复

1. **第 0 个路点没有可封闭的前序图边。** A/A2 相向停在道路 (-259.0378, 220.03) 与 (-252.3133, 220.03)，均指向 (-256.596, 220)。原逻辑每次等待后调用同一规划器，路径起点不变，始终没有恢复动作。现在仅在首个路点被真实车辆阻挡时使用既有的有限倒车/转向恢复；后续图边仍优先绕行。不改速度、恢复次数、碰撞或车辆支撑条件。
2. **行驶车辆驶离后，原阻塞仍永久封路。** 为带控制器的活车辆单独登记弱引用、实际阻塞位置与车型长度余量；只有原阻挡车辆离开该位置超过长度余量或实例已移除，才释放这条临时封路并重规划。墙体、停放车辆、残骸和已登记永久阻塞不按时间或距离自动过期；新的静态阻塞会撤销该边的动态分类。同目标重试继续保留永久阻塞记忆。

## 同条件短对照

`tests/run_river_entry_traffic_checks.gd`（早期诊断名 `diagnose_river_entry_traffic.gd`）使用真实河谷几何、两款生产 Actor、共享物理调度、正常团队 AI 的持续目标策略。只配置初始位置，不瞬移或直接写入行驶速度。默认执行 120 s 场景及 4 项断言；`--driver-only --diagnose-only` 可单独观察底层驾驶器。

- `logs/WT040-progress/entry-baseline-team.json`：通过固定 Godot 的脚本入口，在 `8a09c995` 构建干净源码目录执行同一新夹具，资源根路径和驾驶器字节哈希写入记录；原代码 120 s 仍在原位，重规划计数 30/47。该次尚为 3 项版断言，其中 2 项失败。
- `entry-check.json`：仅首路点恢复时，两车均离开堵点、豹到达原入口，但 T-80 仍沿旧封路形成长绕行。
- `entry-memory-final.json`：增加动态阻塞分类后的最终夹具 4 项通过。两车 60 s 内均离开原堵点超过 30 m，豹到达并停在原 A 点接近区；T-80 在 120 s 内接近原目标至 300 m 内。逐帧位移保持有限且不超物理速度余量。样本 t=118 s：T-80 约 (332,220)，豹约 (-503,121)。
- 早期 `entry-full-ai.json` 漏开团队的持续目标策略，底层驾驶器有限失败后没有团队重试；该诊断不作为完整团队行为证据。后继夹具显式启用与 TeamRange 相同的策略。

六套相关回归 **275 项、0 失败**：AI 驾驶 40、目标恢复 16、战术 39、团队交通 34、现代支撑 19、AppFlow 127。证据 `logs/WT040-traffic-memory/8a09c995-moving-block-wip/20260917-120730/`。永久障碍有限失败、两辆真实车辆相向会车的已有契约均仍通过。

## 自然整局，分别记录

仅首路点恢复的 `modern-match-entry-recovery.json`：389 s 自然终局、0∶78、4 个开火槽位、12 发射/21 接触/16 毁伤事件。异常静止峰值 **165→25 s**，原静止判据通过；总计 8 项中仍有 1 项失败：未达到至少 6 个开火槽位。没有调整判据、延长局时或提高速度。

含动态阻塞释放的 `modern-match-traffic-memory.json`：**332.6167 s 自然终局、票数 25∶0，8 项全部通过**。8 个槽位均实际开火，16 发射、29 接触、22 毁伤事件；双方分别到达 C/A 与 B/A 占领区；异常静止峰值 25 s。原规则、车型、出生、种子 44001、4v4/16v16 布局及判据均保留。所有记录器中的终场残骸计数均不代表全场死亡总数，8 个开火槽位也不等于 8 发炮弹。该结果证明本次工程 AI 整局通过，不代替正常玩家整局、其他种子或 10v10/16v16 容量验证。

## 包内诊断入口

之前外置脚本不能验证官方导出模板，现将已验证的两个夹具提升为包内可选诊断；源码测试改为调用同一实现，避免源码与导出包各维护一份逻辑。

- `-- --verify-modern-garage`：两车实际车库入口、绑定预览、装甲模式、配弹保存/重读、真实点击出战、三点/4v4 场景、重开与返回。无窗口检查明确只发 UI 信号；窗口检查才记真实输入和截图。
- `-- --verify-modern-life`：明确标注的平地交战夹具，脚本敌方炮手用正常武器发射；同玩家生命接触/毁伤/致死/扣票/等待/真实点击/保持车型与配弹。所有证据写入隔离 user 路径。源窗口入口 **9 项通过**，日志 `modern-life-entry.log`，事件和截图在 `modern-life-entry-userdata/Godot/app_userdata/PixelArmor/tests/modern_live_round/`。

两入口都采用独立存档，不加载或覆盖正常玩家档。仍不是“正常规则完整现代河谷玩家流程”；自然完整对局、包内完整流程与真人验收分别保留。后继 `4747d84d` 已含两入口与本轮导航修复，旧 `8a09c995` 包不含这些入口或本轮 AI 修复。

首次 `9c4b69e8` 构建在干净导入后被现有套件名称检查拒绝（新增诊断名没有 `run_` 前缀），未生成候选；`20260917-121228-452` 目录保留。将该夹具改为上述标准套件名，保留构建校验规则后重建。包内窗口驱动为 `tests/run_modern_package_checks.ps1`，按实际 BUILD_MANIFEST 校验源码身份、隔离存档和工作目录、检查退出码/标记/截图及包前后哈希；两分项均不宣称完整自然玩家对局通过。

## 实际独立包与原生窗口复验

源码身份 `4747d84deff8361e6fe9950ad544f6869c39e725`，构建目录 `backups/builds/031/4747d84deff8361e6fe9950ad544f6869c39e725/20260917-121625-710/`。`-Candidate -ModernRiver` 干净构建，9 套共 **325 项、0 失败**；导出后资源 58、窗口 59 项通过，实际退出码 0，未触发已登记例外。两车、对应模型/弹药/布局与河谷在必需内容内。

| 文件 | SHA-256 |
| --- | --- |
| ZIP | `9F1125C129D95AEFCE355C0AE6FAADFEFD9D50F824EE4721F434F676C2EAB48E` |
| EXE | `8E6C6A7782B22965569CE4CBA6CE7B7E6BD357C33302E2BE420AC22556DC17C9` |
| PCK | `B015FA05B08506C15941D39D9575F15F0FFB3234675D3B9C25771359C5EA7E26` |
| BUILD_MANIFEST | `59993BADA6DD3C90B161BB71B454FB4BD17D2F1BDCEAAC0779654845BDC093BA` |

直接运行该 EXE 原生入口，非外置脚本覆盖：

- `logs/WT040-package/4747d84deff8361e6fe9950ad544f6869c39e725/garage-20260917-122012-425/`：现代车库 **37/0、exit 0**，两车共四张截图。
- 同目录 `life-20260917-122133-712/`：同玩家实弹生命周期夹具 **9/0、exit 0**。life 1 的 T-80B 在 11.783333 s 被敌炮实弹造成乘员致死，票池 300→270 一次；正常点击再出击后 19.916667 s 生成 life 9，保留 T-80B 与 8 发配弹。事件 JSON、等待和再出击截图已复制。
- 驱动先核对实际 EXE/PCK 与 manifest.files 的哈希，再在运行后确认二者未变；以上两次均已执行该加强校验。

`release_ready=false`、`public_release=false`，性能 HOLD、真人 PENDING。37 项与 9 项分项通过不等于完整现代玩家整局通过。当前包完整历史玩家流程复验结果 **96 项、1 失败，exit 1，无超时，无缺图**，证据 `logs/034/4747d84deff8361e6fe9950ad544f6869c39e725/20260917-122351/`。M36 正常入口及实际四发配弹、两张历史地图自然终局/结算重读/下一局均通过；失败为首次返回后的车卡选择，不能把历史旧包 96/0 移作本包结果。

## 由真实窗口发现的后续缺口

实际现代河谷截图中，正式 BattleHUD/小地图/世界标记仍沿用单 A 点，尽管规则层已是三个据点。本轮后继源码已接通同一权威 snapshot：顶部最多三张状态卡、小地图实际 A/B/C 坐标与半径、归属/争夺颜色，地面圈沿用实地驾驶场景的地形贴合几何并提取共用构建器；移除河谷错误的原点 A 圈，历史单点场景保持原布局。小地图标题简化为“河谷枢纽 · 4v4”，不把布局规格充作比赛人数。尚未进入上述 `4747d84d` 包，不回填此包。

源码实际窗口 `tests/run_river_objective_display_checks.gd` **18/0、exit 0**，分别验证 A 友方、B 争夺、C 中立进度，snapshot 隔离、显示不改票池、真实圈顶点贴合地形、1280×720 与 1920×1080 不越界/重叠。最后截图 `logs/WT040-progress/three-objectives-final-shots/river_three_objectives.png` 已目视。这里不同归属是明确的呈现夹具，不冒充自然占领。相关 HUD **56/0**、多据点规则 **35/0**、车库前端无窗口 **23/0**，日志前缀 `three-objectives-run_*`。

## 自动点击短复现

`run_garage_frontend_checks.gd` 窗口短复现没有重现同一车卡失败，但依次暴露出部署按钮失败和训练页/部署按钮失败。新增只读输入诊断记录：模拟目标在训练页 (709,52) 或部署 (179,504.5)，原生光标始终是 (640,360)；按下到释放间 hovered 从按钮变为预览 SubViewportContainer/其他面板。证据 `garage-mouse-diagnostic.log` **29 项、2 失败**；更早 `garage-card-window.log` **29 项、1 失败**。

仅在窗口验证驱动中点击前用 `Input.warp_mouse` 同步原生光标，保留真实按钮按下/释放、选择判据和超时，不改变生产车库逻辑；增加越界拒绝和完整玩家流选择失败截图。相同车库窗口复现 `garage-mouse-synced.log` **29/0、exit 0**，坐标和 hovered 均与按钮相符。该结果证明短夹具中的鼠标漂移已消除；包内那一次车卡失败仍需后继包完整复验确认，尚不宣布已关闭。

T-80B 原缓存 hull 段未提供可继承材料标识，12 个 hull 区仍保留 unknown。`logs/WT040-progress/material-gap-audit.json` 为只读审计。附件节点的 RHA 不能反推车体；炮塔乘员实弹夹具通过也不代表这些未知 hull 材料已经补齐。
