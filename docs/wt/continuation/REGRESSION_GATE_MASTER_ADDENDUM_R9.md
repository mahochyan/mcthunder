# 回归门终版（第 9 次回填）：**111 套件全量门禁**

## 1. 终账（原始分类）
```
PASS = 104 · FAIL = 1 · ZERO_CHECKS = 4 · TIMEOUT = 2
```
## 2. 按**正确口径**复算（本会话确立的规则）
| 类别 | 数量 | 说明 |
|---|---|---|
| **实质通过** | **108** | 104 直接 PASS ✓ + **4 项"仅打印结束标记"**（`layout` · `query` · `projectile` · `moving_target_tick`）✓ —— 运行器规则本就是"结果行 **或** 标记" ✓（我的临时分类脚本没看标记 ✗，非套件问题 ✓） |
| **慢但通过** | **2** | `run_industrial_checks`（**962 s，595/0** ✓）· `run_industrial_battle_checks`（**~1001 s，15/1 = 基线** ✓）—— 二者只是超过我临时脚本的 301 s 上限 ✗ |
| **唯一真实失败** | **1** | `run_challenge_checks` **138/2** ＝ 已登记的**防守夹具能力边界**（脚本飞行员在 hard 变体被启用 AI 打瘫：`repairs=4 / shots=0`）✓ |
⇒ **111 套件中，除那 1 项已登记的夹具边界外全部通过** ✓✓

## 3. 本会话为门禁做的三件事
### 3.1 覆盖扩容：**32 → 111 套件**（累计新增 79 ✓）
轨迹 32 → 40 → 50 → 61 → 73 → 84 → 94 → 96 → 111 ✓；每批**先试跑、全绿或修好**才入列表 ✓
覆盖缺口 **147 → 68**（余多为窗口/玩家/网络/河谷类 ✓）
**价值实证**：补跑未覆盖套件**两次抓到我引入的回归** ✓（`ai_drive` 让行类 ✗ → `6937e684`；`partial_support` 支撑/转向类 ✗ → `c26ac2c1`），且两者**现已被常规门禁看住** ✓

### 3.2 判定口径修复（工具）
本环境 `Start-Process` 的 **`ExitCode` 常为 `$null`**（曾致运行器在 **import 步**中断 ⇒ 一套件未跑 ✗）＋捕获日志中文**乱码**致结果行正则漏判 ✗
⇒ 改为**以日志证据判定**（无脚本错误 ∧ 无意外错误 ∧〔结果行 0 失败 ∨ ASCII `CHECKS_PASS` 标记〕），退出码仅记录 ✓；**正反双向验证** ✓

### 3.3 默认超时修复
`industrial_checks` 需 962 s > 原默认 **900 s** ✗ ⇒ 默认超时提高至 **1500 s** ✓（否则两个已证通过的慢套件会被误判 ✓）

## 4. 分类规则（写入口径，避免以后误判）
| 类别 | 处置 |
|---|---|
| 逻辑/集成类且稳定绿 | **入默认门禁** ✓ |
| 单次 >240 s（慢） | **慢档**，不入门禁（长时档按需运行）✓ |
| **诊断电池**（如 T039-D：按设计有已知失败） | **永不入门禁** ✓ |
| 零检查输出（`turret_tick` · `multi_objective`） | **NOT_RUN** ✓ |
| 窗口/渲染/导出/流量（4 项） | **NOT_RUN**（环境依赖）✓ |
| 网络/房间类 | 需**多进程专用入口** ✓ |

## 5. 仍未结（不声称完成）
`challenge` 防守（B1–B4）· `T018-H01` 预算（A1/A2）· M26 坡上起步（C1–C3）· 8.5 m 包络（D1/D2）· 授权 E1–E4 · 资产路径 A/B/C · 工业战斗**既有**抵达红（建议立项）· 真人验收与性能（NOT_RUN）
