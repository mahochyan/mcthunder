# WT-039-R1 决策请求：地图"声明包络"与地形曲率冲突（7 轮取证后的唯一阻塞点）

- 状态：**两项修复假设均已被实测否证**，产品侧仅保留"可测化"改动（诊断字段，不改判定）
- 需要决定：**修复地形** 还是 **修正包络语义**（属作者/设计决策，我不擅自选择）

## 1. 事实链（全部本机实测，可复核）

| # | 事实 | 证据 |
|---|---|---|
| 1 | 最大包络代理卡死：`throttle=0.00`、`steer=-1.00`、**speed 0.000 连续 240+ tick** | `logs/route-traces/*`、探针 3194 tick 判 STALL |
| 2 | 卡死点 `(-120.8, 4.02, 19.8)`，坡度 **12.2°**（邻域 11.7–13.3°），**远低于上限 28°** | 探针实测 |
| 3 | 地图声明 `max_vehicle_size = (4.2, 2.4, 8.5)`；烘焙以**刚体盒**+**0.08 m 余量**校验 | `map_definition.gd:10`、`navigation_bake_pipeline.gd:27` |
| 4 | 烘焙**排除地面**（`query.exclude=[ground_rid]`）→ 地面曲率从未被检验 | 源码 + 新增字段实测 |
| 5 | 把地面纳入后：烘焙采样（≤3 m）**38/1786 受阻**；1 m 加密 **100 受阻**；集中在 x≈−120 侧翼路 | 新增 `curvature_failures` |
| 6 | 解析矢高：8.5 m 盒半长 4.25 m 处 **≈0.14 m > 0.08 m 余量** → 刚体盒必然楔入 | `VillageDefinition.height` 解析式推导 + 实测一致 |
| 7 | **真实车辆可通行**：M26（6.15 m、履带悬挂）孤立运行 **4409 tick 抵达**（重复亦然） | 探针实测 |
| 8 | 失败**不是**出生点/宽度/图连通问题 | 16/16 `request_path ok`；16/16 泊位零碰撞重叠 |

## 2. 两次被否证的修复（负结果，均已回退）

| 尝试 | 结果 | 原因 |
|---|---|---|
| **道路切坡（C0）**：走廊内线性纵坡 + 横向渐回 | `T018-03b` 受阻 **38 → 55（更差）** | 横向渐回形成**新横向坡 ≈15.6°**，在平台边缘制造新楔入点；纵向亦非 C1 |
| **地形加密（4 m → 2 m）** | 受阻 **38 → 40（实质不变）** | 说明根因是**解析曲率**而非网格面片 |
| 回退核对 | 地形文件**与基线零差异**，受阻**回到 38** | `git diff --name-only a1bac406..HEAD -- village_world.gd` 为空 |

## 3. 冲突本质（必须由人决定）

- 材料 **WT-019** 要求：路线图**不能与物理地面脱节**、**两队正常车辆能到达关键目标** → **已满足**（事实 7：真实车辆可通行；村庄整局 21/21、结构 54/54 全绿）。
- 地图同时又**声明**支持 **8.5 m 刚体包络** → **未满足**（事实 5/6）。
- 二者并非都可由我单方面满足：要么**改地形**（作者化工作，改变地图形态），要么**改声明语义/余量**（降低标准，需显式批准）。

## 4. 四个选项（请择一；我已备好执行路径）

| 选项 | 内容 | 代价/风险 | 我需要的授权 |
|---|---|---|---|
| **A. 地形作者化** | 对 x≈−120 侧翼路做 **C1 连续**的公路切坡（纵向 Hermite 纵坡 + 横向 C1 融合），并必要时处理其他坡脊 | 中等：需**完整地图回归**（`run_map_checks`/`run_historical_road_checks`/`run_village_battle_checks`/`run_structure_checks`/`run_industrial_checks`）；可能改变地图观感 | 允许改动**地图地形数据**（`village_definition.gd` / `village_world.gd`） |
| **B. 修正包络语义/余量** | 明确 `max_vehicle_size` 是**包围**包络（真实车带悬挂/履带多接触点），把烘焙的包络检查改为**接触模型**或按证据调整余量 | 快；但**改变既有标准**，且 `world_props`/`run_structure_checks` 亦消费该值 | 允许**调整校验标准**并接受"检查语义变更"记录 |
| **C. 保持诊断 + 登记缺口** | 不改地形与标准；`T018-03b` 保持 FAIL 作为**已知缺陷**，我转向 M26 爬行、其他既有红与未开工单 | 零风险；缺陷存续 | 无需授权（默认可继续） |
| **D. 修正测试代理** | 用产品实际"车体+行动装置"接触模型验证 8.5 m **包围**包络，而非无悬挂刚体盒 | 改变"最大尺寸代理"的含义，须显式说明 | 允许**改测试夹具语义** |

## 5. 若您不指定，我的默认动作

按 **C**（保持诊断、登记缺口），**不擅自改地形或标准**，把本轮之后的推进转向：M26 爬行定位（可能与本案同源）、`chassis_response` 与 `team_checks` 两个既有红、以及 **WT-036-R1 收尾（11 个 DEFERRED 的长预算重跑）**——并随时可执行 A/B/D。

## 6. 本轮交付物与边界

- 交付：本决策文档 + 两次否证与回退的日志（`run_map_checks-after-{grading,2m-cell,2m-revert}.log`）；
- **产品/地图净零改动**（`village_world.gd` 与基线逐字一致）；唯一保留的产品改动是**烘焙诊断字段**（不影响 `ok`）；
- 未触碰：车辆定义、图数据、坡度上限、脏文件、构建流程。
