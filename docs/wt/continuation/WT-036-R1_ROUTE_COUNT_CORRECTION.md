# WT-036-R1 更正：工业套件的**检查计数**（我此前的表述有重复计数）

## 1. 更正
我在此前两处文档与提交信息中写了"**256 条 capture 与 256 条 supply 路线**"——**错误** ✗。
正确算式（源码：`run_industrial_checks.gd` 四层循环 × 两张地图）：
```
车辆 4 × 队伍 2 × 槽位 8 × 目标 2（capture / supply）× 地图 2 = 256 条路线检查
  其中：capture 128 条 · supply 128 条
```
另加：**256 条出生净空断言**（每路线一条，由本分支加固新增）✓

## 2. 修正后的构成（总计 595，与实测一致）
| 组成 | 数量 |
|---|---|
| 路线检查（128 capture + 128 supply） | **256** |
| 出生净空断言（加固新增） | **256** |
| 结构 / 碰撞 / 转场 / 注册表等其余检查 | **83** |
| **合计** | **595** ✓（实测 PASS=595 FAIL=0 ✓） |

## 3. 结论不变
**工业套件 595 PASS / 0 FAIL（EXIT=0）** ✓，基线对照 **283/56 → 595/0** ✓；
被清除的 56 项＝**supply 36 · capture 19 · restart 1** ✓（此三项与计数更正无关 ✓）
