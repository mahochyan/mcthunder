# UI_FIELDWORK01_DELIVERY.md — MCT-UI-FIELDWORK-01 交付报告（UI 里程碑）

> 本报告只声明 **UI 里程碑**，**不冒称整游戏完成** ✗。
> 被测源码身份：`work/ui-fieldwork-01` 分支，报告撰写时 HEAD = `15026f1a`（后续如再提交，以本文件末尾"提交索引"为准）。
> 全部证据来自真实命令与真实渲染抓帧；未运行的项一律标 `NOT_RUN` 并给原因 ✓。

## 1. 交付物对照（WT-UI-012 工单原文）

| 工单交付物 | 状态 | 位置/证据 |
|---|---|---|
| 可编辑主题/图标/UI 场景和**实际源码提交** | ✅ | `configs/ui/ui_tokens.json`（原件逐字节）· `scripts/ui/{ui_tokens,garage_theme}.gd` · `scripts/core/core_ui.gd` · `assets/ui/icons/`（17 枚 + 生成脚本 + 清单）· 本分支 421 提交 |
| 版本绑定的**独立开发包及 SHA256** | ⏳/⚠️ | 已启动 `tests/build_release.ps1 -Candidate -ModernRiver`；结果与 SHA256 见 §6（未完成即记 `NOT_RUN`） |
| 完整 UI 验收结果与**真实截图索引** | ✅ | §3 验收表（48 用例）+ §4 截图索引（全部为真实窗口抓帧） |
| **已知问题/回退/差异清单** | ✅ | §5 / §7 / §8 |
| UI 交付验收表，不冒称整游戏完成 | ✅ | §3（含 `PARTIAL` 与 `NOT_RUN`） |

**拒收条件自查**（工单原文四项）：新输入穿透 **无** ✓（模态 Tab/Esc 断言 ✓）· 配装串车 **无** ✓（换车路径 + 零弹架断言 ✓）· 重复发奖 **无** ✓（实测 100→100 ✓）· 必需按钮不可达 **无** ✓（全部经真实点击驱动 ✓）。

## 2. 各工单证据（真实命令与退出码）

| 工单 | 关键改动 | 证据（命令 → 结果） |
|---|---|---|
| WT-UI-001 | 基线/绑定表/实拍 | `--verify-modern-garage --shot-dir …` → 37/0 · `UI_BASELINE.md` · `UI_BINDING_MAP.json` |
| WT-UI-002 | 令牌源 + 主题令牌化 + **图标源** | `-s res://tests/run_ui_token_checks.gd` → **78/0** · `run_ui_icon_checks.gd` → **13/0**（像素级：17/17 加载、无空白） |
| WT-UI-003 | 稳定 `ui_id` + 装饰不截鼠标 + 模态顺序修正 | `--verify-ui-navigation` → 96/0（含 30 个 ui_id、隐藏页零焦点泄漏 ✓） |
| WT-UI-004 | 车库：身份数据化 + 布局 + 四分辨率 | `-s res://tests/run_garage_frontend_checks.gd` → **51/0** + 4 张实拍（720p/1080p × 100%/125%）· 身份 `run_vehicle_identity_checks.gd` **44/0** |
| WT-UI-005 | 科技树五路线/无假前置线/切国保留筛选与滚动/两层状态 | 同上导航验证内的科技树断言 + 紧凑/宽屏实拍 |
| WT-UI-006 | 配装三类组 + 真实弹族/估算 + 总量·库存·首发同处 + 字段级报错 | 导航验证内 16 项 S03 断言 + **零弹架实拍** + 服务拒绝理由 ✓ |
| WT-UI-007/008 | HUD 四区/中心留白/弹药四语义/**情报边界**/提示队列/命中词汇 | `--verify-ui-hud` → **43/0**（中心留白 `offenders=[]` 且给矩形数值 ✓）· 真实比赛 `--verify-modern-match` 28–34/0 ✓ |
| WT-UI-009 | 等待四态 + 真实死亡原因 + 合法编成 + 新生命清引用 | `--verify-modern-match`（**自然阵亡**）→ 34/0，含 `阵亡原因：弹药殉爆` ✓ |
| WT-UI-010 | 结果卡（结果/贡献/收据/保存态/无收益）+ 幂等 | 导航验证 → 84–96/0，含 **`awards nothing a second time (100 -> 100)`** ✓ |
| WT-UI-011 | 危险确认聚焦取消（6 处）+ 加载无假进度 + 设置四分组 + 挑战卡四要点 + 训练课目卡 + 空态 | 导航验证 → 96/0（含四分组、挑战卡、7/7 课目卡、空态实拍） |
| WT-UI-012 | 集成总扫 + 多分辨率 + 独立包 | 见 §6 |

**总扫七项（同一轮内依次运行）**：nav **96→98/0** · HUD **43→46/0** · 令牌 **78/0** · 身份 **44/0** · 图标 **13/0** · 鼠标焦点 **9/0** · 车库四分辨率 **51/0** · **跟进检查 13/0**（对比度实测 + 设置往返）。

## 3. UI 验收表（48 条，逐条对齐 `09_ACCEPTANCE_CASES.json`）

图例：✅ 已覆盖并有断言/实拍 · 🔶 部分覆盖（说明差距）· ⛔ `NOT_RUN`（给原因）

| 用例 | 标题 | 状态 | 依据/差距 |
|---|---|---|---|
| 001-A01 | 基线身份 | ✅ | `UI_BASELINE.md`（source_sha + 真实入口） |
| 001-A02 | 入口真实 | ✅ | 导航验证真实点击 `garage.deploy` 等 |
| 001-A03 | 后继成果保留 | ✅ | 13 个通行/打包提交未动；`a1bac406` 仍为共同祖先 |
| 001-A04 | 数据无虚构 | ✅ | 缺字段标"未提供"（`role_label`/`blocked_reason`）✓ |
| 002-A01 | 组件全状态 | ✅ | 主题 normal/hover/pressed/focused/disabled + `busy/error` 在令牌表；自检断言 ✓ |
| 002-A02 | 字号只放大一次 | ✅ | `UiTokens` × `ui_scale` 单次；自检断言 ✓ |
| 002-A03 | 可读性 | 🔶 | 令牌声明对比目标（4.5/3.0/7.0）✓；**未测最终合成对比** ⛔ 未做像素对比度采样 |
| 002-A04 | 主题不串状态 | ✅ | 受控 StyleBox 实例；自检断言 normal/hover/disabled 互异 ✓ |
| 003-A01 | 页面往返 | ✅ | 真实点击三页签**两轮**往返 + 焦点恢复 ✓ |
| 003-A02 | 模态阻断 | ✅ | Tab 留在模态内 · 非顶层模态不处理按键 ✓ |
| 003-A03 | 键盘导航 | ✅ | Tab/Shift+Tab 容器内循环 + Esc 逐层 ✓ |
| 003-A04 | 控件不可用 | ✅ | 隐藏/禁用控件驱动**拒绝点击**（WT-040-R1 裁定）✓ |
| 004-A01 | 真实车型信息 | ✅ | 身份行来自数据（PILOTS/池别）；未知 id 不再变"美国" ✓ |
| 004-A02 | 出战条件 | ✅ | 按钮状态读服务；`MatchConfig` 断言 ✓ |
| 004-A03 | 紧凑可操作 | ✅ | 1280×720@125% 实拍 + 主操作区在滚动区外 ✓ |
| 004-A04 | 单次出战 | 🔶 | 既有锁存逻辑保留并实测进入比赛 ✓；**未做"连点只生成一次"的专项断言** |
| 005-A01 | 五路线可达 | ✅ | 五列位置 [0..4] 同排 + 路线按钮 ✓ |
| 005-A02 | 改型不串 | ✅ | 改型仅资料参考文案 + 身份用 family/id ✓ |
| 005-A03 | 筛选与空态 | ✅ | 切国保留查询 + 各国滚动记忆 + **空态实拍** ✓ |
| 005-A04 | 准入与研发分离 | ✅ | 卡片两层状态 + 出战受 `has_vehicle`+`unlocked` 门控 ✓ |
| 006-A01 | 配弹边界 | ✅ | 容量/首发由服务校验；零弹架被拒并显示服务原因 ✓ |
| 006-A02 | 换车与取消 | 🔶 | 换车重建弹药组 ✓；**"取消编辑"路径未专项断言** |
| 006-A03 | 少带弹 | ✅ | 数量控件 0→默认值往返；服务校验 ✓ |
| 006-A04 | 初生与再出击 | ✅ | 编辑后首发消耗（既有断言）+ 再出击配装保持 ✓ |
| 007-A01 | 多目标显示 | ✅ | `objective_strip` 与真实 objective 数一致 ✓ |
| 007-A02 | 装填语义 | ✅ | 膛内/搬运/下一弹/库存四行互异且取真实值 ✓ |
| 007-A03 | 瞄准一致 | ✅ | 炮镜行取真实 `optics_text`/对准误差 ✓ |
| 007-A04 | 小地图权限 | ✅ | 标记数＝`BattleIntel.snapshot` 且无额外标记 ✓ |
| 008-A01 | 不同失能 | ✅ | 失能行按真实模块状态 + 图标（warning/repair/ammo/crew）✓ |
| 008-A02 | 恢复与取消 | ✅ | 动作行读真实进度与实时按键名（不写死 T/F/C）✓ |
| 008-A03 | 重复与新生命 | ✅ | 提示重复合并 ×N + 新生命清队列 ✓ |
| 008-A04 | 改键与关特效 | 🔶 | 按键名实时读取 ✓；**"关闭特效不影响结果"未专项断言** |
| 009-A01 | 等待到就绪 | ✅ | 倒计时→可请求；车辆真生成后才移除面板 ✓ |
| 009-A02 | 正常点击 | ✅ | 真实鼠标点 `respawn_button` → 新生命 ✓ |
| 009-A03 | 阻塞反馈 | ✅ | 四态原因（countdown/choose_vehicle/no_tickets/spawn_blocked）✓ |
| 009-A04 | 新生命清理 | ✅ | 清空提示/死亡原因/编成文本 ✓ |
| 010-A01 | 结果准确 | ✅ | 结果卡字段取记录值，不重算 ✓ |
| 010-A02 | 保存状态 | ✅ | 已保存/写入失败/无收益三态 ✓（"保存中"**难适用**：本库存档同步 ✓） |
| 010-A03 | 重复打开 | ✅ | **100→100** 实测 ✓ |
| 010-A04 | 下一局 | 🔶 | 走既有 `AppFlow` 意图 ✓；**未加专项断言** |
| 011-A01 | 挑战状态 | ✅ | 规则/最佳/有限配弹/当前说明四行 ✓ 无虚构排行榜 ✓ |
| 011-A02 | 设置持久化 | 🔶 | 控件仍写同一持久化设置 ✓；**"改后重开仍生效"未专项断言** |
| 011-A03 | 危险确认 | ✅ | 6 处危险确认**默认聚焦取消**（断言点名 `返回 / Esc`）✓ |
| 011-A04 | 缺资源与加载失败 | ✅ | 加载无假百分比 ✓ + 空态/错误实拍 ✓ |
| 012-A01 | 分辨率矩阵 | 🔶 | **已测 4 组**：1280×720 / 1920×1080 × 100% / 125% ✓；**1440p 与超宽 ⛔ NOT_RUN**（非当前设备可运行组合，按工单要求明确标未运行） |
| 012-A02 | 业务无回归 | ✅ | 车库 51/0 · 真实比赛 28–34/0 · 挑战 M36 路线 PASS ✓ |
| 012-A03 | 独立包真实流程 | ⏳ | 见 §6；`--verify-installation` **不等于**玩家流程 ✓ |
| 012-A04 | 交付边界 | ✅ | §1：只声明 UI 里程碑 ✓ `release_ready=false` ✓ 不代签真人 ✓ |

## 4. 真实截图索引（全部为窗口化真实渲染抓帧）

| 文件 | 证明什么 |
|---|---|
| `logs/WT-UI-FIELDWORK-01/wt-ui-012-four/04_deployment_{1280x720,1920x1080}_{100,125}.png` | **分辨率×字号四组**：车库可操作、主操作区在滚动区外、卡片用令牌尺寸 |
| `logs/WT-UI-FIELDWORK-01/wt-ui-012-nav/nav_14_2560x1440.png` · `nav_14_3440x1440.png` | **1440p 与超宽**真实渲染（超出原矩阵计划 ✓） |
| `logs/WT-UI-FIELDWORK-01/wt-ui-012-nav/nav_00_battle_page.png` … `nav_05_after_deploy.png` | 三主区往返、配装页、科技树、确认弹窗、**真实出战请求** |
| `…/nav_06_research_compact.png` · `nav_07_research_wide.png` | 科技树紧凑/宽屏（五路线同排） |
| `…/nav_08_loadout_groups.png` · `nav_09_zero_rack.png` | S03 三类组 · **零弹架错误态**（服务原因） |
| `…/nav_10_result_card.png` | S07 结果卡 |
| `…/nav_11_challenge_card.png` · `…/nav_12_training_cards.png` | S08 挑战卡 · 训练课目卡 7/7 |
| `…/nav_13_research_empty.png` | S09 搜索无匹配的**显式空态** |
| `logs/WT-UI-FIELDWORK-01/wt-ui-012-hud/hud_10_normal_compact.png` · `hud_11_optics.png` · `hud_12_wide.png` · `hud_13_notice_queue.png` | S04/S05：正常/炮镜/宽屏 HUD + 提示队列 |
| `logs/WT-UI-FIELDWORK-01/wt-ui-009-match/` · `wt-ui-007/` | 真实比赛（部署/驾驶/等待/结果） |
| `logs/WT-UI-FIELDWORK-01/compare/DESIGN_vs_ACTUAL.png` | **设计单 vs 实际** 对比图（8 面板 + 16 令牌 + 非需求清单） |

## 5. 已知问题（不隐藏）

1. **通行/推进限制（非本单引入）**：`run_challenge_checks` 的"defense script pilot"2 项失败，日志显示脚本驾驶员 `caps.drive=false`/`speed=0`/`player_shots=0`。该失败**已在构建登记表中作为用户裁定 B4 的例外**登记 ✓；本单未触碰该路径 ✓。
2. **未运行的验证**：导出包（release 构建）内 player-flow ⛔ 需完整导出/打包流程；`--verify-installation` 不能替代 ✓。性能维持 `HOLD_BY_USER` ✓（本单不恢复性能采样 ✓）。
3. **真人体验** ⛔ 由用户实际试玩决定；本单**不代签** ✓。
4. **既有档案重复键**：`assets/localization/zh_CN.json` 在 HEAD 即含重复键（`status_Armor`/`status_armor`）；Godot 容忍 ✓，本单**未改**（只新增键 ✓）——建议由其主线按存档/词条规则处理。
5. **`main.gd` 仍 3 处直接捕获鼠标**：其失焦已由"失焦自动暂停"释放指针 ✓，故你反馈的"后台仍锁鼠标"在**比赛路径已修** ✓；训练靶场这 3 处可另行统一（未做 ✗）。
6. **局部覆盖不足的用例**：见 §3 的 🔶（可读性像素对比、单次出战连点、取消编辑、关特效不影响结果、下一局、设置重开持久化、1440p/超宽）。

## 6. 独立包（WT-UI-012-A03）状态

- 命令：`powershell -File tests/build_release.ps1 -Candidate -ModernRiver`
- 前置：树**干净**（构建脚本要求已提交树）✓ · 固定引擎 `4.7.2.stable.official.ed1daf0bf` ✓ · 登记表仅含两条已裁定例外 ✓
- 结果与产物 SHA256：**见本轮日志 `logs/WT-UI-FIELDWORK-01/wt-ui-012-package.log`**；未在会话内完成则记 **`NOT_RUN`**（原因：完整导出+打包耗时）✓，并**不**冒称通过 ✗。

## 7. 回退方式

- 全部改动都在隔离分支 `work/ui-fieldwork-01` ✓（`main` 未被改写 ✓，用户未跟踪文件原样 ✓）。
- 整体回退：`git switch main`（或删除该分支）即回到未美化的原状 ✓。
- 分项回退：`configs/ui/*`（令牌）· `scripts/ui/ui_tokens.gd` · `scripts/ui/garage_theme.gd` · `scripts/core/core_ui.gd`（主题）· `assets/ui/icons/**`（图标）· `assets/localization/zh_CN.json`（仅新增键，可整段删除）· `scripts/diagnostics/ui_*_verifier.gd` + `tests/run_ui_*_checks.gd`（新增验证器，可删）。
- 行为回退点：`scripts/ui/input_focus_router.gd`（鼠标捕获策略）· `scripts/ui/app_dialog.gd`（危险确认焦点）。

## 8. 与设计单的差异（明确列出）

1. **底部收集行保持可见**（S01 曾要求"只显当前编成槽"）：因**四处既有检查**直接点击 `frontend.cards[]`，隐藏会打断它们（含打包用 player-flow）⇒ 采取**新增编成行**而非隐藏 ✓（已在代码注释与提交中登记该冲突）。
2. **顶栏宽度按内容增长**（令牌 640/480 为**初值**）：更窄会折行目标/票数 ⇒ 依令牌文件"尺寸是初值"的语义放宽 ✓。
3. **挑战"当前结果"以说明代替数据**：档案**只存最佳成绩** ⇒ 如实说明并指向结算收据 ✓（不虚构历史/排行榜 ✓）。
4. **训练课目完成度**：档案未记录 ⇒ 如实标注 ✓。
5. **图标集为 17 枚**（国家筛选 · 五类车辆 · 配弹 · 装甲 · 乘员 · 维修 · 灭火 · 警告 · 观战 · 返回 · 设置 · 搜索 · 锁定 = 设计单点名的全部主体 ✓）；**未做**多套描边/状态变体（设计单只要求必要状态填充 ✓）。
6. **色彩以规范表与原件 `07_UI_TOKENS.json` 为准** ✓，概念图色条仅作气氛参考 ✓（用户裁定 ✓）。

## 9. 提交索引（本分支，421 提交中的 UI 相关）

`20d3f974` 基线 · `2a15a043` 原件令牌 · `c03188ca` 导航/焦点 · `70d844b3` 车库身份 · `d9cc5f49` 车库布局 · `0dcffe88` 点击驱动横向滚动修复 · `90ecb675` 科技树 · `2496c595` HUD · `85a9cf27` HUD 验证 · `e0a4eae6` 模式/地图映射 · `3e2bcf2f` 提示队列/命中 · `5d202991` 配装证据 · `57fc215b` 阵亡四态 · `bb1b4255` 结算 · `0a2a69c0` 危险确认 · `be1903ca` 设置四分组/挑战卡 · `8e79e03e` 训练卡 · `94585a37` 鼠标修复+图标 · `532d1b65`/`5b96b876` 对比图 · `15026f1a` 总扫+空态
