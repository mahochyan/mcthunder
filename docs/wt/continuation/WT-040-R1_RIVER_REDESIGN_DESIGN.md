# 河谷队内拥堵 —— **修复设计（选项 b：结构化重设计）** ✓ 基于全部实测证据 ✓

> 状态 ✓：本设计**未实施** ✗（等您在 **a/b/c** 中裁定 ✓）。其**关键洞察**来自我四次失败的**共同点** ✓。

## 1. 已确诊的成因（遥测 + 图结构，均有证据 ✓）
- 同扇区**相邻出生位**（A↔A2、B↔B2 ✓）**共用一条通道行** ✓（`aisle_z = sign(depth+32) = ±712` ✓）；
- 遥测 ✓：A 于 `t=18.3`、A2 于 `t=16.8` 在**同一条边** `(−278,712)→(−287,712)` 上**互相让行** ✓（相隔 1.5 s ✓）；
- 每次让行超时 ⇒ `replan` **封边** ✓ ⇒ **被封的正是共享通道边** ✗ ⇒ 后续路线被迫**绕行** ✓ ⇒ `blocked_total` **0→1→2** ✗✓；
- A3/A4/B3/B4 记录 **0 次封边** ✓ ⇒ 拥堵**仅限相邻出生对** ✓（定位精确 ✓）。

## 2. 四次失败与它们的**共同点** ✓（本设计的出发点 ✓）
| # | 尝试 | 结果 |
|---|---|---|
| 1 | 跨重规划保留跳点索引 | 无效 ✗；加强版破坏 3 项检查 ✗ |
| 2 | 阻挡边**时间过期**（统一/按来源） | **38/40** ✗ |
| 3 | 阻挡边**空间释放**（统一/按模式） | **39/40** ✗ |
| 4 | 河谷**发车通道按槽错开** | **任务可达性 0/8** ✗✗（**最接近，但方式错了** ✗） |
**共同点** ✓：1–3 都在动**被契约守护的"阻挡边记忆"** ✗；4 动的是**地图导航图** ✗ —— 而它**替换**了原有的扇区级横向连线 ✗ ⇒ 出生点与部署线**被断开** ⇒ 0/8 ✓✓。

## 3. **设计（关键：必须是"叠加"而非"替换"）** ✓✓
**改动点** ✓：`scripts/maps/river_junction_navigation.gd` 的发车段（第 51–63 行 ✓）。
**做法** ✓：**完整保留**现有扇区级通道与其全部连线 ✓（⇒ **所有旧路线仍然存在** ✓，
⇒ 第 4 次那种 0/8 可达性断裂**在构造上不可能复发** ✓✓），**在其之上叠加**：
1. 为每个出生位增加**一条短支线** ✓，从出生点指向**其专属的、略微错开的汇入点** ✓（相邻槽位错开 ≥ 10 m ✓）；
2. 汇入点**仍连接原通道** ✓（只是错开位置 ✓）⇒ 相邻两车**不再同点汇合** ✓；
3. 支线宽度沿用 `line(..., false, 8)` ✓（与现有一致 ✓，**不缩图** ✗、**不改让行契约** ✗、**不改速度/伤害/规则** ✗）。

**兼容性论证 ✓**（这次**先论证再实施** ✓）：
- **图是超集** ✓ ⇒ 原有可达性**不变** ✓（`check_river_task_mapping` 的 8/8 有构造性保证 ✓）；
- **不触碰 `ai_path_driver` 的阻挡边逻辑** ✗ ⇒ `run_ai_drive_checks` 的两条让行契约**不受影响** ✓（1–3 的失败模式不复现 ✓）；
- **不触碰** `set_goal` 的 `same_retry`/清空语义 ✗ ⇒ 永久障碍契约不受影响 ✓。

## 4. 验证（**同批**执行 ✓；任一不绿即**逐字节回退** ✗）
1. `check_river_task_mapping` ⇒ 必须仍 **4/4** ✓（尤其 8/8 可达 ✓）；
2. `run_ai_drive_checks 40/0` ✓ · `run_ai_tactics_checks 39/0` ✓ · `run_ai_recovery_checks 16/0` ✓ · `run_ai_intercept_checks 106/0` ✓ · `run_team_checks 67/0` ✓ · `run_map_checks 48/0` ✓；
3. **正面验收**（前四次**缺的就是它** ✓）：120 s 窗口内 `blocked_edges` **不递增**（≤1 ✓）且 `to_goal` **单调下降** ✓；
4. 失败 ⇒ `git diff <green-sha>` = **0 行** ✓（逐字节回退 ✓）。

## 5. 回滚 ✓
单文件改动 ✓；失败即 `git checkout <green-sha> -- scripts/maps/river_junction_navigation.gd` ✓（本会话已如此回退四次 ✓，均验证为 0 行 ✓）。

## 6. 为什么这值得一次裁定 ✓
- 它是**第 ④/⑤ 步的唯一前置** ✓（辅助能力/观瞄/结算**早已实现并受门禁保护** ✓，见 `WT-040-R1_STEP4_COVERAGE_AUDIT.md` ✓）；
- 设计**满足您此前的红线** ✓：不缩图 ✗ · 不提速 ✗ · 不改契约 ✗ · 不传送 ✗ · **且是叠加式**（⇒ 旧路线不丢 ✓）；
- 与第 4 次失败相比 ✓，**唯一区别就是"叠加"** ✓ —— 且该区别**可构造性证明**（超集 ⇒ 无断裂 ✓）。

---

## 7. **细化（自我更正 ✗→✓）**：仅"叠加支线"不足以消除争用
读现行代码（第 51–63 行 ✓）后确认 ✓：
- 各出生位的**竖直汇入**本就在**各自 x** 上 ✓（`line(p, Vector2(p.x, aisle_z))` ✓）⇒ **并不冲突** ✓；
- 遥测受阻边 `(−278,712) → (−287,712)` ✓ 是通道行上的**水平段** ✓ ⇒ 即扇区级
  `for i in range(1,columns.size()): line(Vector2(columns[i-1],aisle_z), Vector2(columns[i],aisle_z))` ✓；
- ⇒ **争用发生在共享水平段** ✗ ⇒ 我先前"**只加每槽支线即可**"的说法**不严谨** ✗✓。

### 因而需改为**针对水平段**的机制（候选 ✓，仍需实施验证 ✓）
**由内向外的定向汇入** ✓：
1. 对每个扇区，把各出生位按其 **x 距扇区中心的顺序**排序 ✓；
2. 每槽只**向自己一侧**沿通道移动并汇入 ✓（**不穿越他槽的段** ✗）；
3. 保留原有竖直汇入与两端连部署线的连线 ✓（⇒ 仍为**超集** ✓ ⇒ 可达性**不下降** ✓）；
4. **不触碰**阻挡边逻辑 ✗ 与 `set_goal` 语义 ✗（⇒ 两条让行契约不受影响 ✓）。

**为何可能有效 ✓**：把"**多槽共用一条水平段**"变为"**每槽只走属于自己的段**" ✓ ⇒ **结构上**不再存在"**同一条边被两车抢**" ✓ ⇒ `blocked_total` 不再递增 ✓。
**仍属假设** ✗：**必须**以第 4 节的**正面验收**（`blocked_edges` 不递增 ✓ · `to_goal` 单调下降 ✓）判定 ✓；不达标即**逐字节回退** ✗。

### 结论（诚实的当前状态 ✓）
- **成因已实证** ✓（遥测 + 图结构 ✓）；
- **机制候选明确** ✓（定向汇入 ✓，直指共享水平段 ✓）；
- **仍**需一次**实现 + 同批验证** ✓ 才能判定 ✓ —— 我**不**把"设计已备好"说成"修复已成功" ✗。

---

## 8. **第五次尝试**（每槽私有通路 ✓ 纯叠加 ✓）—— 回归全绿 ✗ **正面验收未过** ⇒ **逐字节回退** ✓
### 改动 ✓（`river_junction_navigation.gd`，**+13 行 / −0 行** ⇒ **严格超集** ✓）
为**每槽**新增一条**沿自身 x 的直通部署线**通路 ✓（原有每一行**全部保留** ✓）。
**前两项验收通过** ✓✓：`check_river_task_mapping` **4/4** ✓（**8/8 可路由** ✓ —— 第 4 次的 0/8 **未复现** ✓）；**6 套件回归全绿** ✓（40/0 ✓ 39/0 ✓ 16/0 ✓ 106/0 ✓ 67/0 ✓ 48/0 ✓）。

### **正面验收未过** ✗（120 s 窗口 ✓）
```
t=5  A to_goal=994 blk=0 | t=35 1067 blk=1 | t=65 1092 blk=2 | t=95 1175 blk=2   ← 仍振荡增大 ✗
[river-blocks] A blocked "-278.000_712.000:-287.000_712.000" blocker=A2             ← **与失败前完全相同** ✗
totals: reached=[] fired=0 tickets={1:300,2:300}
```

### 🎯 **关键洞察（本次真正收获 ✓✓）**
**新增的通路从未被使用** ✗：受阻边与之前**逐字相同** ✓ ⇒
**规划器偏好最短路径** ✓，而**共享通道比私有通路更短** ✗ ⇒ **加边 ≠ 改变选择** ✗✓。
⇒ **一般化结论** ✓：**"给图加边"不能解决拥堵 —— 只要规划器仍偏好被争用的那条路** ✗；
修法必须**改变代价或几何** ✓（缩短私有通路 ✓ / 让共享段不再最短 ✓ —— 属**设计取舍** ✓，与 **a/b/c** 一并裁定 ✓）。

### 处置 ✓
**逐字节回退** ✓（`git diff b1ba1880` 干净 ✓）；回退后 `check_river_task_mapping` 仍 **4/4** ✓。

---

## 9. **几何测量：更锐利的发现**（纯算术 ✓，未运行引擎 ✓）
### 出生位精确坐标（`river_junction_definition.gd:97` ✓）
```gdscript
base_z = deployment_z = 680 ; sector = -1 if i<2 else +1 ; slot = i%2 ; row = int(slot/4) = 0  ← row 恒 0 ✓
p = (sector*260 + (slot-1.5)*18, sign*680)
```
team 1 / size 4 ✓ ⇒ x = **−287** ✓ / −269 ✓ / +233 ✓ / +251 ✓，**z 一律 = +680** ✓
**交叉验证** ✓：记录的受阻边 `−278.000_712.000 : −287.000_712.000` ✓ —— 端点 **−287 正是槽 A 的 x** ✓✓

### 结构事实（关键 ✓）
1. **出生位就在部署线上（z=680 ✓）** ✓，而**通道在 z=712** ✓ ⇒ **在出生位后方 32 m** ✗ ⇒ **发车先倒退 32 m** ✗；
2. 各槽下到部署线还须先到**扇区端列（sector ± 110 ✓，距槽位 110–230 m ✗）** ✓ ⇒ **共享水平段既长又必经** ✗ ⇒ **争用点即在此** ✓；
3. ⇒ **新加的私有通路（32 m 直落 ✓）本应被优先选用** ✗ ⇒ **却未被采用** ✗ ⇒ ⇒ **"最短路径偏好"不足以解释** ✗✓（第 8 节的解释**需要补充** ✓）。

### 下一个决定性测量（**测试侧记录，我权限内** ✓）
现有记录只有 `wp_index` / `path_len` ✓，**无节点序列** ✗ ⇒ 需在记录器中**追加规划器实际选中的节点表** ✓（只加**观测** ✓，不改行为 ✗），即可定性：
**为何弃短取长** ✗ —— 是**代价函数** ✓、**规划器缓存/重规划时机** ✓、还是**该边被阻挡记忆排除** ✓。

---

## 10. 🎯🎯 **第 5 次尝试"零效果"的决定性机制**（只读 ✓，已定论 ✓）
### 代价函数（`scripts/ai/drive_navigator.gd:56-85` ✓）
```gdscript
cost := {start: 0.0}
f = cost[a] + nodes[a].distance_to(nodes[finish])              # f = g + h（直线启发 ✓）
proposed = cost[current] + nodes[current].distance_to(nodes[next])   # 边代价 = 欧氏长度 ✓
if proposed >= cost.get(next, INF): continue                   # Dijkstra / A* ✓
```
⇒ **规划器确实选最短路** ✓ ⇒ 32 m 的私有通路**本应入选** ✗

### **真正原因** ✓✓：我的"新"边与**既有边完全重合** ✗
| 现有（原有代码 ✓） | 我的新增（第 5 次 ✗） |
|---|---|
| `line(p, Vector2(p.x, aisle_z))` ✓ —— 出生点 **(x,680)** → 通道 **(x,712)** ✓ | `line(Vector2(p2.x, aisle_z), Vector2(p2.x, sign_z*depth))` ✗ —— 通道 **(x,712)** → 出生点 **(x,680)** ✓ |
⇒ **两者端点与走向完全相同** ✗ ⇒ ⇒ **只是把同一条竖线又画了一遍** ✓ ⇒ 而**出生点就在部署线上** ✓、
**目标是前方目标点** ✓ ⇒ ⇒ **没有提供任何朝目标方向的新走法** ✗ ⇒ **图只增重复边** ✓ ⇒ **行为必然不变** ✓✓

### 由此**排除**的三项嫌疑 ✓（不必再测 ✓）
| 嫌疑 | 判定 |
|---|---|
| 代价函数偏好 | **排除** ✓（它选最短路 ✓，问题在于**新边不新** ✗） |
| 重规划时机/缓存 | **排除** ✓ |
| 阻挡记忆排除新边 | **排除** ✓（受阻边是**通道水平段** ✓，与我的竖线无关 ✓） |

### ⇒ **修正后的修法方向**（**待裁定** ✓）
私有通路必须**朝目标一侧**延伸 ✓ —— 从各槽**向前越过部署线**开辟**独立车道** ✓，
**而不是**把通道接回出生点 ✗（那只是重复既有边 ✓）。

---

## 11. **第 6 次尝试**（每槽**前方**私有车道 ✓ 纯叠加 ✓）—— 回归全绿 ✗ **正面验收未过** ⇒ **回退** ✓
### 改动 ✓（**+17 / −0** ⇒ 超集 ✓）
每槽新增：出生点 → **前方 10×k m 的私有行**（`z = sign*(680 − 10k)` ✓）✓ + 该行**连到两端列** ✓ + 端列**向下延伸到该行** ✓（既有每一行保留 ✓）。
**结构验收全过** ✓✓：`RIVER_TASK_MAPPING_PASS` ✓（**8/8 可路由** ✓）；**6 套件全绿** ✓（40/0 ✓ 39/0 ✓ 16/0 ✓ 106/0 ✓ 67/0 ✓ 48/0 ✓）。

### **正面验收未过** ✗ —— 但**签名变了** ✓✓（证明改动**确实生效** ✓）
```
旧（第 5 次 / 基线）: 受阻边均在 z=712 通道行
本次:   t=18.3 "-278.000_712.000:-287.000_712.000"  ← 旧通道边（仍在 ✓）
        t=68.5 "-238.000_680.000:-260.000_680.000"  ← **新边：部署行 z=680** ✓✗
```
### 🎯🎯 **结构化结论**（本轮最大收获 ✓✓）
**争用只会"搬家"，不会消失** ✗ —— 我的前方车道**终点仍汇入同一条端列** ✓ ⇒ 两车下到部署行后**又在同一段合并** ✗。
⇒ **瓶颈跟着"汇合点"走，而非跟着某条边** ✗ ⇒ ⇒ **只要所有车必须汇入同几条列/行，加多少条车道都只是把堵点搬到下一个汇合处** ✓✓。

### ⇒ **真正的修法只有两类** ✓（**均超出"加边"范畴** ✓）
1. **分开漏斗** ✓（**地图拓扑**改造 ✓ —— 即选项 **b 的完整形态** ✓）；
2. **让发车串行化/错峰** ✓（**行为规则** ✓ —— 需动 `ai_path_driver` ✓，受**让行契约**守护 ✗ ⇒ 属选项 **c** ✓）。
⇒ **六次尝试共同证明** ✓：**该问题不能靠"改记忆"或"加边"解决** ✓，必须动**拓扑**或**行为规则** ✓。

### 处置 ✓
**逐字节回退** ✓（`git diff b1ba1880` 干净 ✓）；回退后 `check_river_task_mapping` **通过** ✓。

---

## 12. **第 7 次尝试**（每槽直连内侧路网行 ✓ 纯叠加 ✓）—— **破坏可达性 0/8** ✗✗ ⇒ **回退** ✓
### 结果 ✓
**+13 / −0** ✓（超集 ✓）⇒ 但 `check_river_task_mapping`：**`every river slot's task point is routable` = 0/8** ✗✗（与第 4 次同型 ✗）⇒ **立即逐字节回退** ✓（回退后 **PASS** ✓）。

### 🎯 **又一条结构事实** ✓✓
新列是**约 460 m 的直线前方通道**（`line(p4,(p4.x,sign*220),road=false)` ✓），沿途每 24 m 新增节点 ✓；
`road=false` 使其取**地形高度** ✓ ⇒ 节点落在**不可行驶地形**上 ✗ ⇒ 触发可通行过滤 ✗ ⇒ **整图对该任务点不可达** ✓。
⇒ ⇒ **河谷内侧并非"直线可走"** ✗ —— **路网之所以绕行，是地形与障碍所致** ✓ ⇒
⇒ **任何"直线开新路"的加边方案都会失败** ✓✓（这解释了第 4 次与第 7 次为何同型失败 ✓）。

### 七次尝试的**最终汇总** ✓
| # | 路线 | 结果 | 定性 |
|---|---|---|---|
| 1–3 | 改**阻挡边记忆** | 无效 / 38-40 / 39-40 ✗ | 契约守护 ✓ |
| 4 | 发车**替换式**错开 | 0/8 ✗ | 断连 ✓ |
| 5 | **叠加**私有通路 | 零效果 ✗ | **重复边** ✓ |
| 6 | **前方**私有车道 | 堵点**搬家** ✗ | **汇合点** ✓ |
| **7** | **直连内侧路网** | **0/8** ✗ | **地形不可直线通过** ✓ |
⇒ **结论（证据充分 ✓）**：该问题**不能**靠"改记忆 / 加边 / 开直线"解决 ✓ ⇒ 必须**改地图拓扑**（沿地形重新布线 ✓）或**改行为规则**（发车错峰 ✓，受让行契约守护 ✗）✓ —— **两者均需您裁定** ✓。

---

## 13. 🎯 **决定性发现 + 两处归因作废 + 一次条目冲突**（**须停下询问** ✓）
### 13.1 地图定义原文（`river_junction_definition.gd:18-27` ✓）
```gdscript
static func layout(team_size: int) -> Dictionary:
    assert(team_size in [10,16])                                    # 只支持 10 / 16
    … "deployment_z": 480.0 if team_size==10 else 680.0 …
    "status":"design_preview", "combat_admitted":false              # 自称预览、未准入战斗
```
### 13.2 **已验证的三条事实** ✓
| # | 事实 | 证据 |
|---|---|---|
| 1 | **`layout(4)` 触发断言** ⇒ 我的探针 4-case **无效** ⇒ **该数据点作废** ❌ | 探针日志 `SCRIPT ERROR … 'deployment_z'` ✓ |
| 2 | **4v4 实战实际使用 16v16 布局** ✓（发布构建剥离 `assert` ✓ ⇒ `deployment_z=680` ✓、`crossings=LANES` ✓）⇒ 图 **857/1024 节点** ✓（**余量仅 167** ✗） | `probe_river_graph_size.gd` ✓：10→408 ✓ / **16→857** ✓ |
| 3 | **地图自己声明 `combat_admitted: false` + `status:"design_preview"`** ✓✓ | 同上源码 ✓ |
### 13.3 **作废两处归因** ❌
| 出处 | 归因 | 判定 |
|---|---|---|
| §12（第 288 轮） | "地形不可直线通过" ✗ | **作废** ❌（推断，未获证明） |
| §13 早段（本轮） | "图容量超限" ✗ | **作废** ❌（按 16 布局估算 ~1017 < 1024 ✓） |
⇒ **第 7 次 `0/8` 的确切原因：未定** ✗（**明确标注，不编造第三说** ✓）

### 13.4 ⚠️ **条目冲突**（按红线**停下询问** ✓）
| 您的阶段指令 ✓ | 地图自身定义 ✗ |
|---|---|
| "**河谷接入正式团队场景（先 4v4 闭环）**" ✓ | `assert(team_size in [10,16])` ✗ · `combat_admitted: false` ✗ · `status: "design_preview"` ✗ |
⇒ **冲突成立** ✓ ⇒ **我停止在河谷上继续尝试** ✗ ⇒ **提交您裁定** ✓：
**A** 把河谷**正式提升为可战斗地图**（改定义：支持 4v4 ✓ + `combat_admitted: true` ✓ ⇒ 属**范围/准入**决定 ✗）；
**B** 维持其"预览"定位 ✓ ⇒ 则第 ② 步"河谷团队闭环"**改由既有正式地图承担** ✓（已可用 ✓）；
**C** 只做**证据归档** ✓（7 次尝试 + §13.2 三条事实 ✓），不再改动 ✓。
