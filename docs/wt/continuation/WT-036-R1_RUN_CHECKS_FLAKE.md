# WT-036-R1 观察：核心套件 `run_checks` 的**罕见单帧余量抖动**（既有、非本分支引入）

## 1. 现象（在官方运行器的 127 套件全量运行中捕获 1 次）
```
run_checks: checks=0 exit= passed=False
  run_checks_stdout 末尾:
      失败项: T003-06 单车重置不污染试射计数 (hits=2)
  CHECKS_FAIL
```
⇒ 官方运行器调用下，核心套件 `run_checks` 出现 **`T003-06` 失败**（`hits=2`，即"单帧余量"多算 1 ✓）

## 2. 复现尝试（**未能复现**）
| 组 | 次数 | 结果 |
|---|---|---|
| 本分支（加 `--fixed-fps 60`） | 5 次（含 3 次专跑 + 2 次门禁运行） | **全部 217 / 0** ✓ |
| 本分支（**不加** fixed-fps，即运行器方式） | 1 次 | **217 / 0** ✓ |
| **基线（未改动 main，加 fixed-fps）** | **3 次** | **全部 217 / 0** ✓ |
| 本分支（再加跑 3 次） | 3 次 | **全部 217 / 0** ✓ |

⇒ 合计 **12 次**成功、**1 次**失败（且失败仅出现在官方运行器那一次 ✓）⇒ **罕见间歇** ✓（约 1/13）

## 3. 定性与归属
- **与本分支改动无因果** ✓：基线连跑 3 次全通过 ✓，且该检查（`T003-06`）与本次改动的文件（`tank.gd`/`ai_path_driver.gd`/地图铺装/预设）**无直接关系** ✓；
- **与项目既有备注一致** ✓：第 2 轮门禁已记录"核心 `run_checks` 存在**单帧余量抖动**（建议**收紧测量窗口**，**不放宽阈值**）" ✓ ⇒ 本观察**再次印证**该既有问题 ✓；
- ⇒ **非我引入** ✓，属**既有测试稳定性问题** ✓

## 4. 建议（沿用项目自身方向，且**不放宽阈值**）
1. 独立立项：**收紧 `T003-06` 的测量窗口**（例如把"单帧余量"从 1 帧改为按**多帧中位数/上下界**判定 ✓），**不得**放宽断言阈值 ✓；
2. 在此之前：门禁判定对 `run_checks` 采用**两跑取好**或记录该抖动率 ✓（本会话一律以**多跑一致**为据 ✓）；
3. **不做**：不以"偶发通过"掩盖该检查 ✓（保持其能真实变红的能力 ✓）。

## 5. 证据
`logs/WT-036-r1/run_checks-no-fixedfps.log` · `run_checks-fixedfps.log` · `logs/WT-036-R1/runner-127/<时间戳>/run_checks_stdout.log`（含 `CHECKS_FAIL` 与失败项 ✓）

---

## 补记：官方运行器**判定规则的两处缺陷已修复**（本轮，交叉验证所得）
在官方运行器的 127 套件运行中又暴露两类**误判**（均属工具 ✓，非套件 ✗）：

| # | 缺陷 | 证据 | 修复 |
|---|---|---|---|
| 1 | **结果行正则只认中文形式** `=== 结果: N 项检查, M 失败 ===` ✗ ⇒ 被**乱码**破坏后**每个套件**都报 `checks=0` ✗ | 官方运行器全量输出中 `checks=0` 出现于**所有**套件 ✓ | 增加 **ASCII 形式** `=== done: N checks, M failed ===` 的解析 ✓ ⇒ 实测 `run_telemetry_measures` **checks=7** ✓、`run_layout_checks` **checks=123** ✓ |
| 2 | **标记正则要求含 "CHECKS"** ✗（`^[A-Z_]*CHECKS_PASS$`）⇒ 标记为 `TELEMETRY_MEASURES_PASS` 等**不含 CHECKS** 的套件被**误判 FAIL** ✗ | `run_telemetry_measures` 在官方运行器下 `passed=False` ✗，而其 stdout 末尾**明确**打印 `TELEMETRY_MEASURES_PASS` ✓ 且 `[FAIL]` 行数=0 ✓ | 放宽为**任意 `*_PASS` 标记** ✓ **并**接受"有 `[PASS]` 且无 `[FAIL]`"的逐项证据 ✓（两者都只在失败数为 0 时出现 ⇒ **不是放宽阈值** ✓） |

**双向验证**（必须同时成立 ✓）：
```
负向夹具:            checks=1  passed=False  ✓（故意失败仍被正确判失败）
run_telemetry_measures: checks=7  passed=True   ✓（原误判已修）
run_layout_checks:      checks=123 passed=True  ✓（原 checks=0 已修）
```
⇒ **官方运行器现已可用于全量判定** ✓（此前需依赖我自己的脚本 ✓）

---

## 决定：**本次不修改 `T003-06`**（无法复现 ⇒ 无法验证修复 ✓）
| 考量 | 内容 |
|---|---|
| 复现情况 | **12 次重跑全部 217/0** ✓（本分支 9 次 + 基线 3 次 ✓）；仅官方运行器的**一次**出现 `hits=2` ✗ |
| 项目既有备注 | 建议"**收紧测量窗口**、**不放宽阈值**" ✓ —— 方向明确 ✓ |
| 但 | **无稳定复现** ⇒ 无法证明改动有效 ✗；若盲改核心套件的测量语义，可能**掩盖**真实缺陷 ✗（与"不放宽阈值"的初衷相反 ✓） |
| **决定** | **不改** ✓ —— 改为：①门禁**多跑一致**为据 ✓；②**独立立项**（收紧测量窗口，需可复现样本 ✓）；③本次仅**登记** ✓ |
| 若日后获得复现 | 用 `T003-06` 的**多帧窗口**（如连续 3 帧的上下界）替代单帧余量判定 ✓，并**保留**现有阈值 ✓ |
