# WT-040-R1 移动环诊断（链路第 3 环）：**重规划回退**，非寻路失败

> 依据您的链路定位法：**任务 → 路径 → 移动 → 观察 → 瞄准 → 发射**，一次只查一环。
> 任务环已闭合 ✓、路径环 8/8 可达 ✓ ⇒ 本轮定位 **移动环**。

## 1. 实测（120 s 诊断窗口，`logs/WT-040-R1/river-movement-diag2.log`、`river-churn-diag.log`）
| t | A: wp/路径长 | replanned | **task_events** | A2: wp/路径长 | replanned | **task_events** |
|---|---|---|---|---|---|---|
| 5 | 1/69 | 2 | **1** | 1/43 | 3 | **1** |
| 35 | 4/70 | 3 | **1** | 9/55 | 4 | **1** |
| 65 | 12/70 | 3 | **1** | 18/55 | 4 | **1** |
| 95 | **6/82** ✗ 回退 | 4 | **1** | **9/59** ✗ 回退 | 5 | **1** |

`to_goal` 同步振荡：A 994→1067→976→1071 ✗；A2 609→671→611→700 ✗（**无净收敛**）

## 2. 结论（每条都有观测支撑）
1. **不是寻路失败** ✓：`planning.failed = 0`、`unreachable = 0 ` 全程 ✓；
2. **不是我的任务层** ✓：`task_assigned` 事件**恒为 1** ✓（签名门控生效 ✓，任务只派一次 ✓）；
3. **是驱动器自身重规划** ✓：`replanned` 持续上升（2→3→3→4 / 3→4→4→5 ✓）；
4. **且重规划会回退进度** ✗：跳点索引在重规划后**由 12→6、18→9 回退** ✓、`path_len` 随之变化（70→82、55→59 ✓）⇒ **每次重规划都从路线前段重新开始** ✗ ⇒ **净推进≈0** ✓。

## 3. 下一步修复（**只此一处**，不改速度/时步/地图/规则）
在 `scripts/ai/ai_path_driver.gd` 的重规划路径上**保留行进进度**：重新规划后**从"当前位置前方最近的跳点"续行** ✓，
而不是把 `waypoint` 重置回路线开头 ✓；并**复用**仍有效的前缀（若新路线与旧路线共享前缀 ✓）。
**验收**：
- `wp_index` 随时间**单调不减**（允许跳变但不得回到路线前段 ✓）；
- `to_goal` **单调下降**至 0 ✓（当前 994→1071→976→1071 ✗）；
- `replanned` 仍可上升 ✓（重规划本身允许 ✓，但不许丢进度 ✓）；
- 回归：`run_ai_drive_checks` 40/0 ✓ · `run_ai_tactics_checks` 39/0 ✓ · `run_team_checks` 67/0 ✓ · 目标映射 4/4 ✓。

## 4. 边界（不声称）
- 观察/瞄准/发射**仍未出现** ✗（因两队尚未接近 ✓）；
- 完整比赛**未验证** ✗；240 s/120 s 均为**诊断窗口** ✓，不改正式规则 ✓。
