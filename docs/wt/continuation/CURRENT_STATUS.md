# 当前摘要（指向最新源码 · 门禁 · 构建）

> 生成于轮 197。**本文件是唯一"当前状态"入口**；更早的报告（ROUND1–ROUND5、R4–R10 增补）保留为**历史**。
> 分支 `work/continuation-20260913` · 基线 `main @ a1bac406…`（**未改动** ✓）

## 1. 最新源码（本摘要对应）
| 项 | 值 |
|---|---|
| 分支 / HEAD | `work/continuation-20260913`（提交号见 `git -C <cont> rev-parse HEAD`） |
| 基线 | `a1bac406d2bc12b32c7f7d130590f1c1a17907c9`（`main` 全程未动 ✓ porcelain 恒 379 ✓） |
| 远端 | 已推送 `origin`（最近一次 `663f4cbf`；其后提交随本轮补推 ✓） |
| 产品代码净变更 | `scripts/` 61 文件 / +3,780 −21（另 **E1 授权**的 `export_presets.cfg` +55 ✓） |

## 2. 最新门禁（**127 套件，双路径一致**）
```
官方 tests/run_suite_checks.ps1 : 125 PASS / 2 FAIL   （红项＝已定性非回归）
独立脚本（逐套件复跑）          : 125 PASS / 2 FAIL   （逐套件一致 ✓）
```
| 两项红 | 定性 |
|---|---|
| `run_industrial_battle_checks` | **既有基线红**（与未改动基线逐项一致 ✓）——**中央抵达率/交通失效**，建议独立立项 ✓ |
| `run_challenge_checks` | **已登记**：防守夹具能力边界（您已裁定 **B4** 保持登记 ✓） |
- **179 个 `run_*.gd` 全部有据归类、无未试跑残留** ✓（门禁 127 · 慢档 4 · 诊断电池 1 · 零输出/需实参窗口 28 · 环境依赖 8 · 既有红 1 · 演示失败 1 ✓）
- 运行器 4 处判定缺陷**已修复并双向验证**（负向夹具仍判失败 ✓，未放宽任何断言 ✓）

## 3. 最新构建（**E1 已授权并实跑**）
| 项 | 结果 |
|---|---|
| 预设 | `export_presets.cfg` 新增 **`Windows Server`**（`dedicated_server=true`）+ **`Windows Client`** ✓ |
| 导出 | 两次 **exit=0** ✓（各 exe 104.08 MB + pck 92.71 MB ✓） |
| 启动 | 导出的 server **headless 实启**、stderr 空 ✓ |
| 入库策略 | 构建产物在 `backups/`（**gitignored** ✓ 不入库 ✓） |

## 4. 资产侧
| 项 | 状态 |
|---|---|
| 96 车适配产物 | **96/96** ✓（位置自断言 **delta=0** ✓、`source_unchanged=true` ✓、86.86 MB 入库 ✓） |
| 资产 ⑤（layout/binding） | draft 已生成 ✓；**待作者**每车 6 类字段 + 签收 ✓（`WT-030D-R2_AUTHOR_DATA_REQUEST.md`） |
| E3/E4 | **未授权** ⇒ NOT_RUN / `HOLD_BY_USER`（**零采样** ✓） |

## 5. **本阶段新主交付（您 2026-09-15 指令）的当前进度**
> 目标：苏德两辆现代样车在河谷完成一次正常团队战 + 交付对应源码的独立可运行包。

| 步骤 | 状态 | 证据 |
|---|---|---|
| ① 用 AI 实际对局并记录数据 | **已能对局** ✓（见 §6），**尚未**达成"正常交火" ✗ | `tests/record_river_ai_match.gd` · `logs/WT-040-R1/` |
| ② 河谷接入正式团队场景 | **已接通** ✓（机制层）；**AI 长距离交通失效** ✗ 阻断实战 | `scripts/maps/river_team_range.gd` · `river_team_definition.gd` |
| ③ 两辆现代样车战斗包 | **未开始** ✗（查证结论见 §7） | — |
| ④ 实际操作接通（含烟幕/辅助武器之一） | **未开始** ✗ | — |
| ⑤ 完整流程 + 独立包 | **未开始** ✗ | — |

## 6. 河谷团队战：**已接通但未达实战**（本轮实测）
- **能跑** ✓：8 个 AI（4v4）在**已授权 16v16 布局**上初始化、`phase=playing`、图/出生位/目标点全通、stderr 无错 ✓；
- **不能战** ✗：逐车诊断（`[river-actors]`）显示 AI **速度仅 0.7–4.9 m/s 且位置来回振荡**、距目标 600–900 m 几乎不缩短 ⇒ **155 s 零交火** ✗；
- **根因定位过程（三个真实缺口，逐个修掉）**：① 河谷无 `MapDefinition`（已补 ✓）；② 图误用战略设计图（已改用 `RiverJunctionNavigation` ✓）；③ **AI 目标点仍指向旧 `TeamArena.GOALS`**（已覆盖为河谷 A/B/C ✓）；④ 现存：**长距离 AI 交通** ✗。
- 边界诚实：河谷自述 **`design_preview / combat_admitted=false`** ⇒ 本场景**未**列入车库 `IDS` ✓，定位为**工程性 AI 对局测量** ✓。

## 7. 现代样车战斗包：**查证结论（直接回应用户第 1 步）**
| 查证 | 结果 |
|---|---|
| 全库 `admission` | **510 处全为 `candidate_only`** ✗ |
| 全库真实 `combat_definition` 对象 | **0 个** ✗ |
| 两车现有材料 | 仅 `assets/reference_data/candidates/*.json`（~210 KB **参考轨** ✓，项目刻意与"战斗配置"分离 ✓） |
| 生产配置 | 仅 **4 辆历史车**（`VehicleCatalog.IDS` ✓） |
| 准入 | `SPECS` 命中 ⇒ `preview_only` ✗；`candidate_only` ⇒ `not_applicable` + `config_incomplete` ✗ |
⇒ **做法**（既不等作者也不用通用模板 ✓）：**可实测字段**由工具从模型/适配产物生成并核对 ✓；**参考字段**从 `candidates` 取值并**逐字段标注出处 + 标"待设计确认"** ✓；**纯设计项**逐条列出 ✓；随后把 `preview_only` 硬拒改为**基于完整配置与验证状态**的判断 ✓。

## 8. 已知阻塞与归属
| 阻塞 | 归属 |
|---|---|
| **河谷 AI 长距离交通（爬行/游走）** | 本分支可攻 ✓（下一阶段头号指标 ✓） |
| 工业战斗抵达率（既有红） | 建议独立立项 ✓ |
| 资产 ⑤ 作者字段 + 签收 | **作者** ✓ |
| E3/E4 | **用户授权** ✓ |
| 现代样车战斗包 | 本分支可做 ✓（设计项需您确认 ✓） |

---

## 【2026-09-15 · 第 ③ 步（两辆现代样车战斗包）状态】**本阶段最新**
> 详版见 `WT-040-R1_STEP3_DELIVERY.md` ✓ · 数字出处见 `WT-040-R1_EVIDENCE_TABLE.md` ✓ · 待您输入项见 `WT-040-R1_INPUT_REQUEST.md` ✓。

### 已完成（均由**项目自身校验器**验证 ✓）
| 项 | 结果 |
|---|---|
| 两车适配产物 | T-80B **新建** ✓ · 豹2 首次生成 ✓（98 行记录全 verified ✓；重跑**逐字节确定** ✓） |
| `geometry` 实测 | 15 必需字段 ✓ · **自校验 29/29 PASS** ✓（且 **adapter↔source 逐项一致** ✓） |
| `facts`（带引用 ✓） | 速度/倒车/转速/质量/发动机/弹药容量/乘员角色 ✓ |
| `assembly`（6 项 ✓） | `125mm_2A46_2` vs `120mm_Rheinmetall_L44` 等**逐车不同** ✓ |
| `armor` 17 zone 草案 | 逐 zone 带节点+行号+材料 ✓；T-80B **12 项如实 `unknown`** ✓（不硬套 ✓） |
| `modules`（7 kind · 10 行 ✓） | 弹架**精确配平** 10+14+14=**38** ✓ / 10+16+16=**42** ✓ |
| `crew` | 角色**引用** ✓ + 位置**派生并标注** ✓（T-80B 3 人无装填手 ✓ 豹2 4 人有 ✓） |
| **`definitions`（车/炮/弹）** | **两车全 0 错误** ✓✓ |
| 一条命令的流水线 | `tests/run_modern_vehicle_pipeline.ps1` ✓ **9 步全绿** ✓ |
| 数据完整性 | **三项回查**（armor ✓ facts ✓ crew/assembly/modules ✓）+ **环↔AABB 交叉** ✓ |

**缺口**：shape 门 **24 → 9**（豹2 25 → 10）✓，方法为"**每次下降对应实际提供并接线的字段**" ✓。

### 未完成（**逐项归属明确** ✓）
| 项 | 归属 |
|---|---|
| **2 项几何修正**（炮盾下限 ✓ · 轮廓去重 ✓）—— 成因**已解析证明** ✓、影响面**已证**（仅 T-80B `hh` 0.054→0.0812 ✓）、**已算术预验证** ✓ | **我方** ✓（**未落**：门禁占用引擎 ✓） |
| 复合装甲 `response_profile` ✓ · 弹种集 ✓ · 三项设计值 ✓ | **设计** ✓ |
| `dimensions.*`（须文献原值+推导 ✓）· `assembly.year/suspension/mount` ✓ | **史料** ✓ |
| **准入判断**（档案自称 `runtime_admitted:false` ✓） | **您裁定** ✓ |
| 河谷队内拥堵 | **待您选 a/b/c** ✓（**b 机制已细化** ✓，**未实施** ✗） |

### 门禁
`gate-solo` 运行至 **120/127** ✓（**0 失败** ✓，正跑 `run_industrial_*` 等长套件 ✓；判据用 **CPU 累积** ✓ 与**套件日志写入** ✓，**不看**总日志时间戳 ✗）。

### 产品代码改动（**仅 1 处 8 行** ✓）
`scripts/content/role_mapping_audit.gd` ✓（`GUN_MESH_HINTS` 末位追加 `"gun"` ✓ —— 修 T-80B 炮口测量 ✓，已回归 `run_modern_model_mount_checks 41/0` ✓）。
其余产品文件：`modern_model_mount_adapter.gd` ✓ 与 `river_junction_navigation.gd` ✓ **逐字节等于基线** ✓。

---

## 【2026-09-15 · 补充：**门禁裁决**与**构建受阻**】**本阶段最新**

### ✅ 官方门禁：干净运行 = 基线（**126 PASS / 2 FAIL**）✓✓
三次运行全部跑完 ✓；**最干净的一次**（`gate-solo`，HEAD `99afbe5c` ✓）**精确复现基线** ✓，两红**正是已登记的非回归** ✓：
`run_industrial_battle_checks` ✓（既存到点红 ✓）· `run_challenge_checks` ✓（**140 项**登记夹具边界 ✓，您裁定 **B4** ✓）。
⇒ **我的改动零回归** ✓；且 `run_modern_model_mount_checks` ✗ **仅在回退前那次**出现 ✓、后两次均无 ✓ ⇒ **回退由门禁自身确认** ✓。
（有效总数 **128** ✓：runner 在含 `run_art_checks` 时追加 `run_menu_fire_handoff_checks` ✓。）

### ✅ 两项几何修正**经证伪循环落地** ✓✓
先装断言 ⇒ **红在预测的两处** ✓（T-80B 炮盾高度 ✗ · 豹2 轮廓重合点 ✗）⇒ 落修正 ⇒ **全绿** ✓ ⇒ 层级探针：**T-80B `LayoutValidator` 4 → 0** ✓、**豹2 7 → 2** ✓（仅剩 2 项**设计** ✓）；9 步流水线 `MODERN_PIPELINE_OK` ✓。

### ⚠️ **真实构建受阻**（既存工具链缺陷 ✗，**按红线未改** ✗）
`build_release.ps1` 在 **`fresh_import`** 步停下 ✓（响亮失败 ✓ `No verified release ZIP created` ✓），但逐份证据显示：**导入实际成功** ✓（`[DONE] reimport` ×2 ✓、`[DONE] loading_editor_layout` ✓、**0 条错误关键词** ✓），`stderr` 仅 1 条 WARNING ✓，而 **`exit_code=null`** ✗ ⇒ 即 **PowerShell `Start-Process` 退出码 `$null` 陷阱** ✓ ⇒ **脚本把成功的步骤判失败** ✗。
**两条路（待您选）** ✓：
1. **授权我修一处退出码判定** ✓（一行级 ✓ —— 但**属改构建流程** ⇒ **需您明确授权** ✗）；
2. **接受"包校验暂缓"** ✓，以 **应用流程 127/0** ✓ · **辅助能力 51/0** ✓ · **科技树 50/0** ✓ · **无注入真实对局**（进行中 ✓）作为本阶段运行证据 ✓。
**另记** ✓：`backups/builds/PixelArmorClient.exe`（104.1 MB · preset.4「Windows Client」✓）**本已存在** ✓ ⇒ 导出路径可用 ✓；缺的只是 **`BUILD_MANIFEST.json`** ✗（仅由该脚本写出 ✓）。**我未手工复现它** ✗（避免绕过构建流程 ✗）。

### 待您裁定汇总 ✓
**①** armor 三项（含**准入** ✓）· **②** 河谷 **a/b/c**（b 机制已细化 ✓，**未实施** ✗）· **③** G–L 外部输入（两车仅剩 **弹种集** ✓ 与**复合装甲规则** ✓）· **④** 构建：**选项 1 或 2** ✓。
