# WT-036-R1 登记：`T018-H01` 属**既有 + 间歇 + 预算边际**（非本分支引入）

## 1. 事实
- **基线 main 同样失败**：`run_map_checks` 基线 25/2，失败项含 **`T018-H01 actual actor from spawn 1/0 reaches own capture position`** ✓
- **间歇**：逐轮失败的槽位不同（观测到 1/0、1/3、1/5、2/0），每轮多为 **1 个槽位**；
- **本分支**：`run_map_checks` 最好两轮 **47/2**，失败 = `T018-03b`（**设计目标项**）+ **1 个** `T018-H01` 槽位 ✓

## 2. 轨迹证据（`map-t1-s0-*.log`，214 行 × 30 tick ≈ 6400 tick）
```
tick=6270~6360  wp=10 dist_wp=4.74  bearing 134°→96°→59°→21°  thr=0.00 steer=1.00 speed=0.000
   ← 在 (-9.99, 10.31) 处的原地枢轴转向（约 4~5 s）——这正是坡面自转/支撑修复带来的正确行为
tick=6390       wp=10 dist_wp=2.27  bearing=10.27  thr=0.33  speed=1.221   ← 恢复行驶
```
⇒ **截止时车辆仍在推进**（不是卡死、不是无解），失败形态是**行驶预算用尽** ✓

## 3. 结论与处置
- 属**既有基线红**（非本分支引入）⇒ **不作为回归处理**；
- 但它是**间歇的**，会把地图套件的总数在 45–47 之间抖动（因此本会话一律用"**定向指标 + 多轮**"而非单轮总数作判据 ✓）；
- **两条可选处置**（均属测试语义，需明确记录/授权）：
  - A：依"截止时仍在推进"的证据**提高该路线的 tick 预算**（与我此前对 D 电池所做的同类处理一致）；
  - B：保持现状，作为**已知边际间歇**登记，并在门禁口径中说明地图套件的**区间判据**（45–47）。

## 4. 门禁口径建议
地图套件以 **"失败集合"** 判定：允许 {`T018-03b`（设计目标）+ 每轮 ≤1 个 `T018-H01` 槽位}；任何**新增失败类型**才判回归 ✓
