# WT-036-R1：战斗套件**恢复至基线水平**（静止项转绿，仅剩既有抵达红）

## 1. 结果（采用策略：优先级 + 双时限重规划）
```
run_industrial_battle_checks   exit=1  PASS=15  FAIL=1   (1001.4 s)
  唯一失败: at least three actual slots from each team physically reach central approaches
```
| 检查 | 基线（未改动） | 本分支（各策略） |
|---|---|---|
| `at least three actual slots … reach central approaches` | **FAIL**（既有 ✗） | **FAIL**（同 ✓ = 既有） |
| `no healthy actor … stationary for 90 seconds` | PASS ✓ | **PASS ✓（已修复）** |

⇒ **与基线逐项一致（15/1）** ✓ ⇒ 我在该套件上**不再有任何引入项** ✓

## 2. 修法回顾（四轮迭代，全部留证）
| 轮 | 策略 | 结果 |
|---|---|---|
| 1 | 仅"无主车辆"→恢复 | `ai_drive` ✗；战斗静止项 ✗ |
| 2 | **任意车辆**→恢复（2.5/5.0 s） | `ai_drive` ✓；村庄 ✗ |
| 3 | 对峙→**显式失败** | `ai_drive` ✓ 村庄 ✓ 地图 ✓；**抵达减少** ✗ |
| **4** | **优先级 + 双时限重规划**（采用 `6937e684`） | **四门全 ✓**：`ai_drive` 40/0 ✓ · 村庄 21/0 ✓ · 地图 48/1（仅设计目标）✓ · **战斗 15/1（=基线）✓** |

**判据要点**：停放车必须**倒车**（检查含 `reverse`）· AI 车**不倒车**（村庄时序极紧）· **不放弃**（放弃减少抵达）· 高优先**先动**、低优先 **1.5 s** 后也动（路径分叉解对峙）· 静态几何**永不计时** ✓

## 3. 意义
本会话唯一由我引入的**整类回归**（让行/僵持处理）已**全部消除**，且**四个相关套件同时达标** ✓
剩余失败一律为**既有**（基线同样失败）或有明确裁定请求 ✓
