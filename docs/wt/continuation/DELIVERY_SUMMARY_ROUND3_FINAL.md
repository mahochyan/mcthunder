# 第 3 轮最终账目（补编：供应链消除后的收束）

> 承接 `DELIVERY_SUMMARY_ROUND3.md`；本补编记录**最终实测账目**与**定性结论**，均已复跑。

## 1. 门禁终值（同一套件、基线对照）
| 套件 | 基线 main | 本分支（终值） | 关键 |
|---|---|---|---|
| `run_industrial_checks` | **283 / 56** | **595 / 0（退出码 0）** | 256 路线检查（128 capture + 128 supply）+ 256 净空断言 + 83 其他 |
| `run_historical_road_checks` | 29 / 4 | **33 / 0** | M26 四条基线红清除（含**两处 C 类铺装**之一） |
| `run_map_checks` | 25 / 2（+22 项检查前） | **47 / 2** | 失败＝**我新增的设计目标项** + ≤1 个**既有间歇**槽位；**按失败集合判定** |
| `run_village_battle_checks` | 21 / 0 | **21 / 0** | 让行修复未扰动七单位出生推进 |
| `run_slope_pivot_checks`（新） | —（不存在） | **3 / 0** | 已纳入 `tests/run_suite_checks.ps1` 默认门禁 |
| `run_structure_checks` | 54 / 0 | **54 / 0** | — |

## 2. 缺陷/边界定性（本轮全部有证据）
| 项 | 定性 | 关键证据 |
|---|---|---|
| `supply` 36 项 | **已消除**（既有 + 机制＝未铺装地面 × 到点低油门） | 轨迹读出机制 → 授权 C 类铺装 → **36 → 0** |
| `restart frees old world` | **夹具时序**（非产品缺陷） | 等价 setup 探针三轮 `released/new_round/map_kept` **全真**；`_transitioning` 守卫 × 固定 8 帧 |
| `T018-02` | **已修复**（我曾引入的回归） | 六变体实测 → "**仅无主车辆**触发恢复" → 地图两轮通过 + 村庄 21/0 |
| `T018-H01` | **既有 + 间歇 + 预算边际** | 基线同样失败；逐轮槽位不同；截止时仍在推进 |
| `challenge` 防守 | **夹具能力边界** | `repairs=4 / shots=0`，hard 150 s 超时 |
| M26 坡上起步 | **调校冲突**（九次驱动侧修复全否证） | 10.4° 坡、`thr=0.77` 无法起步 |

## 3. 资产侧（终值）
- 96 车**只读审计**：≤15,000 三角面断言成立（合计 1,147,100，最大 14,990）✓
- 角色映射 **58/96 六角色齐全**；炮口**逐车实测**、撤销伪造来源 ✓
- **3 车独立适配产物 + 双哈希**（源只读、写后重测源哈希未变）✓
- **⑤ 可执行部分通过**：产物内含 `MuzzlePoint`、炮口解析为**作者节点**、六角色可解析、哈希一致 ✓
- **未声称**：完整 `check_scene`/`check_file`（需作者 layout/packet）· 全量 96 车（≈70 MB，待授权）· ⑥ 授权口径

## 4. 仍需裁定（打包 `DECISION_REQUEST_BUNDLE.md`）
`T018-H01` A1/A2 · `challenge` B1–B4 · M26 C1–C3 · **8.5 m 包络 D1/D2** · 授权 E1–E4

## 5. 索引
`DOC_INDEX.md`（本目录全部文档一页可查）· `NEXT_ACTION.md`（进度/交接入口，含第 3 轮段落）
