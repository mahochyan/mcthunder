# WT-036-R1：`T018-02` 回归（我引入）与两次修法的权衡

## 1. 回归事实（决定性对照）
| | `run_map_checks` | `T018-02 停放车绕行` |
|---|---|---|
| **基线 main**（未改动） | 25 PASS / 2 FAIL（新增检查前规模） | **`arrived` 84.9 s ✓ 通过** |
| 本分支 | 46 PASS / 3 FAIL | **`yielding` 250 s ✗ 永久让行** |

`T018-02` 判据：`blocked.phase == "arrived" and recovered`（事件里必须出现 `reverse`）。

## 2. 机制（已定位）
我在 **WT-039-R1** 为修"楔死车"给卡死窗口加了门限：
```gdscript
stuck.observe(pos, expecting_progress and absf(cmd.throttle) > 0.1, delta)
```
而 `yielding` 时驱动器**故意把 `throttle` 置 0**（前方有物理障碍）⇒ **窗口永不累计 ⇒ 恢复永不触发** ⇒ 对停放车**永久让行** ✓

## 3. 两次修法与结果
| 修法 | `T018-02` | 村庄 | 结论 |
|---|---|---|---|
| **A** 门限改为 `throttle>0.1 或 phase=="yielding"` | **修复** ✓（不再出现在失败列表） | **20/1 ✗**（"七个 AI 单位离开出生点"失败） | 门禁拒绝 → **回退** |
| （更早）完全去掉门限 | — | 亦 20/1 ✗ | 早已回退 |

⇒ **村庄那条检查是约束**：任何"让行期间进入恢复（会倒车）"的做法都会扰动 AI 出生推进 ✓

## 4. 下一版修法（不经"卡死窗口"，避免倒车扰动）
**让行超时**：若 `phase=="yielding"` 持续超过 `AI_YIELD_TIMEOUT_S`（新增常量）且**位置几乎未变**，
则**把当前阻塞边标记为 blocked 并立即 `_plan()` 重规划**（**不倒车**）✓
- 与楔死修复互补：楔死靠恢复（倒车），让行靠**重规划绕行**；
- 预期：`T018-02` 恢复抵达，且**不触发**村庄 AI 的倒车扰动 ✓（待四门验证）

## 5. 当前状态与安全
- 两次尝试**均已回退**，`scripts/ai/ai_path_driver.gd` 与 HEAD **逐字一致** ✓
- 该回归**只在 46/3 的失败列表里体现为 `T018-02`**，且**基线为绿** ⇒ 属**必须修**的项（不得留作"已知失败"）
