# WT-040-R1 修复记录：河谷 AI **任务→路径**环（独立修复提交 + 版本绑定对照）

> 基线固定：本记录的对照数据绑定在修复前提交 `f5d5d46e` 与修复提交（见下）之上。
> 本轮**只修"路径"这一环** ✓ —— 未提速 ✗ · 未缩图 ✗ · 未传送 ✗ · 未清冷却 ✗ · 未强造命中 ✗。

## 1. 修复前（探针 	ests/probe_river_ai_route.gd，提交 `f5d5d46e`）
| 车 | 目标 | 可达 | 路径节点 |
|---|---|---|---|
| A / B | (−520,120) | **false** ✗ | 0 |
| A2 / B2 | (0,−130) | **false** ✗ | 0 |
| A4 / B4 | (0,90) | **false** ✗ | 0 |
| A3 / B3 | (520,100) | **true** ✓ | 65 / 75 |
⇒ **8 辆中仅 2 辆可达** ✗ ⇒ 最先失败的一环＝**路径** ✓
（另：修复前目标取自"目标点中心"✗，且中央通路用标题子串匹配 ✗ ⇒ 我自己的 bug 已修 ✓）

## 2. 修复内容（仅一处函数族）
scripts/maps/river_team_range.gd：
- 任务点改为河谷**已授权**的 **A/B/C 入口**（driving_stops 中位于道路上者 ✓）+ **中央跨河通路**（按**位置**识别 lane 0 ✓）；
- 每个点**吸附到地图自身 RiverJunctionNavigation 生成的 857 节点图中最近节点** ✓ —— 该图正是导航器能路由的点集 ✓。
（注释中已写明：**这是唯一改动**，其余一律不动 ✓）

## 3. 修复后（探针，同一脚本、同一图）
| 车 | 新目标 | 可达 | 路径节点 | 直线距离 |
|---|---|---|---|---|
| A / B | (−492,200) | **true** ✓ | 37 / 54 | 523 / 905 |
| A2 / B2 | (−14,−315) | **true** ✓ | 64 / 37 | 1028 / 447 |
| A3 / B3 | (520,180) | **true** ✓ | 61 / 79 | 919 / — |
| A4 / B4 | (7,100) | **true** ✓ | 42 / 51 | 628 / — |
`
ok=true: 8 ; ok=false: 0        ⇐ 修复前 2 / 6
`

## 4. 目标映射回归（新增永久看住 ✓）	ests/check_river_task_mapping.gd
`
[PASS] river match initialises eight AI actors
[PASS] no river slot targets a legacy TeamArena goal (found 0)
[PASS] every river slot's task point is routable on the map graph (8/8)
[PASS] river tasks sit on the map's authored driving stops (8 of 8)
RIVER_TASK_MAPPING_PASS
`
⇒ 您的三点检查要求**全部有断言** ✓：①目标来自河谷 ✓ ②**不会回退 TeamArena**（0 命中 ✓）③中央通路点是**可达且适合车辆的地面任务点**（8/8 可达 ✓）。

## 5. 本轮**未**做（按您要求）
- **未**扩功能 ✗ · **未**调整战斗数值 ✗ · **未**提速/缩图/传送/清冷却/强造命中 ✗ · 性能专项继续 **HOLD** ✓；
- 240 s 仅为**诊断观察窗口** ✓，**不改变正式比赛规则** ✓；到上限未自然结束 ⇒ 标记**"观察结束"** ✓，**完整比赛仍未验证** ✓。

## 6. 下一步（下一阶段，按您的链路继续）
1. **记录器补齐**您列出的字段：实际任务推进 ✓ · **首次观察** ✓ · **瞄准或禁火原因** ✓ · **实际发射** ✓ · **接触损伤** ✓ · **占点票池变化** ✓ · **结束原因** ✓；
   （并**作废**我那个被证伪的 `respawns` 指标 ✗ —— 实测 life_id **按实体固定**（A=1…B=8 ✓ 三采样不变 ✓），我用 life>1 计数把 7 辆首命车误计为再出击 ✗）
2. **再出击未重新分配任务** ✗（spawn_slot 内无 set_patrol ✓ 已查明）⇒ 下一环 ✓；
3. 之后才进入**观察→瞄准→发射**环（若仍无交火 ✓）。

---

## 7. 修复在**对局中**的实证（六环记录器第一份数据，提交 ``778eedc1``）
**任务推进（A 车距目标，逐 30 s）**：
```
t=5  → 523 m
t=35 → 509 m
t=65 → 472 m
t=95 → 241 m      ⇐ 单调收敛 ✓
修复前同项：608 → 594 → 608   ⇐ 原地振荡、零推进 ✗
```
⇒ **修复真实改变了行为** ✓（AI 现在沿路线持续接近目标 ✓）

**六环当前状态（t=5…95）**：
| 环 | 观测 | 结论 |
|---|---|---|
| **任务** | `task=""` `role=""` `task_reason=""` | **角色分配器尚未派发** ✗ ⇒ 下一个观察点 ✓ |
| **路径** | `driver_reason="path_ready"` ✓ | 已修 ✓（8/8 可达 ✓） |
| **移动** | `driver="following"` ✓ + `to_goal` 单调下降 ✓ | **推进正常** ✓ |
| **观察** | `first_seen_s = -1`（全程） | **尚未接敌/未见敌** ✗ |
| **瞄准** | `aim=""` | 未进入 ✗ |
| **发射** | `shots = 0`（全程） | 未进入 ✗ |
| 击毁 | `dead=true` 计数 = 0 | 无损耗 ✓ |

⇒ **最先未通过的一环＝观察** ✗（其上游：**任务分配为空** ✗）
⇒ 240 s 为诊断窗口 ✓；到上限即标 **"观察结束"** ✓，**完整比赛仍未验证** ✓。

## 8. 再出击任务修复：**代码完成、尚未观察到**
`RiverTeamRange.spawn_slot()` 覆盖已就位 ✓（用与首次生成**同一个** `objective_goal` ✓）；
但本次窗口内**无击毁** ⇒ **未有再出击样本** ✗ ⇒ 状态记为 **"代码完成、待实战样本验证"** ✓（不声称已验证 ✗）。
