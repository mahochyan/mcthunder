# WT-036-R1 登记：工业战斗**抵达率**既有红的机制（本轮量化）

## 1. 末态实体状态对比（套件逐 30 s 诊断，`logs/WT-036-R1/full-gate-127/`）
| seed | 结果 | `drive` 分布 | `dead` | `stagnant` 最大 |
|---|---|---|---|---|
| **23023**（通过 ✓） | **4 抵达** ✓ | arrived ×4 · unreachable ×1 · idle ×1 · following ×2 | 0 | **0** |
| **23024**（失败 ✗） | **仅 1 抵达** ✗ | arrived ×1 · **wreck ×3** · **following ×5** · **failed ×1** · **yielding ×1** | 3 | **45** ✗ |

⇒ 失败的 seed 中，**既有被击毁（wreck ×3＝战斗损耗）**，**也有存活却未抵达者**（`following ×5` 中 stagnant 最高 **45 s** ✗、**`failed ×1`** ✗、**`yielding ×1`** ✗）

## 2. 结论（诚实定性）
1. 该检查要求"**每队至少 3 个实体抵达中央**"✓；在**一方被压制**的局面上，**阈值 × 场景平衡**共同决定结果 ✓；
2. **基线同样失败** ✓（15/1 ✓，同一项 ✓）⇒ **非本分支引入** ✓；
3. 但未抵达实体的**构成**会受我的**让行策略**影响（出现 `failed` 形态 ✗）⇒ 我不在无授权下改；
4. 全程 **8 个快照**出现 `stagnant ≥ 10`（最高 45 s ✓，未达检查阈值 90 s ✓）⇒ 存在**交通拥堵 / 长期低速**现象 ✓（与早前 `supply`/坡脊类同源：低速 + 让行 + 重车动力 ✓）。

## 3. 三个选项（需您裁定）
| 选项 | 内容 | 代价 |
|---|---|---|
| **1. 调场景平衡** | 改 `challenge`/比赛配置的初始兵力或票数，使双方更接近对等 | **玩法平衡 → 需授权** ✗ |
| **2. 调检查对损耗的容忍** | 例如允许"每队 ≥3 抵达**或**该队已被全歼"（其注释已承认车可能在抵达前被击毁 ✓） | **测试语义 → 需显式记录与您同意** ✗ |
| **3. 维持登记并独立立项** | 作为既有红保留，另立"工业图中央抵达率/交通拥堵"工作单 | 无需改动 ✓ |

**我的建议**：**选项 3**（既有红、且 1/2 均属需授权/需改语义的范畴 ✓）

## 4. 复现
```
& <godot> --headless --path <cont> --fixed-fps 60 -s res://tests/run_industrial_battle_checks.gd    # 约 1000 s
```
