# 裁定请求包（一次性汇总，减少打断）

> 本文件汇总**必须由用户/作者决定**的事项；每项含**证据**、**选项**与**我的建议**。
> 除这些事项外，本分支的其余工作均可自行推进，不需要批准 ✓

## A. `T018-H01`（出生→占点，1 槽位间歇失败）
**证据**：**未改动基线同样失败**（25/2 含此项）；逐轮槽位不同（1/0、1/3、1/5、2/0）；轨迹显示截止时**仍在推进**（枢轴转向后 `speed=1.221`、`dist_wp=2.27`）⇒ 属**预算边际**而非卡死。
- **A1** 提高该路线 tick 预算（依"仍在推进"的证据；与我此前对 D 电池所做的同类处理一致）——**属测试语义变更，须显式记录**；
- **A2** 保持现状，作为**已知边际间歇**登记，门禁按**失败集合**判定（允许设计目标项 + ≤1 个此槽位）。
- **建议**：**A2**（基线既红、且改动会削弱"行进预算"这一断言语义）。

## B. `challenge_defence_waves`（脚本飞行员赢不了的困难变体）
**证据**：`hold_ground` 需 **占区 45 s（已达标）＋ 击杀全部敌人**；实测脚本飞行员**被启用 AI 打瘫/点燃**（`repairs=4`、`shots=0`）⇒ hard 的 150 s 上限超时；**三轮夹具修复全部否证**（含"无主车辆才恢复"变体因墙=无主而误判 ✓ 已更正）。
- **B1** 强化夹具（更强的脚本飞行员：先灭火/退避、用掩体、侧翼射击）——测试侧工作量中等，**不保证**能赢 hard；
- **B2** 该检查改测 **normal** 难度（hard 另立一项）——**改变覆盖面，须显式记录**，不得当作"降难度"；
- **B3** 产品侧复核 **hard 平衡**（`enemy_rounds=12`、AI 压制力）——玩法/平衡，**需授权**；
- **B4** 保持登记为**夹具能力边界**。
- **建议**：**B4**（或 B1 若您愿意投入测试侧工时）。

## C. M26「坡上起步」能力（调校冲突）
**证据**：10.4° 坡、`thr=0.77` 无法从静止起步；**九次驱动侧修复全部否证**（每次都留证）；该坡道**已铺装**（非地面分类问题）。
- **C1** 调整机动参数（M26 动力/爬坡/低油门段）——**需授权**（影响平衡）；
- **C2** 限定范围地图：降低该坡脊局部坡度或让重车路线避开——**须说明影响区域与战术变化**；
- **C3** 作为**已知受限**写入交付（D 电池判定表已按"受限"记录）。
- **建议**：**C3**（或 C2，若您希望重车也能完成该坡道）。

## D. 8.5 m 刚体包络（`T018-03b`，我新增的**设计目标检查**，当前失败）
**证据**：`MapDefinition.max_vehicle_size = (4.2, 2.4, 8.5)`，`validate()` 用其**宽度**、烘焙与结构检查用其宽度；**仓库内无车辆达到 8.5 m**；作为刚体盒烘焙时有 **38/1786** 采样受阻（真实车辆因有悬挂/底盘而能通过）。
- **D1** **维持需求** ⇒ 需**限定范围地形/路线修复**（与已授权的 C 类同法）；
- **D2** **修改声明**（把 8.5 m 刚体包络降级为"设计目标/非硬性"）⇒ 文档与 `validate()` 口径同步更新。
- **建议**：**D2**（或 D1 若硬性需求成立）——**此项决定影响 WT-019/WT-018 的验收口径**，请明确。

## E. 授权请求（红线内必须由您开启）
| # | 事项 | 为何需授权 |
|---|---|---|
| E1 | `export_presets.cfg` 的 **server/client 导出预设** | 属**构建/发布流程**改动（红线） |
| E2 | **96 车全量适配产物**（≈70 MB 入库） | 体积与入库策略 |
| E3 | **资产接入与登记**（路径接入授权、素材许可依据**分别**记录） | 授权与许可归属 |
| E4 | 真人验收（42/42）与**性能采样**（当前 `HOLD_BY_USER`，零采样） | 需真人与授权 |

## F. 当前**不需要裁定**即可继续的事项
门禁总表回填 · 探针/加固等测试侧完善 · 资产侧 ④⑤ 的可执行部分（已完成）· 文档与交付汇总 · 已授权 C 类铺装的同类**限定范围**修复（须先说明影响）。
