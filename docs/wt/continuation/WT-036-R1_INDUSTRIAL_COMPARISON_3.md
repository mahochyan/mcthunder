# WT-036-R1 附录四（**更正上一轮口径**）：工业套件"完整运行对完整运行"定案

## 1. 为什么要更正
上一轮我用 **run1（85/30）** 与基线比较，并加了"轮间方差"保留。本轮 `pwsh-16` 的第二次分支运行**未被 600 s 截断**（用时 **1350.2 s，完整跑完**）→ 说明 **run1 是截断样本**（601 s 被批处理上限砍掉），**不可与完整基线直接比较**。

## 2. 更正后的完整对照（同一检查总数 339）
| 指标 | 基线 main（完整） | 本分支 run2（完整） |
|---|---|---|
| PASS | **283** | **300** ✓ |
| FAIL（唯一失败检查名） | **56** | **39** ✓ |
| 检查总数 | 339 | 339 ✓ |

**集合比对**：
- **仅基线失败：19**（本分支已消除）——以 **M26 捕获路线**为主，含 M36 部分捕获路线 ✓ 与"坡面自转修复 + 卡死窗口修复 + near→cap 铺装"一致；
- **仅分支失败：2** —— ① `hill_village: same vehicle mean spawn arrival imbalance below 15% us_m24_m6_t85e1_1951`（**统计类检查**）② `us_m24_m6_t85e1_1951/2/5/capture`；
- **共同失败：37** —— 其中 **supply 36 · capture 0** ✓✓

## 3. 结论（更正版）
1. **本分支净改善 17 项**（56 → 39），**消除了 19 项基线红**（M26/M36 捕获），**仅引入 2 项**；
2. **`supply` 收口 100% 属既有系统性失败**（36 项在两棵树上全部失败，capture 无共同失败）→ 完全**不是本分支引入**；
3. **套件自身确有轮间方差**：同一分支两次运行（run1 截断 / run2 完整）→ **仅 run2 失败 10 项、仅 run1 失败 1 项、两轮稳定失败 29 项**；**29 项稳定失败**是可靠核心，其余属**截断 + 噪声**；
4. 已由 `WT-036-R1_SUPPLY_INVESTIGATION.md` 证明：**同一条 supply 路线孤立运行可成功抵达（0 次恢复）** → supply 失败兼具"**既有系统性收口问题**"与"**长序列电池内不稳定**"两重性质，应单独立项并配测试侧加固。

## 4. 对门禁 JSON 的影响
`regression-gate-deferred-closeout.json` 中的 `industrial_comparison` 应以**完整运行**为准（本文件 §2），并保留 `variance` 字段（29 稳定 / 10 噪声 / 1 截断特有）。
