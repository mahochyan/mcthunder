# WT-030D-R2：**适配产物的 `MuzzlePoint` 位于原点**（缺陷登记 + 更正上一条提交的说法）

## 1. 更正
我在提交 `d4b2bfbf` 的信息中写了"现在回退到实测炮口 ⇒ 作者看到实测数字而非误导性的 0" ✗ —— **与实测不符**：重跑后 `barrel=0.000` **依旧** ✗。
原因：适配产物中的 `MuzzlePoint` 被解析为 **`kind = "node"`**（作者节点 ✓），因此我加的 `measured_frame` 回退分支**未触发** ✓；而该节点本身**位于 (0,0,0)** ✗。

## 2. 缺陷（有证据）
| 证据 | 内容 |
|---|---|
| 角色审计（对**适配产物**） | `muzzle kind = node`，路径 `MuzzlePoint` ✓ |
| 实测位置 | `turret / gun / muzzle` 三者 `global_position` **均为 (0,0,0)** ✗（`35t` / `KPz70` / `M48` 三车一致 ✓） |
| 结论 | 适配产物**只带节点名、未带位置** ✗ ⇒ **未满足"产物必须携带实测炮口位置"** 的要求 ✓ |

## 3. 根因假设（待下一轮实测验证）
1. `Marker3D` 在 GLTF 导出中可能不作为带变换的节点导出 ✗（或变换被重置 ✓）；
2. 标记是**场景根的直接子节点** ✗，而导出可能把根变换归一化 ✓；
3. `owner` 赋值虽已设置 ✓，但导出路径未保留其局部变换 ✗。

## 4. 修复计划（下一轮）
- 改 `tests/export_model_binding_adapter.gd`：把标记**挂到炮管网格节点之下**（让父链携带位置 ✓）或改用**带网格的空节点** ✓，并在导出后**重新读取产物**验证 `MuzzlePoint` 的位置 == 实测偏移 ✓（**自校验**：不通过即报错 ✓）；
- 重新导出 3 车产物 ⇒ **新哈希** ✓（按您的要求"独立适配产物并重新记录哈希" ✓）；
- 重跑 `check_adapter_artifacts`（含**位置断言** ✓）与 `export_draft_layouts` / `export_draft_binding_inputs`（几何随之变为实测值 ✓）。

## 5. 状态
- **不声称适配产物已完成** ✓；此前"⑤ 可执行部分通过（产物内含 `MuzzlePoint`）"仅证明**节点存在** ✓，**不证明位置正确** ✓ —— 本轮实测予以更正 ✓。

## 6. 后续两次修复尝试（均**失败**，均有自校验证据）
| # | 变更 | 结果（自校验） | 产物哈希 |
|---|---|---|---|
| 1 | 标记挂到 **`gun` 节点**（`VehicleRoot/TurretPivot/GunPivot`）之下，位置用父链逆变换求得 | **`verified=False`**，`delta=1.95/5.619/5.285`（＝`|offset|` ⇒ 位置仍在原点 ✗） | `f179c74d…` / `ddefebaa…` / `34fbfe29…` |
| 2 | 再加**真实网格锚点**（`Node3D` + 2 cm `BoxMesh` 子节点，网格节点导出时必带变换） | **`verified=False`**，`delta` 同上 ✗ | `e59e5d3d…` / `29f70255…` / `811ec085…` |

⇒ **结论**：在我的写法下，**导出确实没有保留该锚点的位置** ✗（两次不同写法一致 ✓）

## 7. 下一步诊断（下一轮，按证据推进）
1. **打印产物内的节点变换**（含 **local** 与 global ✓）——区分"变换被丢弃" ✗ 与"全局值与父链不符" ✗；
2. 检查导出 API：`GLTFDocument.append_from_scene` 是否要求**根节点**为 `Node3D` 且锚点为**根的子节点** ✗；尝试把锚点作为**根的直接子节点但用 `position` 而非 `transform`** ✗，或**先把锚点加入再用 `set_owner_recursive`** ✗；
3. 若导出路线始终不可行 ⇒ 改为**后处理**：导出后用 `GltfState`/直接改写 GLB 的节点变换 ✗（属新依赖风险 ✗，须先说明）；
4. **在修复成功前**：`adapter_artifacts.json` 中 `muzzle_verified_in_artifact=false` 如实记录 ✓，**不声称**适配产物已满足要求 ✓。

## 8. 当前诚实结论
- 适配产物仍**只带节点名、位置在原点** ✗ ⇒ ⑤ 的"产物携带实测炮口"**未达成** ✓；
- **自校验已固化**（每次导出都会重读产物断言 ✓）⇒ 不会再出现"节点存在就算通过"的误判 ✓；
- draft layout / draft binding 的**几何值也随之无效**（全 0 ✗）⇒ 待产物修复后再生成 ✓。
