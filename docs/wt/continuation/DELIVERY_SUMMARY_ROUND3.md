# 第 3 轮交付汇总（引擎缺陷攻坚 + WT-036 收尾 + 资产侧入库）

- **基线**：`a1bac406d2bc12b32c7f7d130590f1c1a17907c9`
- **本轮起点**：第 2 轮交付 `f261e785`；**当前头**：见末尾提交
- **规模**：本轮 **64 提交 / 430 文件**；**全链 97 提交 / 707 文件**
- **产品代码净变更（相对第 2 轮交付）**：**9 文件 / +407 −10**（其中新增 `role_mapping_audit.gd` +295；其余为下述四处修复与诊断字段）
- **变更清单全文**：`DELIVERY_CHANGELOG_ROUND3.txt`

## 一、落地并验证的产品修复（4 项，每项列出验证它的门）
| 修复 | 文件 | 验证 |
|---|---|---|
| **坡面自转缺陷**（车体在坡上几乎不转：176°→20°） | `tank.gd`、`game_config.gd` | **T039-E 3/3**（平地/11.9°/12.7° 均 ≈−176°，后半无衰减）+ 村庄 21/0 + 地图 46/3 |
| **卡死窗口失效**（重规划 ≤1 s 清零 2 s 窗口 ⇒ 恢复永不触发） | `ai_path_driver.gd` | 村庄 21/0 · 地图 46/3 · 历史 33/0 |
| **转向支撑下限**（单侧探针失支撑把 steer 清零） | `tank.gd`、`game_config.gd` | 同上（回归中立） |
| **`near→cap` 道路铺装**（草地 drag 0.55 使最弱车无法推进） | `village_definition.gd` | **历史道路 29/4 → 33/0**（M26 四条基线红全清） |

## 二、发现并登记的缺陷 / 边界（4 项，均含证据与归属）
| 项 | 性质 | 关键证据 | 归属 |
|---|---|---|---|
| **M26 坡上起步** | **调校冲突** | 10.4° 坡、`thr=0.77` 无法从静止起步；**九次驱动侧修复全部否证** | 机动参数（需授权）或限定范围地图 |
| **`supply` 收口** | **既有系统性 + 电池内不稳定** | 36 项 supply 在两棵树全失败；**同路线孤立运行 0 恢复抵达** | 测试侧加固（已实施）+ 产品侧收口 |
| **挑战防守波次** | **夹具能力边界** | 脚本飞行员被启用 AI 打瘫（`repairs=4`、`shots=0`）→ hard 150 s 超时 | 作者/用户裁定（4 选项） |
| **8.5 m 刚体包络** | **设计目标未达标（保留失败）** | `T018-03b` 受阻 38/1786 采样 | 地形作者化或需求变更（需授权） |

## 三、质量门收尾（WT-036-R1）
| 项 | 结果 |
|---|---|
| **DEFERRED** | **11 → 0**：4 项环境依赖 NOT_RUN（窗口/渲染/引擎流程）· 2 项慢但已测 · 5 项已测（含 1 项真实 FAIL = 挑战夹具） |
| **工业套件（完整对完整）** | 基线唯一失败 **56** → 本分支 **39**；**消除 19**；**可归因回归 0**；共同 37（**supply 36**） |
| **确定性套件** | 历史 **33/0** · 地图 **46/3（连跑 3 次一致）** · T039-E **3/3** · 村庄 **21/0** · 结构 **54/54** |
| 权威汇总 | `REGRESSION_GATE_MASTER.md` · `logs/WT-036-r1/regression-gate-deferred-closeout.json` |

## 四、新增测试基础设施（6 个入口）
`tests/route_harness.gd`（出生净空查询 + 逐 tick 轨迹）· `tests/run_slope_pivot_checks.gd`（**T039-E** 坡面自转守门）· `tests/run_flank_crest_traversal_checks.gd`（**T039-D** 生产车辆通行电池）· `tests/export_german_intake_audit.gd` · `tests/export_german_role_mapping.gd` · `tests/export_model_binding_adapter.gd`

## 五、资产侧（作者素材入库路径，**只读源素材**）
- **96 车只读审计**：96/96 可解析；**三角面 ≤15,000 断言成立**（合计 1,147,100，最大 14,990）；
- **角色映射**：**58/96 六角色齐全**；`muzzle` 已改为**逐车实测**（经父链合成），**撤掉伪造来源**；
- **适配产物（④）**：`35t`/`KPz70`/`M48` 三车生成**独立 GLB + 自身哈希**，并**重测源哈希证明源未被修改** ✓；
- **⑤/⑥ 边界**：`check_scene`/`check_file` 需**仓库内 layout + packet**（外部模型没有）⇒ 待作者数据；路径接入授权与许可依据**待授权口径**。

## 六、明确 NOT_RUN（不冒充通过）
真人验收（42/42）· 性能（`HOLD_BY_USER`，**零采样**）· 公网联网（发现/中继/专管/账号）· 10v10/16v16 容量 · 空海扩展（EXT-01—04）· 两项待授权（server/client 导出预设；96 车适配全量 ≈70 MB）。

## 七、风险与回滚
| 风险 | 说明 | 回滚 |
|---|---|---|
| 坡面自转修复影响面 | 改动 `tank.gd` 姿态路径（已四门验证无退化） | 单提交回退；T039-E 会立刻转红即告警 |
| 卡死窗口修复 | 改变恢复触发频率（村庄/地图已验证） | 单提交回退 |
| `near→cap` 铺装 | **可见地图变化**（占点进场走廊出现路面），无几何/掩体/通视影响 | 单提交回退（+1 行） |
| 资产侧新增 2.8 MB 适配产物 | 仅 3 车样例；全量需授权 | 删除 `assets/vehicles/adapters/` |
| 整体 | — | 分支可整体切回 `f261e785` 或基线 `a1bac406`；**原 `main` 工作区全程未被改动（porcelain 恒 379）** |

## 八、交接要点（下一位执行者）
1. 先读 `REGRESSION_GATE_MASTER.md`（权威口径）与 `NEXT_ACTION.md`；
2. **不要**再泛改驱动器：九次尝试均有留证（`WT-075-R1_*`、`WT-036-R1_SUPPLY_INVESTIGATION.md`）；
3. 产品侧改动的**验收顺序**：**T039-E（秒级）→ 村庄 → 地图 46/3 → 历史 33/0**，达标才提交；
4. 提交纪律：显式路径、绝不 `git add .`、每单后复核主工作区 HEAD 与 porcelain。
