# WT-036-R1：让行恢复五变体实测对照（含下一版设计）

## 1. 判据回顾
- **`T018-02`**：`blocked.phase == "arrived"` **且** 事件含 **`reverse`** ⇒ 必须"抵达 + 倒车"；
- **村庄**："all seven autonomous actors leave spawn and reach central approaches" ⇒ **七单位全部**在预算内到达中央，**时序余量紧**；
- **基线**：`T018-02` 通过（84.9 s，含恢复）；村庄 21/0 ✓

## 2. 实测对照（每行都有跑过的证据）
| 变体 | 让行恢复策略 | `T018-02` | 村庄 | 结论 |
|---|---|---|---|---|
| 0 | 无让行处理（仅我 WT-039-R1 的油门门限） | ✗ 永久 `yielding`（250 s 用满） | 21/0 ✓ | **回归**（基线为绿） |
| 1 | 仅重规划（已提交 `bcbfb49c`） | ✗ `arrived` 但**无 reverse** | 21/0 ✓ | 判据未满足 |
| 2 | 任意让行 2.5 s → 恢复 | ✓（地图 47/2） | **20/1 ✗** | 扰动村庄 |
| 3 | **静止**阻挡物 2.5 s → 恢复 | ✓（地图 47/2 两轮） | **20/1 ✗** | 村庄亦有静止遭遇 |
| 4 | **静止**阻挡物 **6.0 s** → 恢复 | ✓（地图 47/2） | **20/1 ✗** | 静止遭遇可超 6 s |

⇒ 结论：**"让行时进入恢复"在村庄必然吃掉时序余量**；单靠时间/速度阈值**无法分离**。

## 3. 下一版（第五变体）：按**阻挡物是否受 AI 控制**区分
- 停放车夹具：`VehicleActor` **无控制器**（`controller == null`）；
- 村庄阻挡物：`AITankController` **在控**（即便暂时静止/被卡）。
```gdscript
var blocker: Object = obstacle.get("collider") if obstacle is Dictionary else null
var dead_end := blocker != null and blocker.get("controller") == null     # 无主/停放 = 死路
yield_elapsed = (yield_elapsed + delta) if dead_end else 0.0
```
⇒ 预期：**停放车**（无主）→ 触发恢复 ⇒ `T018-02` ✓；**村庄 AI**（在控）→ **永不触发** ⇒ 21/0 ✓

## 4. 若第五变体仍失败
则此项升为**需作者/用户裁定的策略冲突**（两条都是合理要求，但在同一恢复策略下互斥），并按 D 口径登记：
- 选项 A：作者调整村庄检查的时序余量（**测试语义**，须显式记录）；
- 选项 B：产品侧定义"无主阻挡物才恢复"的策略（即第五变体，若可行则无需裁定）；
- 选项 C：把 `T018-02` 的 `recovered` 判据改为"**出现恢复或成功绕行**"（**改判据 ⇒ 需明确授权**）。

## 5. 当前状态
- 变体 2/3/4 均已**回退**（`git diff` 为空 ✓）；当前 HEAD 含变体 1；
- 变体 1 下 `T018-02` **失败** ⇒ **我引入的回归仍未消除**，必须继续（不得留作"已知失败"）。
