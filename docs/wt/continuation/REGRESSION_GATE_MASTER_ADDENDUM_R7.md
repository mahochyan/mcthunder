# 回归门总表 · 第 7 次回填：全量 28/4 的四项定性 + **让行策略五方案对账表**

> 运行方式：项目默认 32 套件（含本分支 T039-E）；证据 `logs/WT-036-R1/full-gate-reliable/` 与各专项日志。

## 1. 全量门禁
**32 套件 → 28 PASS / 4 FAIL**（细节见 `REGRESSION_GATE_MASTER_ADDENDUM_R6.md`）。
代表绿灯：`run_industrial_checks` **595/0** · `run_historical_road_checks` **33/0** · `run_team_checks` **67/0** · `run_village_battle_checks` 21/0 · `run_slope_pivot_checks` 3/0 · `run_art_checks` 63/0 · `run_structure_checks` 54/54 · `run_wreck_visual_checks` 71/0 · `run_feedback_checks` 40/0。

## 2. 四项失败的最终定性
| 套件 | 结果 | 定性 |
|---|---|---|
| `run_ai_drive_checks` | 39/1 → **40/0** | **我引入的回归，已修复** ✓（策略见 §3） |
| `run_map_checks` | **48/1** | 仅 **`T018-03b`（设计目标项）** ⇒ 按**集合判据**可接受 ✓ |
| `run_challenge_checks` | 138/2 | 已登记：**防守夹具能力边界** ✓ |
| `run_industrial_battle_checks` | 基线 **15/1** | 抵达项**既有基线红** ✓（非本分支引入）；本分支各策略见 §3 |

## 3. 让行策略五方案对账表（本会话核心决策记录）
| # | 策略 | `ai_drive` | 地图(`T018-02`) | 村庄 | 战斗(抵达/静止) |
|---|---|---|---|---|---|
| 0 | 无让行处理（仅油门门限） | ✗ | ✗ 永久 `yielding` | ✓ | — |
| 1 | 仅重规划（无优先级） | ✗ 对峙不解 | ✗ 到达但无 reverse | ✓ | — |
| 2 | 任意/静止阻挡物 → 恢复（2.5 s / 6.0 s） | ✓ | ✓ | **✗ 20/1** | — |
| 3 | **显式失败**（`mutual_yield`） | ✓ | ✓ | ✓ | **✗ 抵达减少**（1→2 seed 失败） |
| 4 | **优先级 + 仅重规划**（采用 `aebe0029`） | **✓ 40/0** | **✓ 48/1（仅设计目标）** | **✓ 21/0** | 复验中 ✓ |
| **判据要点** | ① 停放车（无控制器）必须**倒车**（判据含 `reverse`）；② **两辆 AI 车只让一方反应**（`entity_id` 较大者）：路径分叉 ⇒ 对峙自解；③ **不倒车**（村庄时序余量极紧）；④ **不放弃**（放弃会减少抵达）；⑤ 静态几何**永不计时** | | | | |

## 4. 工具修复（测试工具，断言不变）
`tests/run_suite_checks.ps1`：本环境 `Start-Process` 的 `ExitCode` 常为 `$null`（曾致运行器在 **import 步**中断）＋捕获日志中文**乱码**（结果行正则漏判）⇒ 判定改为**以日志证据为准**（无脚本错误 ∧ 无意外错误 ∧〔结果行 0 失败 ∨ ASCII `CHECKS_PASS` 标记〕），退出码仅记录；**正反双向验证**通过 ✓

## 5. 教训（写入交接）
1. **判定一律用集合判据**：地图套件的设计目标项失败属已知可接受 —— 我曾因 `exit==0` 全判**错误回退了正确修复** ✗；
2. **"显式放弃"是陷阱**：它让"静止 ≥90 s"检查转绿 ✓，却**代价是抵达数下降** ✗ ⇒ 必须**同时**满足两类检查才可提交；
3. **对称反应是僵持之源**：两车同时让行/同时倒车 ⇒ 路径对称 ⇒ 再次相遇；**单侧反应**才是解 ✓。

## 6. 仍未结（不声称完成）
`industrial_battle` 抵达项（**既有**，建议独立立项：工业图中央抵达率）· `T018-H01` 预算（A1/A2）· `challenge` 防守（B1–B4）· M26 坡上起步（C1–C3）· 8.5 m 包络（D1/D2）· 授权 E1–E4 · 真人验收与性能（NOT_RUN）
