b5f9677d WT-036-R1: register the challenge defence failure as a fixture capability boundary after three falsified attempts - the run shows the script pilot never leaves the zone at all, because the driver zeroes throttle and diverts to repair whenever the vehicle is on fire or immobilised (four repairs recorded), so it is disabled by the active AI before it can either advance or fire, and the challenge ends at the hard limit with no shots and no kills; the fixture edit is reverted, the assertion is untouched, and the document lays out four options with their ownership so the decision to strengthen the fixture, change the level tested, review difficulty balance or simply record it goes to the author or user
1551fe51 WT-036 master gate table: consolidate every reproducible verdict of this branch into one authoritative document - the deterministic suites (historical road 33/0 after the M26 reds cleared, map 46/3 identical across three runs, the new sloped-pivot suite 3/3, village battle 21/0, structure 54/54), the industrial suite compared complete against complete (baseline 56 distinct failing checks versus 39, with 19 cleared, no attributable regression and 37 shared of which 36 are the pre-existing supply leg), the eleven deferred suites fully classified so DEFERRED is now empty, the three open items with their evidence and ownership, the five retained product changes with the gate that verified each, and an explicit NOT_RUN list so that human acceptance, performance, public networking, large-capacity matches and the air/naval extensions are never implied to be done
494376de WT-036-R1 correction: my two readings of the defence scenario were wrong and both fixture edits are reverted - the zone radius is 18 rather than 12, the measured held of 45.0 is the cap for the hard level so the hold condition was already satisfied, and relaxing the pilot's trigger tolerance to the value the same suite's shooting helper uses did not make it fire a single shot; the only unmet condition is kills, and the log shows the enemies alive 75 metres north at z=25 with twelve shots of their own against a pilot that never advances there, so the fixture holds the zone and then never goes looking for a fight, which is the gap to close next
2d1bd0f7 WT-036-R1 root cause: the challenge defence failure is a fixture defect, not a product one - the director requires holding inside the zone AND killing every enemy, while the test script parks the vehicle at 13 to 16 metres from the zone centre (outside the 12 metre radius, which is why held stops at 45 seconds) and never fires at all (player_shots=0, kills empty), so the fixture cannot pass whatever the product does; the assertion itself stays untouched and the fix is to keep the script inside the zone and use the suite's own shooting helper to engage
b71f4777 WT-036-R1 correction: dissolve the all-checks-pass-but-non-zero-exit item, which was my own harness bug - the batch marked those suites FAIL because Start-Process left ExitCode null and null compared against zero evaluates true, while the logs print VILLAGE_BATTLE_CHECKS_PASS, TRAFFIC_TELEMETRY_CHECKS_PASS and RIVER_ENGAGEMENT_CHECKS_PASS and river driving reports 34 passed with 0 failed; an untouched baseline reproduces traffic telemetry as 8 pass with the same empty exit code, and the only stderr content is the layout catalog's own non-fatal AABB warning for the test fixture, which also appears at baseline
b3d0d0bc WT-036-R1 triage: neither of the two checks that fail only on the branch is an attributable regression - the M24 2/5 capture route also fails at baseline (twice there), while the spawn-arrival imbalance statistic fails in exactly one of the three runs and passes in the branch's other run and at baseline, which marks it as a marginal statistical threshold on a suite that already flips ten checks between its own runs; the branch therefore clears 19 baseline reds and introduces none, the 29 failures stable across all three runs are the reliable core and are dominated by the supply leg, and the suite is recorded as needing test-side hardening without any change to what it asserts
91e9248e WT-036-R1 correction: the second branch run finished uncut at 1350 seconds, which shows the earlier 85/30 sample was truncated by the batch cap and cannot be compared with a complete baseline - against the complete baseline the branch goes from 56 to 39 distinct failing checks over the same 339 checks, clearing 19 baseline reds (mostly M26 and M36 capture routes) and introducing 2 (one spawn-arrival balance statistic and one M24 capture), while all 36 supply failures are shared and no capture failure is shared, so the supply leg is entirely pre-existing; the same branch also differs from itself by ten checks between runs, so the twenty-nine stable failures are recorded as the reliable core with the rest as truncation and noise
1dda5a3c WT-036-R1 final closeout: all eleven deferred suites are now classified (four hang on window or engine-only flows, two are slow but measured, five produced real results including one complete run), and the industrial suite is settled by a same-budget baseline comparison of failing-check sets - the baseline has 56 distinct failing checks against the branch's 30, with 27 failing only at baseline, exactly one failing only on the branch (one M24 capture route) and 29 shared, so the M36 supply concern was a shorter baseline run rather than a regression and the supply leg is a pre-existing systemic weakness that also reproduces successfully in isolation; the new challenge defence failure and the three all-checks-pass-but-non-zero-exit suites are recorded as separate items
91fca997 WT-036-R1 appendix three: the industrial supply failures are battery-order instability rather than route geometry - the supply point sits on flat untextured ground at (0,0,143) with two width-12 edges, and replaying the exact failing route in isolation with the production driver arrives in about 3000 ticks with zero recovery attempts while the suite fails the same check with four recovery attempts at a position the isolated run passes through; that is the same isolated-succeeds/battery-fails family as the map suite, so the earlier 40-to-30 comparison now carries the caveat that this suite varies between runs, and a second branch run is measuring that variance by comparing failing-check sets
94bcf3fa WT-036-R1 appendix two: split the industrial failures by target type and declare the comparison bias - capture routes improve from 16 failing at baseline to 1 on the branch while the supply leg is identically broken on both for the M4A3 and M24 (12 each, the same failed-with-four-recovery-attempts shape at z around 117 next to the spawn area, while the same vehicle and slot reaches its capture), and the M36 supply failures are explicitly not claimed as branch-introduced because the baseline run was cut at 420 s against the branch's 601 s - a same-budget baseline rerun is now running in the background to settle it
590aefd8 WT-036-R1: compare the industrial suite against an untouched baseline by failing-check set rather than by wall clock - the suite runs but cannot finish inside 600 s, and the branch has 30 distinct failing checks against the baseline's 40, so the work is a net improvement that also clears the M26 capture routes that were red at baseline; six checks appear only on the branch (M36 and M24 supply routes plus one M24 capture) and are recorded as needing follow-up rather than being waved away, while the M24 supply failures that appear on both are pre-existing
4c2b8c33 WT-030D-R2 step four: build independent adapter artifacts and record both hashes - the source GLBs are opened read-only (each source hash is re-measured after the write and matches), a MuzzlePoint marker is placed at the muzzle position the role audit measured from the barrel mesh through the parent chain, and the derived GLB is exported into the repository with its own hash, which is exactly the separation the review asked for: an install-time node cannot make the original GLB pass check_file, so the binding must point at an adapter; three vehicles prove the pipeline (35t, KPz70, M48 at 0.86, 0.93 and 1.14 MB), while the full ninety-six would add about seventy megabytes and needs authorisation
d0a2d2f3 WT-036-R1 closeout, stage one: rerun the eleven DEFERRED suites with a 600 s per-suite cap and refine the criterion - a suite that emits no check at all within 600 s is hanging on a window or engine-only flow rather than merely slow, which is what art showcase and export checks turn out to be (75 byte logs, no checks), while challenge checks does run and finishes 138 with 2 failures, both on the same scripted-pilot defence check whose log shows the pilot holding 45 s with zero kills and six enemies remaining, a newly exposed gameplay result rather than a timeout
40e372e0 WT-075-R1 attribution correction: the M26 and M36 stalls in the low-speed and crest-restart fixtures belong to the same registered restart-on-grade limit rather than to crude fixture steering - those fixtures drive at a fixed 0.35 to 0.5 throttle, the failure sits on a 10.4 degree south-flank section where the production driver also could not start from rest at 0.77 throttle, and the fixture's steering law is the same proportional law the driver uses, while the fixture itself passes its flat-ground calibration on all four vehicles; also start the WT-036 deferred rerun, whose first suite (art showcase) exceeds 600 s without emitting a single check and therefore looks window-dependent rather than merely slow
7672ab63 WT-075-R1: register the heavy-hull restart-on-grade limit after the ninth and final driver-side attempt was falsified - asking for full power at rest on a slope left historical at 33/0, village at 21/0 and the sloped pivot at 3/3 but cost the map suite two checks (44/5 against its now deterministic 46/3), so the driver is a tuned balance and this limit belongs to mobility tuning or to a limited-scope map change; the document records the trace, the nine falsified remedies and the current per-vehicle traverse verdict (M4A3 5/5, M24 5/5, M26 1/5, M36 1/5)
59f687cf WT-039-D correction: the previous commit message claimed the traverse budget was doubled, but that edit had been rejected because the file had changed since its last read, so the run behind it still used 9000 ticks; the budget change is applied here for real and the heavy-hull results are re-measured, with the M26 trace evidence that it closes on its final waypoint (7.28 to 4.68 to 2.79 m) instead of circling
2c755512 WT-039-D: correct my own 'orbiting' reading and double the traverse budget - the M26 trace shows it climbing the flank in first gear at 1.8-2.6 m/s and then closing on its final waypoint (7.28 to 4.68 to 2.79 m at 3.06 m/s) when the 9000-tick cutoff arrived, so it was still making progress on a long detour rather than circling; with 18000 ticks the heavy hulls get the time the route actually needs
40cff2b2 WT-039-D: drop the bounce criterion instead of guessing a threshold - the product's landing model already bounds every rebound by landing_max_rebound, and a vehicle climbing a slope legitimately carries an upward velocity component, so my world-Y tests marked ordinary cresting and ordinary climbing as failures (first with abs() counting the downward drop, then with a 1 m/s cap counting every uphill tick); the model's own landing counter is reported as information and the pass criteria now rest on arrival, stall, floor contact, finiteness and the muzzle/armour/internal checks
40daf5c0 WT-039-R1 option C (user-chosen): paint the near->cap approach legs as road so their ground classifies as road rather than grass - the measured M26 was commanded 0.36 throttle on grass, crept at 0.02 m/s and failed after four recoveries while the three stronger vehicles finished; only those legs change, the dense cap<->cap mesh stays open ground as the author intended, and the effect is mobility and appearance only with no geometry, cover or sightline change
9a9ebbd8 WT-039-R1: revert the minimum-approach-speed change as a no-op and register the M26 limit as a tuning conflict instead of changing it - the arrival profile already demands about 3.2 m/s near the waypoint, so a 1.5 m/s floor could never raise the 0.36 throttle it issued on grass, which is my own mis-targeting; with seven generic remedies now tried and reverted, the evidence is that a 0.36 throttle on grass drag 0.55 gives this least powerful hull about 0.02 m/s while the three stronger vehicles finish, and that belongs to mobility tuning or to a limited-scope map change, both of which need author or user authority rather than another silent edit
73c23d4c WT-039-R1 fix: keep a minimum speed demand on high-drag ground while still short of the waypoint - the measured M26 was commanded only 0.36 throttle on grass (drag 0.55), crept at 0.02 m/s against 2.17 m/s on a lower-drag surface, tripped the correctly working stuck detector and failed the route after four recoveries, while the three more powerful vehicles finished it; the floor is gated on the ground drag so pavement behaviour is untouched, and the historical, map, village and sloped-pivot suites are re-measured
82e0c559 WT-039-R1 evidence: add read-only observability for the consumed command and submit result, and use it to refute the command-path hypothesis - at the M26 stop the actor consumes exactly what the driver commands (0.36 throttle, -0.07 steer) with submit accepted, so the command does arrive; the hull is not frozen either but creeps at 0.02 to 0.03 m/s, and the same hull on the same spot moves at only 0.65 m/s at 0.6 throttle against 4 to 5 m/s on a road, which points at the ground surface under that waypoint plus the M26 being the least powerful of the four rather than at any command or collision defect
35478f2f WT-039-R1 evidence: enable the actor command trace for the M26 in the historical road suite to settle whether the stall is a command-path problem - the hull is level on flat ground with no obstacle, moves when freshly spawned at the same coordinate and still moves after 5400 ticks elsewhere, so the remaining question is whether the command the actor consumes matches the 0.31 throttle the driver reports, and this turns that into a logged column
7007c1ad WT-039-R1: the step-up assist is FALSIFIED and reverted - lifting the hull over a 12 cm facet step did not free the M26 and cost the map suite a check (46/3 to 45/4), so tank.gd and game_config.gd are byte-identical to HEAD again; five generic remedies have now each failed to free this one hull while two of them also broke a different route, which is the evidence that this is a localized blocked position and that the honest next move is the limited-scope repair the option D instructions describe, with the area and tactical impact stated first
e313636f WT-039-R1 evidence: record pose and slide-contact columns in the shared route trace and use them to identify what holds the M26 - at the stopping point the hull is level (pitch -0.01, roll 0.00) on a vertical ground normal yet reports one slide collision against the village terrain mesh, while the sampled slope is 0 degrees and a freshly spawned hull moves there, so the drive collision box is catching a facet step of the 4 m terrain grid rather than any authored obstacle or slope; this is the first direct contact evidence in the whole investigation and it also shows the historical suite can report 28/5 instead of 29/4, so its exact set is re-measured
510d2792 WT-039-R1: the steered reverse recovery is FALSIFIED and reverted - it dropped historical road checks from 29/4 to 28/5 with a new M4A3 failure while the M26 stayed blocked, so the file is byte-identical to HEAD again; two stronger-recovery attempts have now each broken a different route while failing to free the wedged M26, which is the evidence that the remaining failure is a localized contact rather than a generic driver defect and belongs on the limited-scope route or geometry path
0d35b312 WT-039-R1 diagnostics: record the recovery attempts and the accumulated stuck window in the shared route trace, because the wedged M26 still fails after the reset fix and the next question is whether the recovery fires at all or fires and cannot escape - this turns that into a measured column instead of a code reading
149f24eb WT-039-R1 fix: the no-progress window now requires a real throttle command, so a hull that is wedged with 0.31 of throttle and a frozen position still enters recovery while vehicles that legitimately wait or plan no longer do, and replanning no longer clears the window because _plan runs twice as often as the window is long; map checks improve from 46/3 to 47/2 with the maximum-size proxy hill stall gone, historical road and sloped pivot are unchanged, and village battle returns to 21/0
5551b73c WT-039-R1 M26 deadlock narrowed: add command columns to the shared route trace (throttle, steer, slope_blocked) and use them to refute the terrain hypothesis - at the M26's stopping point the ground is flat (0 degrees), slope_blocked is false for all 180 probe ticks and a plain 0.6 throttle moves the same hull there, yet in the suite the hull sits frozen with the driver commanding 0.31 throttle and phase following for 200+ ticks; a long-run probe reproduces the same frozen-position signature while pressed against the map boundary, so the hull is wedged against world geometry and the recovery never fires, with the prime suspect being that replanning advances a waypoint and calls stuck.reset, which wipes the no-progress window before the detector can accumulate it
72275fee WT-039-E effect measured: the sloped-pivot fix lifts the traverse battery from 9 passing and 17 failing to 13 and 13, and the stall signature largely disappears - the production driver now arrives for the M4A3 (1630 ticks) and M24 (1494), and the M26 and M36 complete the crest turnaround in 1307 and 1826 ticks with the M36 showing 102 ticks of genuine reverse motion, so the pivot deadlock that this whole investigation chased is fixed; what remains is a different regime - M26 and M36 orbit (346 to 372 m travelled without arriving) on the uphill and lateral driver runs while the low-speed and crest-restart fixtures still stall for them, which points at path following for the slowest vehicles and at my own crude fixture steering rather than at the drive model
dad637e3 WT-039-E fix: reconcile the two proven constraints by yawing the basis about the ground normal and then rebuilding it from its own in-plane forward through VehiclePose.compose - the direct rotation restores the pivot that the old flattened-forward target could not deliver on a slope, and rebuilding keeps the tilt and roll convention that aiming and perception rely on, with approach still rate limiting the transition
daffcf01 WT-039-E: attempt five (yawing the pose target inside the ground plane) is also FALSIFIED and reverted - it left the sloped pivot at 20.5 and 16.9 degrees and additionally dropped village battle to 20/1 and map checks to 45/4, so tank.gd is byte-identical to HEAD again and every suite is back at its previous value; the record now separates what is proven (a direct rotation of the basis about the ground normal restores the full 175.9 degree pivot, attempt three) from what that broke (the composed tilt and roll convention that village combat depends on), which is the ground the next attempt must reconcile
d5f87b51 WT-039-E: the pose rewrite that finally restored sloped pivots (175.9 deg at 11.9 and 12.6 deg of slope, from 20.1 and 16.5) is nevertheless REVERTED because it regressed village battle combat deterministically - 20/1 against the baseline's 21/0 on an untouched main tree, failing 'normal AI perception and projectile execution produce combat on the map' - since it replaced the composed tilt/roll convention with a single-axis up alignment that aiming, perception or turret logic depends on; tank.gd is byte-identical to HEAD, the defect test still fails as before, and the next attempt must keep the composed basis and apply only the missing yaw on top of it
d19d9aab WT-039-E: the yaw-authoritative pose rewrite is FALSIFIED and reverted - rotating the hull by the commanded yaw before the rate-limited approach left the sloped pivot at 19.8 and 16.2 degrees (flat control still 176) and introduced a new map failure, so tank.gd is byte-identical to HEAD again; the diagnostic stands that the per-tick applied rotation decays geometrically on a slope (0.67 then 0.38, 0.21, 0.10, 0.06 deg) with yaw_rate held at full, which points at the shared angular budget of approach being consumed by a tilt or roll mismatch that my rewrite did not address
68885bcd WT-039-E: reproduce the sloped-pivot defect as a named failing test with a flat-ground control - the hull turns -176 degrees in 240 ticks on flat ground but only -20 degrees at the flank crest (11.6 deg slope) and -16.5 degrees further up (12.5 deg), decaying to zero after the first second while yaw_rate holds at its full -0.768 rad/s and grounded, both supports, slope_blocked and pitch all look healthy; the control proves the fixture, the command path and the drive model can do the rotation, so the suppression is slope-specific and is what deadlocks the AI driver's pivot request
6a7f35d2 WT-039-D: fix two of my own measurement faults and raise the traverse budget to 150 s - progress now counts rotation as well as translation so a legitimate pivot in place is no longer reported as a stall (M26 lateral offset drops from 2173 to 19 stationary ticks) and the bearing is measured in the horizontal plane because signed_angle_to about UP is unstable when the target vector carries a large height difference; with those fixed the corrected numbers show a genuine standstill after 37-43 m on the uphill and lateral routes for all four vehicles, plus outright orbiting in two scenarios (M36 travels 701 m without arriving), and my earlier pivot probes are explicitly noted as not covering the failure because they ran only 600 ticks and never reached the crest at z=29
a8d11afb WT-039-D probe: refute the product-level pivot defect with two independent experiments - driving the model directly with zero throttle and full steer turns the hull -74.07 degrees in 120 ticks, and going through the production actor path with a controller that commands exactly that produces the identical result, so neither the drive model nor the command path drops the steering; the suite's frozen bearing therefore has a cause I have not identified yet, so the stall is explicitly NOT reported as a product defect, and the next step is to trace the command the actor actually consumes in that scenario and compare it with the driver's intent
3cadf262 WT-039-D: keep a floor under the ground-support scaling of the steering command (a single unsupported probe zeroed steer entirely, which cannot be right for a vehicle that can neutral-steer) and extend the traverse trace with the ground state, per-side support and yaw rate; the floor is regression-neutral (map checks stay 46/3 and historical road checks 29/4) but does not by itself end the stall, and the new evidence narrows the mechanism sharply - with throttle above zero the hull turns quickly, while at throttle zero it does not rotate at all even though the probes report grounded, full support on both sides and a neutral-turn profile, so the remaining suspects are how the yaw is committed to the body and the pitch composition
a01b5c10 WT-039-D: the creep-throttle fix for the driver deadlock is FALSIFIED and reverted - keeping 1.5 m/s of throttle while the heading error exceeds 18 degrees dropped map checks from 44-46 passing to 41 with 8 failures and historical road checks from 29/4 to 17/16, breaking even the M4A3 routes that had worked, because a creeping vehicle arcs away from the path instead of pivoting; both files are byte-identical to HEAD again, and the evidence points at the pivot path itself (steer scaled to zero by ground support on slopes while the stuck detector is skipped for large heading errors) as the place to fix next
bb089c6b WT-039-D: dump a per-tick trace for every failing traverse scenario (driver phase, waypoint, distance, bearing, speed and position, plus the raw throttle and steer command, floor state and the stationary counter), so a stall can be told apart from a recovery loop or a contact problem; also repairs a line break and an indentation I broke with a no-op edit, which had left the suite unparsable
afef5b2a WT-039-D: snap every driver goal to the nearest graph node, because the navigator allows a start within 16 m but a goal only within AI_GOAL_RADIUS_M of 1.6 m, which is why the reverse and lateral scenarios aborted on tick 1 with unreachable; the lateral deviation is now carried by the start so the goal remains plannable, and the fixture finally produces a complete per-vehicle per-scenario table instead of a mixture of fixture artefacts
664f0322 WT-039-D: the settle step now places every vehicle on the real collision mesh before any command (all twenty scenarios report settled=true, so the earlier hover was a fixture artefact), and two metric bugs are fixed - the floor-contact fraction divides by the ticks actually run instead of the tick budget, which had made any early-ending scenario read as never touching the floor, and every scenario now reports its tick count and final driver phase so an immediate abort can be told apart from a real stall
28ac70fb WT-039-D: reverse and lateral scenarios now take their height from the terrain at the offset position and the reverse condition runs on the production driver facing away from the goal, with backwards-motion ticks recorded per scenario; the run still shows both of those scenarios never touching the floor (on_floor 0.00, no travel), which means placing a vehicle by the analytic height is not safe on the 4 m faceted mesh and the fix is a settle step (spawn above and wait for a real floor contact) rather than another height calculation; meanwhile the uphill driver stall reproduces unchanged for all four vehicles after 37-41 m while the plain throttle controller completes the route with the M4A3 and M24
71da7aa2 WT-039-D: make the traverse battery interpretable before drawing any conclusion - the 0.5 m hull-box intersection on a 13 degree slope is normal rigid-box geometry and is no longer a pass criterion, ground contact is now judged from the production floor state, and the lateral-offset scenario no longer drives the vehicle into the hill (the west hill is centred on x=-116 while the road runs at x=-120); the run now shows a reproducible production-driver stall on the uphill flank route for all four vehicles (about 2200-2320 stationary ticks after 39 m, on the floor) while a plain low-throttle controller completes the same route with the M4A3 and M24, and it also exposes two still-broken fixtures (the lateral spawn never touches the floor and the reverse mode barely moves)
2bdd2c1f WT-031C-D: treat a whole-chassis running group as an authoring style rather than a missing role - the vocabulary now recognises the author's own node names (SixPneumaticWheelsAndSuspension, EightPneumaticWheelsAxlesAndSuspension, AxlesSuspension) and reports them as an unsplit group that needs an authored left/right split, which raises all-roles-ready from 80 to 87 and removes every phantom running-gear gap; the tallies are now recomputed from the final role table so an earlier incremental count can never leave a resolved role marked absent
c2dc375e WT-031C-D: separate absent roles from deviating axes and judge each class by its own roles - the true axis deviation is a single gun pivot (sdkfz_6_2_flak36 carries a baked 10 degree elevation) with zero turret deviations, five of the six flagged vehicles were merely unarmed, and a measured muzzle frame now satisfies its class requirement instead of being counted as missing; also fix the traverse battery's calibration, which read a throttle off a freed actor and therefore aborted before any verdict, and note that its 24-minute run produced no product conclusions
b1ab9a68 WT-031C-D: compose the measured muzzle through the recorded parent chain instead of expressing it in one node's space - the offset is now given in both the root and the turret frames with the full chain listed (TurretPivot > GunPivot > barrel mesh), the barrel's Z range and its forward convention are recorded for author confirmation, and a within-envelope note flags the sampled vehicles whose barrel tip does not extend past the bounding box so the direction convention can be settled with evidence rather than assumed
84226f98 WT-031C-D: withdraw the muzzle position taken from the gun pivot origin - a pivot sits at the breech side and is not a muzzle - and measure it instead from the single barrel mesh's forward extremity in the turret's frame, leaving it missing with an explicit reason where no muzzle node and no single barrel mesh exist; also fix the readiness counter, which still counted the muzzle as missing after it had been measured, so the structural pass count finally reflects the corrected resolution
17d7b3ed WT-031C-D correction accepted: withdraw the '38 vehicles have real author gaps' figure - a mesh-only scan missed the author's empty pivots, so resolution now reads every node and records full relative paths, parent links, local axis directions and mesh AABBs; turret resolution rises from 69 to 92 nodes with only 4 missing, the muzzle is measured per vehicle from the gun mesh's forward extremity instead of a constant offset falsely labelled author provenance, vehicles are classified (84 single-turret, 7 multi-launcher, 3 support, 2 unarmed) so the six-role scheme is not forced onto unarmed or multi-launcher hulls, and the audit states plainly that shape validation alone proves nothing about hierarchy, muzzle direction, combat layout or internal modules
3fba1016 WT-031C-D: finish the draft binding against the validator source instead of guessing - axes carry space local, the mechanism's +Y yaw and +X elevation and two degree limits for turret and gun only, internal_attachments carries both ID maps, and units carry the measured envelope_m, the measured source unit and conservative tolerances, with the axis limits labelled as game-design values; the only remaining shape errors are then the genuine author gaps (27 turret and 5 muzzle paths), which makes the structural pass count a meaningful number for the first time
d4b393d0 WT-031C-D: complete the draft binding to the validator's exact contract after its error strings listed the required wrapper fields (source_vehicle_id, source_unit, explicit envelope and attachment tolerances, an explicit gun axis and an internal module map); supply only values that are honest - measured unit, conservative 0.02 tolerances, forward gun axis - and leave the internal module map empty rather than inventing modules, so the remaining blockers are a concrete list of author data and the two out-of-repo gates
28fcd78b WT-031C-D correction: the previous commit claimed the draft-binding step worked, but the suite could not run - a no-op edit had joined the summary_line signature and its body onto one line, giving 'Unexpected Indent in class body' at line 103, so RoleMappingAudit failed to parse and the runner could not resolve it; the signature is restored, the script passes --check-only, and the audit now actually executes with its per-vehicle shape errors, pending author steps and the two out-of-repo gates
448b1295 WT-031C-D registration-readiness step: build a draft binding from the resolved node roles for each German model, validate its shape through the production validator, and keep the derived muzzle frame as an explicit pending author step instead of putting two roles on one node; reports per-vehicle shape errors, pending author steps and the two gates that cannot be decided in this repository (the asset lives outside it, and its licence is unknown), so the remaining work before registration is a concrete list
9441b33a WT-031C-D scale-up: read-only role-mapping audit over all 96 German models with an evidence-based hint table (HullArmour/TurretArmour/MainGunAndMuzzleBrake/track_l/wheel_l_*), reporting 96/96 parsed and 58/96 with all six roles resolved, plus per-role counts (hull 96 node, gun 91, turret 69, running gear 82 node with 10 ambiguous wheel sets and 4 missing) and a per-vehicle JSON authoring list; also fixes a false positive where the muzzle hint matched the compound gun mesh and is now strict, so a muzzle is only a real muzzle node or an authored frame
a43bd226 author-input milestone: record and audit the German model set read-only - the source tree at E:/AIprogram/aimodel/德国 holds 96 folders with one uniform vehicle.glb each (96 GLB / 71.3 MB, 96 Blend / 29.3 MB, 28 kB index), nothing is copied or modified, and a read-only intake auditor now measures per vehicle the bytes, SHA-256, triangle count, envelope, unit and binding-role resolution so registration readiness becomes a per-vehicle list; also fixes the triangle counter being called after the scene was freed, which had made all 96 parses look unparsable
8ef7e017 WT-039-D fixture calibration: replace the assumed low-speed throttle with a measured per-vehicle engagement setting after sweeping the throttle-to-motion curve on flat ground - M4A3/M36 need above 0.3, M26 needs above 0.3 (0.3 gives 0.05 m and zero peak speed) and M24 engages at 0.2, which is a real per-vehicle dead-zone and explains the earlier stall counts as a fixture artifact; the flat-ground control now also requires measurable travel, and the bottoming monitor is proven correct (0.0000 m on flat ground) while the full scenario battery still needs a longer background run
c5d07dbd WT-039-D (option D as instructed): clarify that max_vehicle_size is (width 4.2, height 2.4, length 8.5) with no in-repo justification and that no in-repo vehicle reaches 8.5 m, relabel the old check as a design-goal check that currently fails while keeping its real result and number, and add a separate-identity production-vehicle traversal suite (T039-D) that uses production driving/collision/suspension and refuses to report a verdict until its monitors are calibrated, because the first run showed the penetration counter firing on flat faceted ground and its scenario numbers therefore cannot be cited as product evidence
7280530a WT-039-R1 decision request after two falsified fixes: the second experiment (2 m terrain cells) leaves the blocked-sample count essentially unchanged at 40 versus 38, proving the cause is the analytic hill curvature against the declared rigid 8.5 m envelope rather than mesh discretisation, so village_world.gd is reverted to byte-identical and a decision document lays out the measured fact chain plus four options (author a C1 road cut, change the envelope semantics, keep the diagnostic and register the gap, or fix the test proxy) with the note that real suspended vehicles do traverse and the village/structure suites are green
23bdcb4d WT-039-R1: the flank-road grading fix is FALSIFIED and reverted - the C0 road cut raised the blocked-sample count from 38 to 55 because the lateral blend back to the analytic hill created new wedging slopes at the shelf edge, so village_world.gd is byte-identical to the baseline again and the re-run confirms 38; records the negative result with its cause, resolves one gate DEFERRED (village battle now 21/21 PASS in 206 s, structure 54/54) and ranks the remaining options, preferring a terrain-resolution experiment to test whether the defect is mesh discretisation rather than hill curvature
2448836f WT-039-R1 root cause quantified and validator gap closed: the max-size proxy stalls at (-120.8, 4.02, 19.8) with throttle 0 and full steer for 240+ ticks on a 12.2 degree slope, which is far below the 28 degree limit, because the map's declared rigid envelope (4.2x2.4x8.5 m) is wedged by the ground at the flank-road crest - and the bake never tested that, since its envelope query excluded the ground; add a ground-inclusive envelope query reported separately (ok semantics unchanged) plus an explicit T018-03b check, which now fails with 38 blocked samples out of 1786 while the pre-existing T018-03 still passes, proving no existing assertion was altered
12bc6bbe WT-039-R1 hardening + verdict correction: add a test-side route harness (spawn-clearance assertion placed BEFORE actor creation and a per-tick trace dumped on failure) to both reachability suites, then use the traces to CORRECT round 3 - the failures are NOT a flaky harness but product-level stalls, with the maximum-size proxy frozen at speed 0.000 for 300+ ticks on the western hill (y=3.951) and the M26 crawling at ~0.05 m/s into the 12000-tick budget, deterministic across three runs; also correct the order-numbering error where the regression gate was mislabelled WT-035-R1 when WT-035 is the UI/HUD/tutorial order and the gate belongs to WT-036
ea18b3e5 WT-039-R1 investigation round 3 (no code changed): the seven baseline-red reachability checks are a flaky harness, not a product defect - every failing case passes in isolation (map slot 2 arrives after 3218 ticks, slots 0-4 all arrive in a replicated back-to-back sequence with zero leftover bodies, and the M26 arrives with bounded motion at every failing slot including a repeat), while three rounds of direct measurement refute the width, envelope, spawn-occupancy, graph-coverage and turn-margin hypotheses and the failing slots drift between runs; recommends harness hardening that keeps every assertion and threshold untouched, and flags that a post-hardening intermittent failure would then be a product-level defect requiring tick traces
772a4a41 WT-039-R1 investigation round 2 (still no code changed): two hypotheses refuted by direct measurement - all 16 spawn slots plan a path for the widest vehicle (M26 gate 4.11 versus 12 m graph edge width, nearest node 0.03 m away) and all 16 spawn poses have zero collision overlap with the M26 hull box, so the failures are neither routing nor placement but a driver-control margin on specific spawn-to-waypoint geometry; records the failing signatures, the two new hypotheses (turn/start margin for the longest-widest hull, single-tick motion spike) and the exact per-tick diagnostics to run next
03cc9ed4 WT-039-R1 investigation (design phase, no code changed): root-cause the two baseline-red reachability suites from their real route event streams - map checks fail differently per slot (slot 2 never plans: unreachable_or_insufficient_width after two reverse recoveries; slot 0 arrives but trips the motion bound), historical road checks fail ONLY for the M26 from the outer slots while it ends ~40m off target, and both are reproduced identically on the untouched baseline; records verified facts versus four hypotheses with the exact files and measurements needed next
