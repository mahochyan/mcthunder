# WT-040-R1：当前续办裁定

> 来源：**用户转交的本次对话续办指令**（WT040-CONT-20260917-01，原文逐字保存）。本行由执行端补注来源，非原文内容。

指令标识：WT040-CONT-20260917-01  
直接来源：本次对话中重新下达的续办指令。  
适用范围：`work/continuation-20260913` 的当前工作，不重做已经交付的 A/B/C/D。

## 0. 先解决指令歧义

执行端所列的“候选 B”，确实对应此前的 A/B/C/D 四张追加裁定单。它不是本轮应重复实施的任务。“候选 A”是更早的待裁定问题清单，也不是当前执行依据。

当前依据是对上一份收官报告作出的后续裁定，现全文自包含地下发。无需再次查找历史文件、durable 记录、桥接目录或“四项裁定”字样。旧 A/B/C/D 的已实现部分保留，必要时做受本次改动影响的回归；不以重新追溯旧证据作为本轮主要交付。

执行端本次报告的起点为 `1b48a33e`、基线以来 384 提交。开始时只做一次实际 HEAD 和工作区核对；若已有后继提交，保留后继成果，不切回该旧 SHA。原 `main` 和用户未提交内容不动。本次报告列出的五个未跟踪文件原样保留，不自动删除、覆盖或纳入提交。

## 1. 通行／推进限制：P1，允许修复

单独立项、按 P1 优先处理，但仍属于“现代河谷独立可玩候选”的未完成条件。不能转出一个新单就宣布总目标完成。

先保留原受阻车型、配置、位置、地图和规则，做短对照：同一路段玩家与 AI；同车平地与受阻点；单车与多车；提交命令与实际消费命令。记录驱动能力、左右接地、坡度与阻力、期望速度、实际位移、碰撞对象、驾驶状态，定位最早失败的一环。

允许修复证据确认的生产逻辑、输入消费、接地碰撞、导航接线以及单位、尺度和配置错误。不允许为了通过而任意提速、关闭阻力／碰撞、传送、放宽判据或改胜负规则。

交付：原问题短复现、限定范围补丁、修复前后对照、相关回归及实际驾驶／AI通行结果。之后再回到现代河谷整局。

## 2. 工业战斗异常耗时：同一被测提交，独占复跑一次

使用发生异常那次构建的实际 source_sha，不用最新文档 HEAD 冒充。保持引擎、测试、地图、车辆、种子、启动方式和超时一致，不同时导入、构建或跑其他 Godot 任务。

只记录异常复核需要的执行起止、仿真推进、进程退出、结果和并发情况。不采集 FPS、p95/p99 或容量指标，不解除性能 HOLD。

再次慢只能证明相同条件下再次复现；恢复只能证明本次未复现，不能凭一次运行宣布环境根因。超时或异常如实保留，不增加预算来获得通过。本项不要求重新跑全部门禁。

交付：一份同条件复跑记录及有限结论；没有证据不编造原因。

## 3. 包内完整玩家流程与现代河谷内部包：继续交付

先复验执行端报告的当前候选包自身的完整玩家流程，依据真实 BUILD_MANIFEST 绑定版本。M36／入库开放项以当前复验为准：旧包失败、树内通过和当前包通过分别记录。已经通过的入口不重写。

`--verify-installation` 不能代替完整玩家流程；历史地图流程不能代替现代河谷流程。

本阶段指定的内部现代候选包，必须具备两辆工程车、对应弹种／布局／模型绑定和河谷团队场景。禁止公开发布，不等于禁止将有权使用的候选资源装入内部开发包。若个别素材确有使用权限限制，明确点名并隔离，以权限明确的替代资源推进；不能笼统将全部现代内容排除后认定原目标完成。

允许修复相关本地导出、资源加载和清单契约。优先核查运行时原始 GLB 字节读取与导出资源转换之间是否不一致；这是假设，必须由包内证据验证，不预设根因。不得关闭身份、哈希或绑定检查。

历史模式可以在可选工程内容缺失时明确降级；但本阶段必需的现代内容缺失，必须让“现代河谷候选验收”失败。不得回退到历史车、训练车或其他地图后冒充完成。

保留已经实现的真实前端路径、工程接线和分列记录，补齐同一玩家生命链：敌方实际发射／接触／致命损伤 → 死亡与扣票 → 等待面板 → 正常点击再出击 → 新生命保持具体车型与配装。弃车后的 UI 再出击与其他车辆的实弹死亡是各自有效的分项证据，不合并为这条尚未验证的同一生命链。

可以用明确标注的实弹专项夹具先验生命周期，再做正常规则的完整河谷局。两类证据分开。不改正式票池、占领速度、伤害、装填或终局条件来强行产生死亡。

最终交付：绑定实际源码、内容和规则版本的现代河谷内部可玩包；必需内容清单；包内完整流程结果；真实截图／事件证据；已知问题；回退点。

## 4. 验收、许可与性能边界

真人保持 PENDING／NOT_RUN，不代签。性能专项保持 HOLD_BY_USER。所有本阶段包 release_ready=false、public_release=false。允许正常窗口、自动输入、资源完整性和独立包功能验证，这些不构成真人接受或性能验收。

不升级引擎，不采购或公开部署，不强推，不改原 main，不丢弃用户未提交内容。允许现有隔离分支上的必要代码、配置、相关测试和本地构建修复。

当前总状态应表达为“工程接线与历史内容候选包已交付，现代河谷集成交付进行中”。保留已经完成的子项，不重开全部工作；推进限制与必需现代内容包缺口仍是总目标出口条件。

### 同时有效的构建约束

停止默认使用执行端新增的 `R3-A.*58\.55` 对应豁免（以实际代码中的该 R3-A 规则为准），保留诊断记录及此前已明确裁定的两项已知失败例外。窄签名本身不代表已获得例外批准。

再次出现 R3-A 问题时，应报告、修复或单独提出有边界的例外。未触发该例外的旧构建，不因此倒判失败。脚本错误、退出码未知、超时、不完整结果、未登记失败和必需内容缺失不能被豁免。

## 5. 落盘与下一次交付

本文件是当前指令原文。可以原样保存为：
`docs/wt/continuation/WT-040-R1_CONTINUATION_DECISION_20260917.md`

来源写“用户转交的本次对话续办指令”，不冒称仓库里原先已有。当前入口文档只需增加一条指向本文件的链接，不再批量核对或重写旧文档。

阶段 0 的历史原文搜寻到此结束。下一步进入通行短复现和当前包验证；耗时复跑在引擎空闲、条件相同时安排。

下一次报告应交付实际复验结果或限定范围补丁，以及必需现代内容是否已经进入独立包。不要再次只报告“搜索了哪些目录”“未找到裁定”或“旧四单已完成”。若复验表明某项不再需要代码修改，保留证据即可，不为制造 diff 而改动。
