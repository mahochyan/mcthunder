# 第 4 轮交付终稿（让行回归攻坚 · 门禁扩容 · D2 裁定 · 资产 ⑤）

## 1. 最终门禁（最终 HEAD，111 套件）
```
PASS = 109 · FAIL = 2 · ZERO = 0 · TIMEOUT = 0
```
两项失败**均已证实为非回归**：`industrial_battle` **15/1＝基线持平**（既有抵达红 ✓）· `challenge` **138/2**（已登记夹具边界 ✓）

## 2. 完成项（全部有证据）
| 项 | 结果 | 证据 |
|---|---|---|
| **让行/僵持回归（我曾引入）** | 四门同时达标 ✓（`ai_drive` 40/0 · 村庄 21/0 · 地图 48/0 · 战斗 15/1＝基线） | R7 五策略对账表 · R10 |
| **全量门禁** | 111 套件 **109 PASS** ✓ | `logs/WT-036-R1/full-gate-111-final/` |
| **门禁覆盖扩容** | **32 → 111** 套件（缺口 147 → 68）✓ | `WT-036-R1_GATE_EXPANSION_LOG.md` |
| **D2 裁定落地** | 8.5 m 包络改为**声明的设计目标**；`map` **48/0** ✓ | `WT-036-R1_ENVELOPE_DECLARATION.md` |
| **资产 ⑤ 可执行部分** | 适配产物**位置断言 delta=0**（三车 ✓）· draft layout ✓ · draft `source_record`/`geometry`/binding ✓ | `logs/WT-030D-r2/` |
| **工具修复** | 运行器**日志证据判定** ✓ · 默认超时 **900 → 1500 s** ✓ | R9/R10 |

## 3. 撤回与更正清单（本会话自我修正，交接必读）
| # | 曾声称 | 实际 |
|---|---|---|
| 1 | "适配产物 `MuzzlePoint` 位于原点（缺陷）" ✗ | **撤回**：产物**正确携带位置**（局部形式 ✓）；是**我在未入树的场景上读 `global_position`（静默返回 0）** ✗ 伪造了缺陷 ✓ |
| 2 | "`T018-02` 已修复（仅凭 `phase=arrived`）" ✗ | **更正**：该检查还需 **`reverse` 事件**；最终由"优先级+双时限重规划"满足 ✓ |
| 3 | "256 条 capture 与 256 条 supply 路线" ✗ | **更正**：共 **256 条路线**（128 + 128 ✓） |
| 4 | "已应用套件时序修复" ✗（文件实际未改） | **更正**并用 edit 工具真正应用 ✓ |
| 5 | "draft 现显示实测值而非 0" ✗ | **更正**：draft 工具同样离线读坐标 ⇒ 全 0 ✗；修正后为实测值 ✓ |
| 6 | 运行器对 4 个套件的 `FAIL` 判定 ✗ | **更正**：本环境 `ExitCode=$null` 所致 ⇒ 改按日志证据 ✓ |
| 7 | 本文件曾**只写入路径字符串** ✗ | **更正**：here-string 未赋值即被打印 ⇒ 已重写 ✓ |

## 4. 未完成项及原因
| 项 | 原因 |
|---|---|
| `challenge` 防守（138/2） | **夹具能力边界**（脚本赢不了 hard 变体）⇒ 需裁定 B1–B4 ✓ |
| 工业战斗**抵达率**（15/1） | **既有基线红** ⇒ 建议独立立项 ✓ |
| `T018-H01`（1 槽位） | **既有 + 间歇 + 预算边际** ⇒ 裁定 A1/A2 ✓ |
| M26 坡上起步 | **调校冲突**（九次驱动侧修复全否证）⇒ 裁定 C1–C3 ✓ |
| 资产 ⑤ 完整校验 | **待作者字段与签收**（每车 `needs_author` 6 项 ✓）——不虚构 ✓ |
| 全量 96 车适配（≈70 MB）· 导出预设 | **待授权**（E1–E4）✓ |
| 真人验收 · 性能采样 · 公网 · 10v10/16v16 · 空海 | **NOT_RUN / 待授权** ✓ |

## 5. 风险与回滚
- **风险**：地图铺装＝**可见外观变化**（无几何/掩体/通视影响 ✓）· 让行策略影响 AI 交通（**四门已验证** ✓）· 资产侧新增产物（draft `.tres` 3×1.8 KB + 适配 GLB 2.8 MB，可删 ✓）；
- **回滚**：每项**单提交**可退（`tank.gd` · `ai_path_driver.gd` · `game_config.gd` · `village_definition.gd` · `map_definition.gd`（注释）· 测试/工具文件）✓；整体可切回 `f261e785`（第 2 轮交付）或基线 `a1bac406` ✓；
- **原 `main` 工作区全程未被改动**（porcelain 恒 **379** ✓）。

## 6. 交接（先读）
1. `NEXT_ACTION.md`（第 3 轮段落 + 工程陷阱 ✓）→ `REGRESSION_GATE_MASTER.md` + **`_ADDENDUM_R4–R10`** → `DECISION_REQUEST_BUNDLE.md` → 本文件 → `DOC_INDEX.md`；
2. **四条工程陷阱**：未入树读全局坐标 ✗ · 节点存在≠位置正确 ✓ · 导出锚点挂车体根 ✓ · `Start-Process` 退出码不可靠 ⇒ 用日志证据 ✓；
3. **门禁规则**：慢档不入禁 · **诊断电池永不入禁** · 零输出/环境依赖 ⇒ NOT_RUN · 网络类 ⇒ 专用入口 ✓。

## 7. 变更清单
- 提交数：`git -C <cont> rev-list --count a1bac406..HEAD`（**159** 提交 ✓，本更正为第 160）；
- **产品代码净变更（相对基线）**：61 files changed, 3780 insertions(+), 21 deletions(-)
- 交付文档：`docs/wt/continuation/`（**118** 份 ✓，索引见 `DOC_INDEX.md`）。
