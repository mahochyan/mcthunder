# WT-036-R1 结果：工业套件 **595 PASS / 0 FAIL**（EXIT=0）

## 1. 终值
```
EXIT=0
PASS=595 FAIL=0
```
| 组成 | 数量 | 结果 |
|---|---|---|
| 原始检查（4 车 × 2 队 × 8 槽 × 2 目标 × 2 图 + 结构与转场） | 339 | **全部通过** ✓ |
| 本分支新增**出生净空断言**（每路线一条） | 256 | **全部通过** ✓ |
| **合计** | **595** | **0 失败** ✓ |

## 2. 基线对照（同一套件）
| | 基线 main | 本分支 |
|---|---|---|
| PASS / FAIL | **283 / 56** | **595 / 0** |
| 退出码 | 非 0 | **0** ✓ |

⇒ 基线 56 项失败中：**supply 36 · capture 19 · restart 1** —— 全部清除 ✓

## 3. 达成路径（每一项都有证据）
| 步骤 | 改动 | 证据 |
|---|---|---|
| 卡死窗口失效（重规划每 ≤1 s 清零 2 s 窗口） | `ai_path_driver.gd` | 恢复首次能触发 |
| **让行死锁**（`yielding` 时油门置 0 ⇒ 窗口不累计；停放车永久阻塞） | `ai_path_driver.gd`（**仅"无主车辆"触发恢复**） | 地图 `T018-02` 通过 · 村庄 21/0 |
| 坡面自转 + 转向支撑下限 | `tank.gd` · `game_config.gd` | T039-E 3/3 |
| **两处 C 类铺装**（`near→cap`、`supply→spawn`；用户已授权） | `village_definition.gd` | 历史 **33/0** · supply **36→0** |
| 测试侧加固（净空断言 + 落地稳定 + 失败轨迹） | `run_industrial_checks.gd` | 256/256 净空通过 · 机制可诊断 |
| 转场时序（等 `_transitioning` 清零，替换固定 8 帧） | `run_industrial_checks.gd` | restart 检查转绿 |

## 4. 影响声明（地图改动）
铺装仅影响**地面分类（通行阻力）与外观**：无地形几何、掩体、障碍、通视变化；密集 `spawn↔spawn`、`cap↔cap` 仍为开阔地 ✓ 回滚＝单提交（+1 行级）✓

## 5. 仍未结（不因此宣称完成）
`T018-H01` 预算（A1/A2）· `challenge` 防守（B1–B4）· M26 坡上起步（C1–C3）· 8.5 m 包络（D1/D2）· 授权（E1–E4）· 真人验收与性能采样（NOT_RUN）
