# WT-040-R1 **决策表**（一页 ✓，五项 ✓）

> 用途 ✓：把本阶段所有**待裁定**事项收成一页 ✓；每项给出**我需要什么** ✓、**选项** ✓、**代价** ✓、**我的建议** ✓、
> 以及**你回复后我立刻执行的下一步** ✓。全部证据见 `WT-040-R1_EVIDENCE_TABLE.md` ✓ 与 `FINAL_COVERAGE_MATRIX.md` ✓。
> 当前状态 ✓：分支 `work/continuation-20260913` ✓（**296 提交** ✓）· 基线 `main @ a1bac406` **未改动** ✓（porcelain **379** ✓）·
> 官方门禁干净运行 ✓ **128 套件 / 126 PASS / 2 FAIL = 既有两红** ✓（**零回归** ✓）。

---

## ① armor 17 zone（含**准入**）—— 归属：**你**
| | |
|---|---|
| **问题** ✓ | 两车的 17 个装甲区我**已按档案映射表逐项落好草案** ✓（T-80B 12 项如实 `unknown` ✓）；但**准入判断**（`runtime_admitted` ✓）与**粒度取舍**（档案分区比我细 ✓）只能由你定 ✓。 |
| **我需要什么** ✓ | 一句：**接受现状** ✓ / **指定粒度** ✓ / **授权我按已写映射表填并登记为 estimated** ✓。 |
| **代价** ✓ | 全部为**游戏设计初值** ✓，不冒充真实性能 ✓（已逐项标注 ✓）。 |
| **我的建议** ✓ | 授权我按**已写映射表**填写 ✓ + 状态 `estimated` ✓ —— 映射表与逐区行号已在 `WT-040-R1_ZONE_MAPPING_T80B/LEOPARD.md` ✓。 |
| **你一回复我就做** ✓ | 落 `armor` 17 项 ✓ → 跑 `run_modern_vehicle_pipeline.ps1` ✓ → 报缺口变化 ✓（预计两车各降若干 ✓）。 |

## ② 河谷（**条目冲突** ✓）—— 归属：**你**
| | |
|---|---|
| **问题** ✓ | 阶段指令要求"**河谷接入正式团队场景（先 4v4）**" ✓；而**地图自身定义**写着 `assert(team_size in [10,16])` ✓ · `status:"design_preview"` ✓ · **`combat_admitted:false`** ✓ ⇒ **冲突** ✓（已按红线停下 ✓）。 |
| **项目原文** ✓（锐化代价 ✓） | `scripts/maps/map_registry.gd:10-12` ✓："**NOT listed in IDS** … `combat_admitted=false` … **Adding it to IDS is the one-line change** that would expose it in the garage list." ✓ ⇒ **准入本身 = 一行** ✓（但**其余三项代价仍在** ✓：真 4v4 支持 ✗ · 拥堵 ✗ · 图余量 **167** 节点 ✗）。 |
| **对照** ✓ | 当前 `MapRegistry.IDS = ["hill_village","industrial_edge"]` ✓（两张面向玩家的地图 ✓）；`VehicleCatalog.IDS` = **4 辆历史车** ✓ ⇒ 与现代车一致地"**未准入即不出现**" ✓。 || **选项** ✓ | **A 提升为可战斗** ✓ —— 代价**四项** ✗：真 4v4 支持（现**静默走 16v16** ✗）· 翻转准入标志 ✓ · 修拥堵 ✓ · **图余量仅 167 节点** ✗。<br>**B 维持预览** ✓ —— 团队闭环**改由既有正式地图承担** ✓（**已实测**：`run_team_checks` **67/0** ✓ · 村庄 **21/0** ✓ · 工业为**真实 4v4 占点对局** ✓）。<br>**C 仅归档** ✓ —— 7 次尝试与结构发现全部留证 ✓，不再改动 ✓。 |
| **我的建议** ✓ | **B** ✓ —— 与项目**三处明文约定**一致 ✓（`map_registry` ✓ `river_team_definition` ✓ `river_team_range` ✓ 均声明**不翻转准入** ✓），且**不阻塞交付** ✓。 |
| **你一回复我就做** ✓ | **B**：把阶段 ② 的承担者写成既有地图 ✓ 并复核其套件 ✓。**A**：先做**只读设计评估**（真 4v4 支持面 ✓ + 拥堵归因**未定项** ✓）再报工作量 ✓。**C**：只更新文档 ✓。 |

## ③ G–L 外部输入 —— 归属：**你/设计**
| | |
|---|---|
| **我需要什么** ✓ | 两车**仅剩两项** ✗：**弹种集**（`compatible_shells` ✓ —— 校验器报 `vehicle has no admitted shell set` ✓）与**豹2 复合装甲的显式版本化 `response_profile` 规则** ✓。另有三项**史料**（`dimensions.*` 须**文献原值+推导** ✓）与三项**设计值**（`reload_time` ✓ `pitch_min/max` ✓ 多点 `penetration_curve` ✓）。 |
| **代价** ✓ | 史料项若缺 ⇒ 只能留空 ✓（**不编造** ✗）；设计项可给**初值** ✓ 但须标明为设计 ✓。 |
| **我的建议** ✓ | 先给**弹种集**与**复合装甲规则**（这两项一落，两车 `LayoutValidator` 预计**清零** ✓）。 |
| **你一回复我就做** ✓ | 落数据 → 跑流水线 ✓ → 报 `LayoutValidator` 与 `definitions` 结果 ✓。 |

## ④ 构建：`fresh_import` 退出码 `$null` ✗ —— 归属：**你**（**红线相关** ✓）
| | |
|---|---|
| **问题** ✓ | `tests/build_release.ps1` 在首步 `fresh_import` 判失败 ✓，但**其自身 stdout 显示导入成功** ✓（`[DONE] reimport` ×2 ✓ · **0 条错误** ✓）而 **`exit_code=null`** ✗ ⇒ PowerShell `Start-Process` 退出码陷阱 ✓。**该脚本就是"构建发布流程"** ✗ ⇒ 我**未改** ✓。 |
| **选项** ✓ | **授权修一行**（退出码判定 ✓）；或 **包校验暂缓** ✓。 |
| **代价** ✓ | 修一行属**改构建流程** ✗ ⇒ 需你**明确授权** ✓；暂缓则**独立包校验**保持未完成 ✓（其余运行证据已足 ✓：**127/0** ✓ · **51/0** ✓ · **50/0** ✓ · **14/14** ✓）。 |
| **更正** ✓ | 我先前的说法"**缺 manifest 会响亮失败**" ✗ **是错的** ✓ —— `run_player_flow_checks.ps1:11` 把 manifest 校验包在 `if (Test-Path …)` 内 ✓ ⇒ **不存在则跳过而非抛错** ✓ ⇒ **无 manifest 也能跑** ✓。 |
| **但仍有诚实的坎** ✗ | 现存 `PixelArmorClient.exe`（09-15 09:28 ✓）**无 manifest** ✗ ⇒ 无处读取其 `source_sha` ✗；历史 `PACKAGE.json` 仅两份（`b9051149…` / `9998f448…`，均 09-10 ✗）且其 zip **已不存在** ✗。脚本 `-SourceSha` **必填** ✓ ⇒ 若填**当前 HEAD** 等于**宣称**该 exe 出自当前提交 ✗ ⇒ **不实** ⇒ **我不做** ✗。 |
| **结论** ✓（未变但更精确 ✓） | 选项 ④ 的目标（**与已知源码绑定的独立包校验** ✓）**仍需一次新构建** ✓ ⇒ **仍需您授权那一行** ✓。 || **备注** ✓ | 客户端包**本已存在** ✓（`backups/builds/PixelArmorClient.exe` ✓ 出自预设 **`Windows Client`** ✓）；缺的只是 **`BUILD_MANIFEST.json`** ✓（**仅**由该脚本写出 ✓）⇒ **我未手工伪造它** ✗。 |
| **你一回复我就做** ✓ | 授权 ⇒ 改一处退出码判定 ✓ → 重跑构建 ✓ → 跑 `run_player_flow_checks.ps1` ✓ 交付**独立包校验** ✓。 |

## ④b **新增发现**（本轮 ✓）：导出包**缺模块档案** ✗
| | |
|---|---|
| **事实** ✓ | 我用**现存客户端包**跑了首例独立验证 ✓（**无 manifest 亦可** ✓）⇒ 包**能启动/渲染/导航** ✓（**2 张真截图** ✓），但**挑战启动失败** ✗ |
| **根因** ✓（包自身 stderr ✓） | `ballistics: defs load failed: vehicle player_tank: loading_profile / fire_control_profile / optics_profile: missing` ✗ ⇒ **导出包未含模块档案** ✓ |
| **旁证** ✓ | `RESULTS.json` 列 **5 张缺失截图**（`05_real_shot_replay` ✓ · `09_battle_0/1` ✓ · `10_settled_0/1` ✓） |
| **与 B4 区别** ✓ | **不同类** ✓：B4 是**测试夹具边界** ✓；本次是**导出包缺数据** ✗ |
| **归属** ✓ | 修复属**导出过滤/构建发布流程** ✗ ⇒ **未改** ✓ ⇒ **须并入 ④ 一起决定** ✓ |
| **建议** ✓ | 若授权 ④ ⇒ 一并查 **导出过滤**（含档案路径 ✓）⇒ 再跑 `run_player_flow_checks.ps1` ⇒ 目标 **8/8** ✓ |## ⑤ 河谷 `0/8` 的**未定原因** ✓ —— 归属：**你**
| | |
|---|---|
| **问题** ✓ | 第 7 次尝试使可达性变为 **0/8** ✗；我两处归因（**地形** ❌ · **容量** ❌）**均已作废** ✓，**现明确标注"未定"** ✗（**不编第三说** ✓）。 |
| **选项** ✓ | **继续追**（可加**只读**诊断 ✓，如记录规划器实际节点序列 ✓）；或 **归档** ✓（若选 ②B 则此项**自动失去意义** ✓）。 |
| **我的建议** ✓ | 若选 **②B** ⇒ **归档** ✓；若选 **②A** ⇒ 再追 ✓。 |

---

## 附：**本阶段已完成**（无需你决定 ✓，仅供核对 ✓）
| 项 | 结果 |
|---|---|
| 两车战斗包（可实测部分） | 适配产物 ✓ · `geometry` **50/0** ✓ · `facts` 带引用 ✓ · `assembly` ✓ · `modules` **精确配平** ✓ · `crew` ✓ |
| 两车 `definitions`（车/炮/弹） | **全 0 错误** ✓✓ |
| 两条命令入口 | `run_modern_vehicle_pipeline.ps1` ✓ **9/9 `MODERN_PIPELINE_OK`** ✓ |
| 两项几何修正 | **证伪循环**落地 ✓（`LayoutValidator` **4→0** ✓ · 豹2 **7→2** ✓，余 2 项为**设计** ✓） |
| 官方门禁（独占干净运行 ✓） | **126 PASS / 2 FAIL = 既有两红** ✓ ⇒ **零回归** ✓ |
| 运行证据 | 应用流程 **127/0** ✓ · 辅助能力 **51/0** ✓ · 科技树 **50/0** ✓ · **无注入真实对局 14/14** ✓ |
| 工具健壮性 | **三条假绿通道**已封 ✓ + **我自己的验收判据**已改正 ✓ |
| 验收工件 | **已刷新至现行代码** ✓（旧件标注 `SUPERSEDED` ✓） |
