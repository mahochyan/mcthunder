# 裁定就绪证据单（决定后即可**立即执行**，每项含复现命令、证据、选项落地步骤、成本与回滚）

## A. `T018-H01`（出生→占点，1 槽位间歇失败）
- **复现**：`& <godot> --headless --path <cont> --fixed-fps 60 -s res://tests/run_map_checks.gd`
- **证据**：基线**同样失败**（`main` 25/2 含此项 ✓）；逐轮槽位不同（观测 1/0 · 1/3 · 1/5 · 2/0 ✓）；轨迹显示**截止时仍在推进**（枢轴转向后 `speed=1.221`、`dist_wp=2.27` ✓）
- **A1（提高预算）落地步骤**：`tests/run_map_checks.gd` 中该路线驱动循环的 tick 上限（当前约 6400）提高（如 9600）✓；**代价**：改**测试语义**（行进预算），须在门禁口径中显式记录 ✓；**风险**：可能掩盖真实"走不到"的路线 ✓ ⇒ 需同时保留"仍在推进"的轨迹断言 ✓
- **A2（保持登记）落地步骤**：无需改码 ✓；门禁按**失败集合**判定（允许 ≤1 个此槽位 ✓）——已在 R10/扩容日志中写明 ✓
- **我的建议**：**A2**（基线既红且 A1 削弱断言语义 ✓）

## B. `challenge_defence_waves`（138/2）
- **复现**：`-s res://tests/run_challenge_checks.gd`（约 200 s ✓）
- **证据**：`hold_ground` 需 **占区 45 s（已达标 ✓）＋ 击杀全部敌人**；实测 `held=45.0`、`kills={}`、`player_shots=0`、`repairs=4` ⇒ 脚本飞行员被启用 AI 打瘫 ⇒ hard 150 s 超时 ✓
- **B1（强化夹具）落地步骤**：`tests/run_challenge_checks.gd::_hold_active` 内改为"先灭火/退避 + 借助掩体 + 侧翼射击"，再前出交战 ✓；**代价**：测试侧中等工时 ✓；**风险**：不保证能赢 hard ✓
- **B2（改测 normal）落地步骤**：`_hold_active("normal")`，并把 hard 另立一项 ✓；**代价**：**改变覆盖面** ⇒ 必须在文档与门禁口径中显式记录 ✓
- **B3（复核 hard 平衡）落地步骤**：需改 `challenge_catalog.gd` 的 `enemy_rounds`/难度 ⇒ **属玩法平衡，须授权** ✓
- **B4（保持登记）落地步骤**：无需改码 ✓
- **我的建议**：**B4**（或 B1 若愿投入测试侧工时 ✓）

## C. M26 坡上起步（调校冲突）
- **复现**：`-s res://tests/run_flank_crest_traversal_checks.gd`（诊断电池 ✓，约 160 s ✓）
- **证据**：`(−120.2, 1.66, −30.9)`、`pitch=10.4°`、`thr=0.77` 下达且 `submit_ok=true`，但 `speed=0.000`、位置逐字不变 ⇒ **从静止无法起步** ✓；**九次驱动侧修复全部否证** ✓；该坡**已铺装**（非地面分类问题 ✓）
- **C1（调机动参数）落地步骤**：`scripts/content/vehicles/*`（M26 动力/爬坡）或 `game_config.gd` 的低油门段 ⇒ **属机动参数，须授权** ✓
- **C2（限定范围地图）落地步骤**：`scripts/maps/village_definition.gd` 降低该坡脊局部坡度，或让重车路线避开 ✓；**须先说明影响区域与战术变化** ✓（先例：已授权的两处 C 类铺装 ✓）
- **C3（作为已知受限）落地步骤**：无需改码 ✓（D 电池判定表已按"受限"记录 ✓）
- **我的建议**：**C3**（或 C2 若您希望重车也能完成该坡道 ✓）

## D. 授权（E1–E4）
| # | 事项 | 落地步骤（授权后） |
|---|---|---|
| E1 | `export_presets.cfg` server/client 预设 | 属**构建/发布流程** ⇒ 授权后我按现有模板添加并实跑导出 ✓ |
| E2 | 96 车全量适配产物（≈70 MB） | 复用 `tests/export_model_binding_adapter.gd`（**含位置自校验** ✓）批量产出 + 逐车哈希清单 ✓ |
| E3 | 资产接入/登记 + **许可依据分记** | 需您给出**授权口径**（路径接入许可 vs 素材许可 ✓） |
| E4 | 真人验收（42/42）· 性能采样 | 需真人与授权（现 `HOLD_BY_USER`，**零采样** ✓） |

## 附：资产 ⑤（已按裁定 **B** 推进 ✓）
- 已完成：适配产物（**位置断言 delta=0** ✓）· draft layout（部件/父子/关节/开口 ✓）· draft `source_record`+`geometry`+binding ✓
- 待作者（每车 6 项 ✓）：装甲厚度/材料/分组 · 内构体积/完整度 · 乘员角色/位置 · **关节限位** · **桶轴权威定义** · 全部证据标识 ⇒ 补齐并签收后即可跑 `check_scene` ✓
