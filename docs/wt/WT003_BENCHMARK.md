# WT-003 当前源码完整局性能采样

生产源码727b78afa617e6f482b3ba5149a588b26fdcddc5，测试引擎SHA256为C8F0A6BC45A19B33541501E57F6F7CD972AB18453743266339D495CBBE846643。本批仅修改诊断脚本，不改游戏规则。run_wt_benchmark.ps1记录源提交、引擎/诊断脚本哈希、命令、CPU、RAM和容量档；游戏报告补实际分辨率、GPU/驱动、渲染器、画质、视距、车型、时间倍率与物理频率。

采样用1920×1080、gl_compatibility、渲染上限60、VSync关闭、历史车型四车轮换、8辆AI和观察镜位。观察相机远裁剪明确设为当前玩家车辆的2620米；这是观察镜位的8AI场景，不是人工驾驶镜位、16/32车容量或所有画质组合。节点/对象/静态内存按原采样点记录，外部5秒采集实际Godot运行子进程的PrivateMemorySize64和WorkingSet64，不能把console包装进程内存当引擎内存。

保留每个绘制帧的原始间隔和15秒后的独立稳态子集，不丢弃初期帧；场景装配耗时单列scene_setup_ms，不冒称装配过程包含在绘制帧统计中。完整局自然推进到结束，900秒墙钟上限仅防无限运行，超时仍标不完整。每15秒写PROGRESS.json并打印实际比赛时间，终局写帧数据/负载覆盖/截图。BENCHMARK_COMPLETE和RESULTS.passed表示采样完成且无错误，不表示60FPS或p95/p99目标达标。

## 分辨率校验

预检发现普通窗口和仅CLI borderless下实际客户区均为1920×1061。125126与125325的短报告虽有旧版passed=true，也不是1080p证据；125209、125422两次刚启动的同配置完整采样已主动停止并记录失败。数据原样保留，不能计作完整局。

125515预检采用显式Window.MODE_FULLSCREEN并验证root.size，实测1920×1080。当前显示设备含Parsec/虚拟显示适配器，实际渲染GPU为RTX 4070 SUPER、CPU为i5-13400；不能把该显示链路下的帧间隔等同于所有本地显示器结果。脚本现在遇到实际尺寸不符直接失败，且在结果端再次验证尺寸、车型与完整比赛状态。

## 村落完整局结果

目录：`logs/wt003-benchmark/727b78afa617e6f482b3ba5149a588b26fdcddc5/village-20260911-125543`。

调用：`tests/run_wt_benchmark.ps1 -Map village`，seed=19001。实际退出0，自然票池终局；413.594秒墙钟、401.650秒比赛时间，共13425个绘制帧。`summarize_wt_benchmark.ps1 -Folder <目录>`从原始帧重算并核对计数、均值和分位数，SUMMARY.json记录完整局与性能目标的独立结论。

| 指标 | 实测 |
|---|---|
| 平均帧率 | 32.459 FPS |
| p95 / p99 | 98.636 / 168.155 ms |
| 最慢帧 | 258.927 ms |
| p95≤20 / p99≤33.3 ms | 均未达标 |
| 引擎私有内存 / 工作集峰值 | 536354816 / 362590208 bytes |
| 节点峰值 | 1027 |
| 采样物理 / process 峰值 | 104.434 / 20.842 ms |

155枚弹丸结束，四种历史车型弹种均实际出现，残骸峰值4；音频WASAPI有8802次接收、峰值30声部。建筑受击/倒塌为0，明确未覆盖。已目视1920×1080终局截图，结算面板可读；这是AI观察场景，不代替真人驾驶和操作验收，观察镜位也不保证整局固定不动。

该批旧runner同时采集了Godot和conhost子进程。运行时取得的PROCESS_ROLES.json保留PID身份；汇总从156条原始记录中仅选78条Godot记录，原始数据不改写。runner已修正为只采集Godot子进程。没有身份文件时汇总内存标为不可验证，不以0假装测量结果。

5秒1080p预检125515的汇总退出1、full_match_verified=false，证明短采样不能被当作完整局通过。旧RC2性能与本批源码、车型、视距及场景不同，不能直接计算优化或退化百分比。当前帧间隔和物理监视值只提示调查方向，不能归因到具体查询或渲染实现。

## 工业区与后续归因

工业区基线目录`logs/wt003-benchmark/ad5dc771ecc064f4e85ab46eef521fcf3ac71fd4/industrial-20260911-130600`，实际退出0。ad5dc77相对727b78a只增加诊断，游戏生产代码一致；实际加载诊断脚本哈希另记在BENCHMARK_PROFILE.json。278.383秒比赛、282.852秒墙钟自然票池终局，12882帧，平均45.543 FPS，p95 60.982ms、p99 110.398ms、最慢230.105ms，两项性能目标仍未达标。私有内存峰值538898432 bytes、工作集360042496 bytes，53条Godot采样均有进程身份；节点峰值1053。已目视终局截图，胜利、票池与结果面板正常显示。两张地图仅完成单seed八车基线，16与32车档尚未运行。

后续可用`-QueryMetrics`单独运行查询归因：记录ShotQueryService累计次数/耗时与带墙钟、帧索引的采样点。此计时不包含快照构建、物理服务器或渲染，不能当作整个战斗CPU耗时；启用状态写入报告，与本批未启用的完整局基线分开。当前未证明WT-003或完整游戏目标完成。

新增开关实际窗口验证：`-Map village -Probe -ProbeSeconds 45 -QueryMetrics`，证据`ad5dc771ecc064f4e85ab46eef521fcf3ac71fd4/village-20260911-131137`，退出0、stderr为空，2564次查询累计1913.999ms，末采样帧2670/墙钟44.998秒与报告累计值一致。此前131058的5秒检查启用计时但实际0次查询，runner按覆盖不足返回1，失败数据保留。45秒是开关验证，不能替代卡顿后半局归因或完整局性能达标；生产战斗规则本批未变。
