# WT-030 车辆包驾驶、光学与机构配置

2026-09-12，以 1c36486e 为基线。本次把明确编写的 DriveProfile、OpticsProfile、FireControlProfile 接入实际车辆包，不再只能依靠旧车型 ID 选择预设。旧包未提供新配置时维持原有行为。

每项配置采用 `{schema_version:1, origin:"game_rule", note, values}`。values 必须逐项列出对应资源的全部导出字段，禁止缺字段后默用默认、任意资源路径、非有限数、数字代替布尔、非整数挡数或错误数组形状。单位沿用资源定义（米、秒、角度、速度、角加速度），FOV 为垂直角度，镜位为对应车体或炮管坐标系内的米。

实际配置必须逐值对应 facts 中的 `drive.profile`、`optics.profile`、`fire_control.profile`，保持 estimated/game_rule、structured 单位和独立来源。稳定器还需 `equipment.stabilizer={mode,module_ids}` 与实际模块一致；启用垂直或双轴能力必须配置可受损的 stabilizer 几何。数据校验成功后，实际 Actor 的驾驶、相机、权威测距装定与机构读取同一份配置。不同准入实例解码为独立资源。

苏德草案位于 `authoring/reference_data/modern_equipment/`。各自绑定原始候选 JSON 的 SHA256，47 个字段均已显式登记。T-80B 采用 20/8 度，豹2A4采用18/6度炮镜；0.8/0.6秒测量、50米装定步进、双轴稳定和原地转向差异均为独立游戏设计草案，不是史料性能，也不是摘要证明已安装的装备。原始候选、未知值、外部模型和正式车型名册均未改动。禁止由 IR 推断热像、补弹时间推断射击装填、hitPower 推断穿深。

**工程验证**

- `1c36486e-final5/20260912-210431`：74项装备包专项通过，含真实地面驾驶、实际镜位/两档FOV、原地转向差异、300米几何目标测距及应用、稳定器损坏/重置和正常权威命令发射。
- `1c36486e-wip2/20260912-205931`：105项参考准入、59项模型绑定通过。
- `1c36486e-wip3/20260912-210114`：19动力、24光学、34机构、62装填回归通过。
- 实际窗口 `wip-uncommitted/window-20260912-210452`：86项通过，四张截图已目视。20→8及18→6度FOV的实际目标画面放大、炮镜刻度与本车隐藏正确。

上述路径均在 `logs/WT030-equipment/` 下；截图在 `docs/evidence/WT030-equipment/`。测试将两套调参装在明确标注 TEST ONLY 的旧车测试外壳上，稳定器失效场景使用显式状态注入，不冒充实弹战损。未据此签收现代战斗几何、历史装备、真人体验或完整样车。

首次无窗口用例关闭了车辆呈现却访问相机，产生 SCRIPT ERROR，被总检查正确判失败；修正用例建立实际相机后通过。首次窗口用例没有在每个物理步保持炮镜输入，退回第三人称、一次测距中断；改为通过控制器逐步轮询持续观察意图后通过。失败日志和图片保留，未修改运行时代码绕过检查。

MCP 短只读任务确实成功。此次限定数据审查在120秒后超时；写入任务因 `windows sandbox failed: helper_unknown_error: setup refresh had errors` 未创建任何文件。本次代码、配置和验证由主集成者完成，不记外部实施或独立审查通过。

**继续项**

优先编写两车实际战斗几何、乘员/弹架/装备模块与完整弹药包，再以交付模型绑定验收。T-80B外部状态仍写明制作中；豹2A4几何PASS不等于模型交付。42项范围保留；WT-016优先T-80B/豹2A4，旧车用于基础回归；性能HOLD、真人NOT_RUN、整版未验收。
