# 015：完整1对1交战与结算
implementation=implemented；engineering=passed_self_review；human=not_run。
分支work/015-duel-match；源码f5df5bd88e83cd4755d1212c235428fde48da834；基线014登记a28d9912984e01722b557564a0ef915331aea3e4。版本0.1.5。

车库“1 对 1 歼灭”→3秒倒计时→正式交战。灰盒区有中央箱区、两侧通路和另外两处掩体。双方同M4外形工程车、AP120、30发弹药、相同模块/维修/火灾规则；厚度与耐久依然是设计夹具，不是历史认证。

MatchDirector是胜负与本局事件的唯一管理者，优先级300在车辆与弹丸之后统一决胜。死亡按life/generation只收一次，同一物理步双方出局判平局；600秒仍未分胜负也平局。主动返回登记abandoned。倒计时和结算不执行车辆命令，结束后立即关闭弹丸接收、取消在途弹、冻结车辆与火灾权威时钟；迟到回调不能改变结果。AppFlow.restart_match完整释放旧场景，使用新的车辆life进入下一局。

正式模式关闭R免费重置、X内构和实验室切车/难度快捷键。实弹回放仅结算后可看。结果面板显示实际发射、车辆接触、未穿、有效模块/乘员损伤及出局原因，不使用整车血条。再次命中已坏部件不累计“有效损伤”。己方起火、动力/火炮失能、维修/灭火动作有明确中文提示；火焰与残骸颜色来自真实状态。

AI驱动失能时仍可通过相同命令停车维修，并在炮闩和炮手正常时反击。开火后在可见外表车体/炮塔左右区域轮换，避免永远瞄一个点；不查询敌方模块或乘员。相应014等价射击测试改为比较冻结的实际发射方向，避免误用AI已经选择的下一次瞄点。

验证全部对应上述源码：
- `tests/run_suite_checks.ps1 -Order 015`：13套1,073PASS，216/123/140/144/81/57/64/67/55/25/40/34/27；全部exit0，无非预期ERROR/SCRIPT ERROR。日志`logs/015/f5df5bd88e83cd4755d1212c235428fde48da834/20260908-190059/`。
- 27项对局检查：真实3秒倒计时、相同配置、出生遮挡、禁止R、陈旧life统计拒绝、无效重复损傷不计、损伤状态驱动胜败/同帧平局/超时、一次结算、结束迟到弹火、动力失能AI实际反击、结束后真实回放、20次AppFlow开退节点/回调数量稳定与重开新身份。
- `tests/run_window_checks.ps1 -Order 015 -Script run_duel_player_checks -TimeoutSeconds 210`：15PASS、7张真实帧，exit0，目录`window-20260908-190408`。从正常车库点击，右键炮镜+鼠标有限转炮射击，W/A/D绕行，第三发侧面实弹造成殉爆胜利；点击再来一局后持续暴露，由电脑实弹击停后火灾造成玩家出局，返回车库正常。窗口脚本读取场景固定瞄点驱动鼠标，未写车辆位置/损伤/冷却，也未直接调用胜利函数。这是自动工程流程，不能冒充真人体验。
- 目视了胜利及最终战败结果图，原因、统计、重试/返回控件清晰；长摘要可滚动。末尾`Engine.get_frames_per_second()`快照为23，尚未采样平均/p95/p99；不能声称达到60FPS。该项记录在GATE_B，后续033独立性能检查处理。
- `tests/package_candidate.ps1 -Order 015`：独立导入与默认启动exit0，本机候选`backups/candidates/015/f5df5bd88e83cd4755d1212c235428fde48da834/PixelArmor/START_GAME.bat`。未发布、未合并main。

发现并修复：最初close_round复用整场重置的cancel_all，意外清除了结算回放；改为永久关闭发射后仅取消活动弹，保留冻结记录，专项复现通过。旧AI只要动力失能就提前返回，导致有火炮也不反击，已修复。早期窗口脚本向仍在滑行的敌车旧坐标射击或被第二掩体挡住、也有长时间侧面暴露被打坏炮闩后失败的运行；保留原日志。最终采用实时跟踪瞄点的正常炮镜输入与明确侧面路线，仍遵守真实装填/碰撞/毁伤规则。

边界：没有AI战术质量或真人手感认可；少量继承的训练HUD英文将在017/027收口。工程出口见GATE_B，下一步016队伍/票数/占点/安全重生，人工状态保持NOT_RUN。
