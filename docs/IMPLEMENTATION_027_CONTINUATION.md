# 027 中文与主流程接续

2026-09-10。从 `work/stab-dev` 的 `12a7d2810240a631cc2094324ee5f6c5c5033ae3` 建立 `codex/mainline-027-continuation` 工作树，目录 `E:/AIprogram/mcthunder-mainline`。

用户提供的旧任务“核对工作单006代码并复跑测试”确认原开发在 027 本地化与键盘焦点阶段中断。对 `mcthunder-development` 的 55 文件未提交差异执行三方接续，原工作树不变。唯一冲突是 `ProfileStore.commit` 的存档锁入口，保留稳定分支的 `_acquire_lock` / `_release_lock`，不恢复旧的直接建目录逻辑。

本次实现范围：

- 集中中文文本与当前按键提示，保留内部 ID、统计、弹道与装甲规则。
- 完成车库、设置、挑战、暂停、训练结果弹窗的焦点进入、Tab 循环与 Esc 返回；滚动容器跟随键盘焦点。
- 车库可直接调整声音、字幕、特效、字号与输入，继续使用原设置持久化服务；大字号覆盖 CoreUI 创建的文字。
- 挑战说明放入可滚动区域，开始与返回按钮固定在说明外。
- 主入口显示实际版本，提供制作/字体及引擎许可、可取消的退出确认。待结算成绩保存失败时保留车库重试入口。
- 版本 0.2.7。使用真实 Godot 窗口输入验证训练与八车战斗返回流程，另检验独立 PCK 的中文资源。

不将 027 窗口检查等同于整个 030 或 036 完成。完整比赛/结算/重开和地图切换压力、029 异常矩阵、性能整局采样及真人验收仍需各自证据。

字体已核实为未经改动的 Noto Sans CJK SC，SHA256 与 `ASSETS_011.md` 一致；OFL 完整文本在资源目录及游戏许可入口可读。旧稳定化记录把字体来源列为待核实，本次有文件与运行字形证据可关闭这一项。
