# 033 实机长测交付

运行源码 `b90511496fe84195ee8421bd3b4c6bfce2768b7a`，最终 `1.0.0-rc.2` 独立包。完整命令、硬件、负载与限制见 PERFORMANCE_REPORT.md。

最终Release实际运行1876.26秒，包含1800秒主要渲染测量和20次创建/退出/释放，67项检查通过，退出0。第5—20次释放始终397节点/3411对象/157资源/0孤儿节点；实际Windows私有内存峰值513.32MiB，工作集峰值355.32MiB，后段中位数没有单调增长。原始两秒采样与解释均保留，不把静态内存监视器的0值当作内存证据。

平均51.86FPS，p95 52.91ms、p99 103.77ms，最大尖峰2653.72ms。**60FPS目标未达成，村庄卡顿保留为PERF-RC2-01/P2**，因此工程状态为 `passed_stability_with_performance_gap`。低画质没有被用于改变命中、伤害或时间步。

已有查询缓存优化复用部件逆变换与精确顶点值包络，容量512项；几何变化、缓存淘汰和完整命中事件等价性有真实反例。测试修正冻结显式PAUSABLE子节点后，优化前/后各18项通过；最终干净构建的1536发与黄金JSON也严格一致。诊断写盘改为10槽/每槽2MiB，4项轮转/超限保留检查通过。

RC1长测错误假设末条有效终止记录一定可以播放，失败后保留STOPPED.json与部分测量。RC2的选择器保留未命中/不完整记录作为证据，播放时选择具有完整目标几何的记录。真实未命中与不完整记录新增6项反例，回放检查共73项；RC2先有300秒/2周期/14项预检，之后本次最终包独立长测通过，不能混用预检帧率。

最终证据：`logs/033/b90511496fe84195ee8421bd3b4c6bfce2768b7a/20260910-185638/`。分析器 `authoring/analyze_performance.py` 已从这些实际JSON生成 `docs/evidence/033/rc2/measurements.md` 和 `summary.json`，保存输入哈希、分组帧时间、内存区间、场景和回放选择记录。两张PNG为实际渲染抓帧。

033稳定性工程条件通过，随后034最终玩家流程80项也已通过；卡顿优化和真人常用分辨率体验仍有保留。没有新建公开发布，也没有把本机结果外推为其它硬件表现。
