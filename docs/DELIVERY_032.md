# 032 平衡筛查与规则冻结交付

测量工具提交 `29fd4ae144dbb656fcc4361ecbb4450931c90389`，最终候选规则源码 `9998f4483637ad5d0b73ca23060e235b46f908c2`。未修改历史装甲/装填/穿深以制造平衡结果；数值来源、未知和游戏规则保持原字段边界。

`run_balance_matrix_checks.gd` 调用真实发射、命中、穿甲与毁伤逻辑，覆盖四车互射、两弹种、三距离、四方向、两高度与两偏移，共 1536 发、768 组配对。使用零重力水平入射夹具，测的是指定几何入射结果，不是实战命中率。14 项检查通过；全部有真实装甲接触，962 发产生毁伤，AP/APHE 分别在 90/140 个配对点造成更多乘员伤亡。

`run_balance_match_checks.gd` 运行两图、两种子及交换 AI 随机队列的八局自然比赛，32 项通过，每局清理后节点 1、孤儿 0、资源 160。票数/阵亡/占领正常产生；两图各四局的小样本不能证明统计公平。完整结果与案例见 BALANCE_REPORT.md，CSV 见 `docs/evidence/032/matrix/`。

命令、退出码、原始 JSON 与日志在 `logs/032/29fd4ae144dbb656fcc4361ecbb4450931c90389/{matrix,swapped-matches}/`。自然比赛启动于提交前，其脚本随后原样提交；不冒充最终 RC 同源码全量测试。最终 RC 干净构建重新通过 14 项矩阵与两图正常回归，见 DELIVERY_031.md。

查询缓存优化前后的完整 1536 发 JSON 逐字节相同，SHA256 `AC6FABD294E72B45E998677EB7F96BB731012F78D0C306389CCC082532592FCA`，证据 `logs/033/36abe97/golden-matrix/`。本次冻结 15 个关键规则/内容文件的确切哈希于 RULESET_1_0_RC.json，后续调参需新规则版本及回归，不改写本次记录。

最终追加修正与复核：旧夹具未冻结显式 PAUSABLE 子节点，跨运行入射角会有小幅漂移。测试提交 `666aea98dff5be17cc482b190661a201d1c0156b` 冻结全部子节点并增加四车姿态断言。优化前/后各 18/18 通过，完整 JSON 严格一致，当前黄金 SHA256 `3B90E05682868BDD67D0CDA8C680AD74BC1C4EC094D2BAB6EAE54A9A86A09940`。CSV 及报告以 `docs/evidence/032/fixed-pose/` 为准；原始记录保留，详情见 BALANCE_REPORT.md。此提交仅改测试，不改变 9998f44 独立运行包或历史数值。

032 工程筛查通过；四车定位、解锁节奏与三星难度的真人接受仍 PENDING。后续性能与最终用户路径单独验收。
