# 范围决策与预留

已决策：Windows离线单机；Godot/GDScript/Compatibility；低模而非体素；四车两图一靶场；双弹种；单据点4对4；3挑战；无整车总血量；无局中保存。
无需再向零基础用户反复询问引擎、语言或是否先做手机。

不确定但已有默认：参数是否好玩、具体地图尺寸、AI误差、音效来源、最低配置。按计划的初值实现，靠测试/试玩修改，不将初值写成现实事实。

后续扩展池（没有1.0实施授权）：朋友联机→网络状态/预测/重放另设技术试验；更多车型/地图→沿用配置；体素破坏/飞机/海战/步兵→独立范围评估。
不以“将来可能联网”为由现在搭建空服务器、账号、反作弊或云端服务。

可缩减顺序：装饰细节/镜头花样/粒子 → 经用户同意减少挑战数量 → 经用户同意减少地图或车型。
不可暗中缩减：真实穿甲链、模块功能、回放一致性、稳定性、基本教学与完整开局结算、可运行交付。

技术债登记格式：现状、为什么暂留、影响范围、回归保护、计划归属、触发修复条件。
任何“临时假实现”必须标清，凡位于首发核心路径的假伤害/假回放/假AI均不得带入候选。

---

## RC-001 需求变更冲突记录（2025-06，与 003 一并交付）

来源：GPT 工作单 RC-001（latest-work-order.md）。变更总要求：正式成品必须有完整 UI、
完整陆战流程；正式载具采用有公开史料依据的具体历史改型，装甲/内构/武器/弹药
不得使用随意编造的统一模板；以战争雷霆陆战为机制对照，画风为原创低模方块风。

### 被覆盖的旧方案条款
| 旧条款 | 状态 | 说明 |
| --- | --- | --- |
| 四款架空正式车型 | 覆盖 | 不再作为正式成品标准；可留作程序测试数据 |
| 正式性能全部使用原创调参值 | 覆盖 | 正式车型参数须有史料依据；游戏化参数明确标为游戏规则 |
| UI 与完整流程全部拖到 025 以后 | 覆盖 | 已有系统必须同期提供可用 UI 与真实反馈；025—030 负责统一完善 |
| APHE 固定按比例设穿深 | 覆盖 | 不再作为正式历史车型数据依据 |

### 003 范围内的新增要求（已随 003 交付）
- 最小车型身份入口：VehicleDefinition 增加 content_tier / source_refs / verification，
  WeaponDefinition / ShellDefinition 增加 source_refs / verification（字段级校验）。
- A/B 测试车明确 TEST ONLY：`configs/player_tank_vehicle.tres` 等
  content_tier="test"、source_refs 显式声明"TEST ONLY: development fixture,
  no historical basis"、verification="unknown"；HUD 控制文本带 [TEST ONLY]。
- 历史数据 / 战雷参考行为 / 本作游戏化规则分开记录（见 SOURCE_NOTES.md 约定）；
  没有来源标未知或估算，不编造史料和页码。

### 未授权（本消息不授权，仍需独立阶段）
完整科技树、联机、陆空海联合作战、直接搬运战雷素材或非公开数据。
003 完成后停止，等待审核；不自动开始 004。
