# 全部后续工作单汇编

002—036，共35张。只按当前授权的一张执行，不一次全部开发。


---

# 工作单 002：001 补验收、缺陷修整与基线冻结

阶段：A｜计划初始状态：未执行｜前置：001已有实现与交付资料即可进入；002负责补验收，不要求001事先已经签收。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

不另起炉灶，把已有靶场变成后续开发可依赖的基线；本单不是承认 001 已通过。

## 2. 实现步骤

1. 先记录当前分支、提交 SHA、未提交改动、Godot --version 与实际路径；读取根 AGENTS.md、001 原单、docs/DELIVERY_001.md、SELF_CHECK_001.log 和 tests/run_checks.gd。当前计划参考 main@29376e20e5675bd15747ac4bfd6b24382d6c3b15；实际仓库若已变化，以本地实际版本为准，并报告差异。

2. 先重跑现有导入、自动检查与有限帧启动，保留修复前日志。交付报告声称的 52 项通过不是本轮已验证事实；检查测试是否真的调用生产代码，是否能在隔离用例或临时副本中对受控错误正确失败；测试后还原，不破坏用户工作。不得只数 PASS 字符。

3. 核查碰撞、炮根到炮口遮挡、实际炮口命中、暂停恢复误开火、窗口失焦、重置状态与启动路径。对真实发现的问题先补复现测试，再做最小修复；没有复现的现象先登记，不凭猜测大改。

4. 重点补 1280×720、1920×1080 与窗口缩放的 HUD/菜单布局；验证屏幕后方瞄点、第三人称贴墙、炮镜切换和鼠标重新捕获。视觉问题必须有真实截图或明确待人工项，不得用像素颜色采样代替目视结论。

5. 保留现有游戏逻辑。把调参入口和调试显示开关整理清楚，提供当前 FPS、车速、炮塔角、装填状态的开发显示；不要在本单建设复杂设置系统。

6. 将 AGENTS.md 的“永远只做001”改成“按当前获批工作单执行”；保留隐私、禁止擅自换引擎、不得虚构测试等永久约束。列出分阶段解除的功能禁令，不让后续 AI 因过时说明拒绝开发，也不允许一次全部开发。

7. 生成基线验收表。Codex 只能写“实现完成待签收”，人工未确认的手感/画面不能勾选通过；修复后运行全部回归，保存可回退提交。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T002-01**：现有检查完整重跑；每个失败均有修复或明确阻塞，不得移除原有断言来换取全绿。

**T002-02**：从含空格路径启动；更换工程位置仍能定位引擎或给出可执行的缺失提示，不能依赖 E:\AIprogram。

**T002-03**：相机可见但炮管被挡、炮口进入墙体、贴墙开炮三个场景，墙后靶板命中计数均不增加。

**T002-04**：暂停前按住开火，恢复/点击继续不产生额外射击；失焦时驾驶与装填按暂停规则停止。

**T002-05**：连续重置 20 次，靶板计数、速度、冷却、鼠标状态回到定义值，无新增脚本错误。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U002-01**：用户亲自启动并完成前后行驶、原地转向、瞄准、开炮、暂停恢复、重置各一次。

**U002-02**：提交至少第三人称、炮镜、暂停菜单三个真实画面；逐项列出哪些尚未目视检查。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- docs/QA_BASELINE_002.md
- logs/002/ 或 docs/ 的真实日志
- 更新后的 AGENTS.md 和启动说明
- docs/DELIVERY_002.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不加敌人、穿甲、内构、其他车型或新地图；不将本单写成纯评审文档而不运行工程。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 003：多车辆基础、数据配置与控制接口

阶段：A｜计划初始状态：未执行｜前置：002已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

同场生成两辆相互独立的测试坦克，玩家和未来电脑通过相同入口控制车辆。

## 2. 实现步骤

1. 在现有 tank/gunner/turret_rig/camera_rig 基础上渐进拆分，不强制整库改名。先画出现有职责到计划职责的映射。

2. 定义 VehicleDefinition、WeaponDefinition、ShellDefinition 的首版数据结构；用可序列化资源保存不可变配置，保留配置校验。速度单位 m/s、装甲 mm、角度接口单位必须明确。

3. 定义 VehicleRuntimeState：实例 ID、队伍、运动、炮塔、冷却、模块运行状态；每个实例独立。禁止把可变耐久或装填进度放进共享车型 Resource。

4. 定义 VehicleCommand：前后输入、左右转向、世界瞄准点、开火请求；车辆统一验证冷却、损伤、暂停等限制。玩家输入转换为命令，不再从所有车辆脚本直接读取全局键盘。

5. 为实体生成、销毁和本地玩家控制权设置单一入口。靶场放一辆玩家车和一辆无控制测试车；重复生成/销毁时清理信号、相机与静态引用。

6. 将自身排除改为按实体 ID/碰撞对象集合处理。现有“所有坦克共用本车视觉层”的做法不能让炮镜隐藏全部敌车；只隐藏本地观察车必要部分，并恢复状态。

7. 实现 reset_vehicle 和 reset_range 的职责分离，避免重置本车顺带清空其他车运行数据。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T003-01**：两车引用同一车型配置：A 的冷却、速度、损伤占位状态改变，B 不变，资源定义不变。

**T003-02**：不受玩家控制的车收到零命令时，按 W 不会一起行驶；给它脚本命令后可独立运动。

**T003-03**：A 发射排除自身但可以命中 B；炮镜看得到 B，不能因共用 cull_mask 消失。

**T003-04**：生成和删除 20 辆测试实例，无残留活动相机、重复信号或已释放对象访问。

**T003-05**：配置缺 ID、非法速度、缺武器引用时校验失败并定位字段，不悄悄给默认值掩盖错误。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U003-01**：同一靶场看两辆车：只有玩家车随键盘移动，靶板与另一辆车均可看见。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 数据定义与校验器
- 控制接口及两车测试场景
- docs/ARCHITECTURE.md 首版
- 新增生产逻辑回归测试
- docs/DELIVERY_003.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不做通用 ECS、联网层、插件系统；不批量生产正式车型。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 004：装甲与内构布局工具、可见调试模型

阶段：A｜计划初始状态：未执行｜前置：003已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

先把“装甲在哪里、模块在哪里”做成看得见且可验证的数据，再写穿透结算。

## 2. 实现步骤

1. 为测试中型车定义车体/炮塔局部坐标、装甲面 ID、厚度、有限面片边界和外法线；车顶、底部、侧后面均要有定义。倾斜由真实面片方向表示，不用角度标签伪装。

2. 定义内部模块有向包围盒：发动机、左/右履带、炮闩、炮塔驱动、两处弹药架，以及车长/炮手/装填手/驾驶员。所有数值为游戏设计值，不标成历史数据。

3. 模块指定所属部件；炮塔旋转时炮闩、炮塔装甲和炮塔内乘员同步变化，车体模块不随炮塔转动。

4. 提供开发模式开关：线框装甲、厚度标签、模块盒、模块名称、局部坐标与选中高亮。正式战斗不得默认显示敌人 X 光。

5. 建立检查：唯一 ID、正厚度、尺寸非零、引用存在、明显越界、未声明的模块重叠；有意重叠必须白名单并说明。

6. 制作简单装甲样板与标准摆位；至少有 20/40/80 mm 测试板，0/30/60 度摆位和可旋转炮塔样车，供后续测试复用。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T004-01**：相同车型的两实例局部定义一致、世界变换不同；旋转车体后模块世界位置正确。

**T004-02**：炮塔转 90 度，炮闩与炮手跟随，而发动机与驾驶员不跟随。

**T004-03**：非法厚度、重复模块 ID、缺父部件明确失败；校验不得改写原始配置。

**T004-04**：对标准封闭样车从前后左右上下采样射线，不出现未声明的装甲缺口；炮管开口等例外必须注明。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U004-01**：打开调试模式，用户能直观看到发动机在车尾、炮闩在炮后、乘员不漂在车外。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 测试车型装甲与模块配置
- 开发查看器
- 装甲/模块数据校验测试
- 标准样板场景
- docs/DELIVERY_004.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

本单不实现击穿和伤害；不建逐体素世界，不做精密机械内构。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 005：统一命中查询与多层交点排序

阶段：A｜计划初始状态：未执行｜前置：004已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

任何一炮都能按距离先后得到正确的墙体、装甲、模块候选交点。

## 2. 实现步骤

1. 建立 ShotQueryService，统一处理物理世界遮挡、车辆装甲有限面片和内部有向盒交点。首版可扫描最多 8 辆车的小规模数据，先保证正确，不先做复杂空间树。

2. 世界实体查询在安全物理更新阶段进行；车辆粗碰撞体只用于驾驶/粗筛，不能抢先吞掉装甲面命中。碰撞层掩码和排除集合集中配置。

3. 将世界线段转换到各部件局部坐标检测，再把交点/法线转换回世界；记录 distance_m、vehicle_id、part_id、surface_id 或 module_id、进出点。

4. 把墙体、装甲和模块候选合并排序。模块只有经后续穿甲授权才能受伤；本服务只提供几何交点，不自行判伤。

5. 处理端点、恰在表面出发、从模块内部出发、相邻装甲接缝、共面交点和浮点容差；真实多层装甲保留，重叠建模错误不能被偷偷当作额外装甲。

6. 保留炮根到炮口遮挡检查；设每段迭代上限和去重规则。禁止用“命中过此坦克就整车永久排除”来规避重复碰撞。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T005-01**：同一线段依次经过前板、空舱、发动机、后板时，交点顺序与手算几何一致。

**T005-02**：墙在车前时，第一有效世界碰撞一定是墙；模块候选不触发伤害。

**T005-03**：旋转炮塔和车体后仍命中正确面 ID，不能只使用未变换 AABB。

**T005-04**：射线平行于面、起点位于盒内、击中接缝时不出现 NaN、无限循环或重复扣算。

**T005-05**：自身过滤只影响发射车；另一辆相同车型不能一并被过滤。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U005-01**：调试线按顺序标注墙、装甲、模块；用户转动车身后重试，位置和编号保持合理。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 统一查询服务
- 交点可视化
- 至少 10 个确定性几何用例
- 碰撞层和容差说明
- docs/DELIVERY_005.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不在 HUD、AI 和炮弹脚本各写一份独立命中规则。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 006：有限速度炮弹、重力与连续路径检测

阶段：B｜计划初始状态：未执行｜前置：005已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

将即时射线替换为实际飞行炮弹，同时保留已经验证的遮挡规则。

## 2. 实现步骤

1. 从真实炮口生成 ProjectileState，包含 shot_id、发射者、队伍、弹种、位置、速度、累计路程、生命期和随机种子；开火时扣弹和进入装填，只执行一次。

2. 使用固定物理步推进重力，逐步检测上一位置到新位置的整条线段；禁止只检查炮弹当前小球是否重叠。视觉弹道不能决定伤害。

3. 使用 005 查询服务；本单碰到第一处装甲或墙先停止并反馈，穿透留给 007。保留炮根—炮口遮挡，不能发射到墙后。

4. 配置首轮测试速度 300 m/s、重力 9.81 m/s²，寿命和距离上限集中设置。这是游戏调试弹，不是历史弹性能。

5. 暂停时不推进弹道和装填，重置/离开场景时回收炮弹；设置活动炮弹数量上限，达到上限不得造成无声命中或死循环。

6. 将生产弹道积分函数用于测试；轨迹线、示踪与后坐订阅事件。有限速度下瞄点先表示炮管直线方向，不伪称它能精确预测落点。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T006-01**：关闭碰撞后，初始水平飞行 0.5 秒的位移与抛体公式在约定误差内一致；测试明确固定步和容差。

**T006-02**：高速度一帧越过薄板仍检出命中；板后目标不被本单尚未开启的穿透击中。

**T006-03**：渲染限制为不同帧率，固定物理步下撞击位置在约定容差内一致，不承诺跨平台位级确定性。

**T006-04**：暂停 100 次、重置 20 次后，不残留飞弹，不额外扣弹/装填，不产生已释放对象错误。

**T006-05**：相机射线与炮口方向故意错开时，只按炮口弹道命中。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U006-01**：能观察近靶较快、远靶稍后中弹，远处弹道有下坠；贴墙仍不能穿墙开炮。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 有限速度炮弹系统
- 抛体及高速薄板测试
- 更新 HUD/操作说明
- 对旧即时射线测试的等价迁移记录
- docs/DELIVERY_006.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不通过保留隐藏即时命中再播放假弹道来冒充弹丸模拟。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 007：分区装甲、入射角、跳弹与剩余穿透

阶段：B｜计划初始状态：未执行｜前置：006已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

相同弹丸打不同厚度和角度产生可解释的未穿/击穿/跳弹结果。

## 2. 实现步骤

1. 实现可单测 ArmorResolver：按入射方向与法线计算夹角，先处理掠射和跳弹，再计算视线厚度 t/abs(dot)。这只是游戏近似，不宣称真实材料穿甲公式。

2. 读取 ShellDefinition 的距离—穿透曲线，分段插值，约束范围。统一“累计穿透消耗”而不是每遇到一块板恢复满穿深。

3. 击穿记录消耗和余量，继续通过空舱、下一块装甲或世界物体；首版无穿甲弹爆炸伤害，也不能越过未击穿面。

4. 先用简化的 75 度跳弹阈值、最多一次有效反弹，反弹方向和穿透折损由游戏配置控制；边界比较、残余不足、背面命中等必须有文档。

5. 防止刚穿过同一零厚度逻辑面被再命中；使用交点去重/容差和上限，不排除整辆敌车。

6. 在调试界面展示面 ID、厚度、夹角、等效厚度、入射可用穿深、剩余量和停止原因；正式 HUD 先只显示未穿/击穿/跳弹。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T007-01**：关闭距离衰减的测试弹 P=60 mm：40 mm 正入射击穿且剩20；80 mm 正入射不穿。

**T007-02**：同弹对40 mm、60度入射板：几何等效80 mm，不穿；夹角从装甲法线量起。

**T007-03**：测试弹 P=70，连续40+40 mm正入射：穿第一块后30，不能穿第二块；不得第二次恢复70。

**T007-04**：阈值上下边界均有黄金用例；近90度输入不出现除零或无限穿深。

**T007-05**：面后空舱中不凭空触发整车损伤；碰到地面/墙体按当前不可穿环境规则停止。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U007-01**：用户在标准靶场分别打薄板、厚板、斜板，能从反馈和调试数据解释差别。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 装甲结算器与穿深曲线
- 多层穿甲回归
- 规则说明及初始参数
- 标准靶场录屏/截图或待人工项
- docs/DELIVERY_007.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不加复合装甲、爆反、材料强度仿真、历史真实穿深宣传或隐藏总血量。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 008：车内命中、模块受损与乘员功能

阶段：B｜计划初始状态：未执行｜前置：007已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

击穿以后，打中哪里就影响哪里的功能，而不是统一扣整车生命。

## 2. 实现步骤

1. 仅对已穿过外装甲的弹丸，按车内路径顺序处理模块进入事件；同次直穿单一模块最多结算一次，非命中模块不受直击伤害。

2. 为发动机、左右履带、炮闩、炮塔驱动、弹药架和四名乘员维护独立运行状态；配置耐久与穿透消耗是游戏数值。履带作为外部模块可被直接命中。

3. 集中计算 VehicleCapabilities：可驱动/可转向/可开火/炮塔速度/装填倍率。车辆驱动和武器读取同一结果，HUD 只显示它。

4. 发动机损毁失去主动动力，车辆按减速规则停止，炮塔仍可使用；任一履带损毁首版停止主动移动；炮闩损毁不能射击；炮塔驱动损毁限制转速或禁止，选定规则写入 GAME_RULES。

5. 乘员失能不可普通维修复活；炮手/驾驶员无人时对应功能暂不可用；装填手失能导致装填变慢。少于2名可用乘员触发失去战斗能力。

6. 弹药架受损本单先记录状态，不提前伪造殉爆效果；009 接入真实终结规则。所有事件带 shot_id 便于后续回放。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T008-01**：击穿发动机：推进能力为0，若炮闩/炮手完好则仍能开炮。

**T008-02**：击中炮闩：枪口不会产生新弹，扣弹数不变化；引擎完好还能行驶。

**T008-03**：射线通过空舱、擦过盒外或被外甲挡住，乘员/模块状态不变。

**T008-04**：A 车发动机受损不影响 B；新生成相同车型恢复完整状态。

**T008-05**：剩2人仍不因人数直接判死，剩1人只发一次出局事件；连续命中不重复计数。

**T008-06**：炮塔转90度后，直线实际经过哪个乘员盒就影响谁，而不是固定车体方向。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U008-01**：分别打发动机、履带、炮闩并操作受损车辆，实际功能必须对应变化。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 模块伤害解析与功能状态
- 乘员/模块显示雏形
- 空舱和部件旋转回归测试
- docs/DELIVERY_008.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不加整车总血量，不让视觉碎片随机决定死亡，不设置已装但未联动的假模块。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 009：起火、殉爆、维修与乘员替补

阶段：B｜计划初始状态：未执行｜前置：008已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

把受损后的应对和最终出局规则补完整，同时堵住无限修复与重复死亡。

## 2. 实现步骤

1. 建立明确状态迁移：正常/受损/起火/失能/已摧毁；首版不将所有状态塞进互斥单枚举，允许引擎损毁与起火并存，但终结状态只能触发一次。

2. 按 GAME_RULES 的阈值或带种子规则处理发动机起火；火灾定期影响声明的关联模块，不能跨车辆随意伤人。保存起火来源用于击毁归属。

3. 弹药架必须绑定实际剩余弹数。空架不殉爆；非空且达到殉爆条件时直接终结本车，产生唯一死亡事件。装填/换弹/补给不得复制弹药。

4. F 灭火：2次可配置耗材，默认4秒动作；T 维修：驻车且未起火才推进，履带/炮闩/发动机按模块顺序维修至可用阈值，不能同时无限修复所有部件。

5. 默认维修基础时长12秒，灭火和维修的中断/重开是否保留进度写清；普通维修不能恢复失能乘员、已殉爆车辆或消耗的弹药。

6. 实现8秒乘员换位：有可用替补且不让同一人占两个岗位；默认优先炮手再驾驶员，装填手缺位仍保留惩罚。取消或重伤必须清理进行中的换位。

7. 残骸失去控制但保留合理遮挡；设残骸生命周期和数量预算，生成/清理不得重复计分。首版殉爆不伤害邻车，以免提前引入爆炸范围系统。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T009-01**：相同命中：空弹药架不殉爆，有弹且达到阈值殉爆；两次重复事件也只出局一次。

**T009-02**：移动/起火中不能推进维修；完成维修不复活成员、不补充弹药、不清空其他独立损伤。

**T009-03**：灭火次数从2减到0以后不可继续使用；动作取消不能返还已明确消耗的资源两次。

**T009-04**：换位后同一成员最多占一个岗位；替补失能、重置、死亡时计时器正常取消。

**T009-05**：火灾终结保留来源；无来源的环境失能不得记给最近任意玩家。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U009-01**：玩家体验断履带后停在掩体维修、起火后灭火、炮手换位和弹药殉爆四个情形。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 损伤后续状态机
- 维修/灭火/换位交互
- 状态边界及事件去重测试
- docs/DELIVERY_009.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不实现场外经济、购买维修费、无限灭火或普通维修复活乘员。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 010：真实命中记录与穿透回放

阶段：B｜计划初始状态：未执行｜前置：009已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

每一炮的穿透动画都来自刚刚发生的同一份结算记录。

## 2. 实现步骤

1. 定义版本化 ShotRecord：shot_id、发射者/目标实例、弹种、规则版本、命中时间、种子、车体与炮塔变换快照、按顺序的装甲/模块交点、状态前后值、停止原因。

2. 战斗解析输出不可变记录，效果、HUD 和回放只订阅；不允许回放再调用伤害函数或重新随机。

3. 为命中目标创建纯显示的剖面副本，按命中瞬间局部变换显示路线、装甲接触和受损模块；副本无物理碰撞、AI、伤害和全局输入。

4. 做右下角2—3秒回放，连续命中可以覆盖或有上限短队列；不暂停正式战斗，用户能关闭该窗口。

5. 缺失/非法记录时显示失败原因，不播放虚构击穿；未穿和跳弹也显示对应结果，不强行画进车内。

6. 只保留最近有限条记录，必要时导出单条 JSON 调试；禁止保存无限增长的逐帧战斗数据。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T010-01**：记录命中发动机但未命中乘员，回放只高亮发动机；模块前后状态与记录一致。

**T010-02**：记录后目标转动炮塔、被删除或重生，回放仍显示原命中瞬间布局。

**T010-03**：重复播放同一记录10次，实战模块、得分、弹药均不改变。

**T010-04**：关闭回放前后，同种子同场景的真实伤害结果一致。

**T010-05**：一次未穿命中，回放轨迹不能穿过外甲并演示乘员伤亡。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U010-01**：对照调试面板与右下角回放检查3种部位；回放不能挡住准星或造成误操作。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- ShotRecord结构及样例
- 回放显示场景
- 回放与结算一致性检查
- 单炮调试记录导出
- docs/DELIVERY_010.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不做全局录像编辑器，不重新模拟结算，不用随机动画假装内构。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 011：装甲靶场整合与核心体验闸门

阶段：B｜计划初始状态：未执行｜前置：010已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

形成第一份有项目辨识度的可玩演示：正面不穿、侧面击穿、部件失能、回放一致。

## 2. 实现步骤

1. 将003—010集成进一个有明确测试区的靶场：驾驶区、厚度板区、斜甲区、可恢复的样车区。旧基础靶板可以保留，功能不能混淆。

2. 提供选定样车重置、装甲/内构显示、发射参数查看和无限训练弹开关；全部只在训练环境生效。

3. 设计五个固定实验：正面防护、侧后弱点、发动机失能、炮闩失能、空弹药架；射击位置与初始状态固定，便于用户复现。

4. 加入最小可读反馈：命中/未穿/跳弹不同音效或提示、炮口和受击效果、模块状态；此阶段可以用原创合成占位资源，026再提升品质。

5. 输出“一炮解释卡”：为什么穿/未穿、实际打到哪、车辆为何还能打或为何出局。把不一致列为阻塞问题。

6. 运行全部回归，并做一轮完整 GUI 试玩。未拿到用户认可前只标记 v0.1 候选，不开始增加正式车型。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T011-01**：五个固定实验都调用正常射击入口；不能通过场景脚本直接设模块损毁来伪装。

**T011-02**：训练重置恢复目标、残骸、火灾、弹药和记录，但不会破坏设置。

**T011-03**：穿深和伤害测试全绿；一次射击没有多次结算、重复计数或跨目标污染。

**T011-04**：关闭所有调试可视化后仍能理解主要反馈，不依赖开发标签才能玩。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U011-01**：用户从正面打不穿后绕到侧面击停样车，再看到它仍能转炮塔；这是必须通过的主体验。

**U011-02**：用户能通过提示说清楚“为什么没打死”，否则先修反馈，不加新系统。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- v0.1 装甲靶场候选
- docs/GATE_A.md
- 五个标准实验说明及真实证据
- docs/DELIVERY_011.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

核心不成立时禁止进入大规模美术、车型或联机开发。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 012：坡地驾驶、障碍与损伤行驶整合

阶段：C｜计划初始状态：未执行｜前置：011已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

坦克能在真正战场的坡面和掩体旁稳定移动，不必模拟每节履带。

## 2. 实现步骤

1. 保留简化可控车辆运动，在测试场增加10/20/30度坡、低台阶、窄通道、墙角和凹坑。车体碰撞尺寸仍由车型定义控制。

2. 用地面探测控制贴地与视觉俯仰，物理车体朝向、视觉倾斜和炮塔瞄准坐标分开；避免视觉倾斜重复应用到装甲模块坐标。

3. 限制最大可爬坡角，默认28度；超过阈值不得贴墙上天。不同车型的坡度限制、前后速度、加减速集中配置。

4. 将发动机/履带状态通过统一能力接口作用于移动；断履带不是只播放动画。倒车和坡地停车有可预测规则。

5. 履带滚动/轮转只作视觉反馈；边界区使用可见障碍并提供只在彻底卡死时可用的重生入口，训练可重置，战斗重生必须按出局规则扣票。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T012-01**：能上下定义为可通行的10/20度坡；30度禁行坡不能无限攀爬。

**T012-02**：贴墙持续前进、转向各60秒，无穿墙、弹飞、NaN或越界。

**T012-03**：在坡地击毁发动机，主动推进停止但不瞬间传送；重置后恢复完整能力。

**T012-04**：车身俯仰后，炮管实际方向、装甲交点与显示模型仍一致。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U012-01**：从平地开上坡再倒车返回；炮镜、车体和炮塔不能互相甩动到无法瞄准。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 驾驶障碍测试场
- 坡度/损伤移动整合
- 碰撞与变换回归
- docs/DELIVERY_012.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不切换成未经验证的真实履带链或大型刚体悬挂系统。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 013：电脑驾驶、路点规划与脱困

阶段：C｜计划初始状态：未执行｜前置：012已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

让一辆电脑坦克通过和玩家相同的驱动系统抵达目的地。

## 2. 实现步骤

1. 先在战斗灰盒区设置有限路点图和可通行连接，可用A*选择路线；路段净宽至少覆盖车辆宽度加安全余量。首版不用自动适配所有任意地形。

2. 实现路径跟随：将下一路点变成油门/转向命令，考虑转弯角度、减速、倒车和车体占用空间；禁止逐帧直接修改位置把车移到路点。

3. 加入前方障碍和友车近距探测，停车让行或重算路线；先处理可靠单车，再测试两车会车。

4. 记录“想前进但实际位移不足”的计时，依次短倒车、改转角、换路；次数有上限，失败要记录而不是无限抖动。

5. 电脑车被击毁发动机或履带后遵守同样限制；测试日志记录目标、路点、卡住原因，调试可画路径但正式版本默认关闭。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T013-01**：固定5条路线含直线、直角弯、绕障和倒车脱困，均到达目标或明确判定不可达。

**T013-02**：通道宽度不足时拒绝路径，不把坦克挤穿障碍。

**T013-03**：两车相遇不会持续顶住超过设定脱困阈值；若失败日志能复现。

**T013-04**：引擎损毁后AI仍可决策，但不能绕过能力接口移动。

**T013-05**：测试中实际调用车辆驱动，不能用测试专用传送功能通过。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U013-01**：观察电脑绕过箱子开到标记点；被玩家挡住后会停车或换路，而不是穿过玩家。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 电脑驾驶控制器
- 路点图与路径可视化
- 5条路线测试和脱困记录
- docs/DELIVERY_013.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不接入大语言模型在线驾驶，不做联机，不允许作弊传送。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 014：电脑感知、瞄准与交战状态

阶段：C｜计划初始状态：未执行｜前置：013已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

电脑能发现可见敌人并公平地瞄准开火，暂不追求战术大师。

## 2. 实现步骤

1. 定义巡逻/前往目标、观察、交战、搜索最后位置、撤退/维修等有限状态。每个状态只能通过统一 VehicleCommand 控制车辆和武器。

2. 感知使用距离、视野和遮挡检测；记住最后可见位置和时间，不持续读取墙后敌车实时位置来精确跟踪。队伍情报共享暂不实现或明确限定。

3. 瞄准具有反应时间、误差、有限炮塔速度与装填；可对暴露的车体/炮塔几何区域选择目标，但不得读取未揭露的内部损伤来作弊。

4. 用发射前炮口方向和友车遮挡检查减少误射；实际弹丸仍走同一飞行和伤害链。命中预测不能替代真实开炮。

5. 定义简单/普通/困难三档配置，差异只在反应、误差和决策间隔；同车型速度、穿深、装填、维修和伤害规则一致。

6. 引入带种子的决策随机源，并输出状态切换原因；不承诺整个Godot物理跨平台位级确定性。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T014-01**：敌人持续在不透明墙后且从未被看见，电脑不能精确锁定或开火。

**T014-02**：看见敌人后躲到墙后，电脑可去最后位置搜索，但不能持续更新隐藏位置。

**T014-03**：炮塔未转到位、炮闩损毁、装填中、炮口被挡时，AI不能绕过限制开火。

**T014-04**：更改难度不会改变武器穿深、车辆最高速或模块耐久。

**T014-05**：AI火炮命中同一标准板的结果与玩家相同。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U014-01**：从掩体探头吸引电脑开火，再躲回去；它应搜索而不是隔墙持续精确追踪。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- AI交战状态机
- 感知与难度配置
- 公平性/遮挡/损伤测试
- docs/DELIVERY_014.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不让AI直接扣玩家模块耐久、不瞬间转炮、不靠加血加穿深提难度。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 015：一对一完整交战与第二道验收

阶段：C｜计划初始状态：未执行｜前置：014已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

玩家第一次能从开局打到胜负，再开始一局，而不是只能测试系统。

## 2. 实现步骤

1. 建立一张小型灰盒对战区，双方出生点不直接互射，有正面路线、绕侧路线和至少两处掩体。

2. 实现1对1歼灭：开局准备、进行中、胜利/失败/同时出局平局、结算、重新开始。临时流程可简洁，之后016复用为完整对局管理。

3. 玩家与敌人均使用相同车型、弹种和模块规则；检查电脑面对受损、起火、维修和脱困的实际表现。

4. 输出每局事件摘要：发射、命中、未穿、有效模块伤害、击毁原因；避免仅统计总伤害诱导整车血条逻辑。

5. 只修复会妨碍完成一局或解释伤害的问题；列出手感调参项，不一边验证一边再加三种模式。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T015-01**：玩家胜、玩家败、同时出局、主动返回与重新开始均有正常终态。

**T015-02**：连续启动/结束10局，不重复创建玩家、相机、HUD或信号回调。

**T015-03**：AI被打停仍可在炮闩完好时反击；已死亡坦克不继续发射。

**T015-04**：结算每车只记录一次死亡，回放开关不改变战斗结果。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U015-01**：用户完成至少一次正面对射和一次绕侧击杀；至少体验一次战败后正常重开。

**U015-02**：用户对驾驶、瞄准和击毁反馈给出“可继续/需返工”，未通过不进入4对4扩展。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- v0.1.5 单挑候选
- docs/GATE_B.md
- 对局生命周期测试
- docs/DELIVERY_015.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不要跳过用户试玩，直接把能运行的1对1宣称为好玩或已签收。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 016：队伍、占点、票数、重生与胜负

阶段：D｜计划初始状态：未执行｜前置：015已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

把单挑提升为一套完整且无歧义的4对4占点规则。

## 2. 实现步骤

1. 实施 GAME_RULES 的单据点模式：每队300票，击毁扣30票，占点后每秒扣敌方1票，上限600秒。参数为可调设计值，不是最终平衡结论。

2. 一名玩家+三名友军AI对四名敌军AI；Team/Match管理不依赖某辆坦克一直存在。状态为准备/进行/结算/退出，所有终态入口统一。

3. 据点半径12米，单方在圈内12秒完成中立占领；争夺时停止推进，多车不加速，敌占先中立化再占领。保护期车辆不参与占领。

4. 阵亡扣票后8秒等待重生，只在本队票数大于0且比赛仍进行时生成；出生位置必须安全可用，堵塞时选备选点或继续等待，不能叠在其他车内。

5. 3秒出生保护具有明显提示；移动可取消保护，开火先取消再执行，占点不允许利用保护。伤害和票数、弹药、起火、残骸均正确重置。

6. 正式战斗关闭R无代价重置。需要脱困重生时按一次出局处理；训练仍可重置。友军伤害默认关闭，但弹丸命中友车停止，不可穿友车打敌人。

7. 超时以剩余票数定胜负，同票平局；同一物理帧双方归零按平局。比赛结束后的在途弹/火灾不再改变结果或发奖励。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T016-01**：占点开始/中断/争夺/换手、保护期入圈和空圈各有计时测试。

**T016-02**：双方同帧归零、超时同票、玩家死亡瞬间结束等边界得到唯一确定结果。

**T016-03**：重复死亡信号只扣30票一次，重生只创建一个实例。

**T016-04**：比赛结束后炮弹命中、火灾tick或迟到回调不能继续扣票/发奖。

**T016-05**：友军挡在射线上时友军不受伤，后方敌人也不能被击中。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U016-01**：玩家阵亡后能看到倒计时、正确重生并继续比赛，比赛结束后不再刷出坦克。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 完整Match/Team/Spawn管理
- 单据点规则配置
- 时序与重复事件测试
- docs/DELIVERY_016.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不做三据点、多模式、联网匹配、局中换队或复杂增援经济。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 017：战斗界面、队伍信息与可读性

阶段：D｜计划初始状态：未执行｜前置：016已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

玩家无需查看开发控制台就知道当前目标、车况和局势。

## 2. 实现步骤

1. 战斗HUD显示真实炮管瞄点、装填、弹数/弹种、速度、乘员和模块状态、灭火剩余、维修进度、双方票数与剩余时间。不可显示整车血条。

2. 加入简洁小地图：地形轮廓、自己、友军、据点；敌人只按已定义感知/短时标记规则显示，不读取全场隐藏敌人实时位置。

3. 显示移动/射击受限的具体原因，如履带损毁、炮闩损毁、炮手换位、装填中，而不只显示“无法操作”。

4. 友敌识别同时用文字/图形和颜色，支持关闭击杀回放、镜头震动与闪光；开发X光不得混进正式战斗。

5. 加入Tab战况面板和阵亡/重生提示；按钮和面板打开不误开火，暂停时鼠标正确释放。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T017-01**：模块状态改变后HUD与实际能力一致，不从自己的重复计时器推断冷却。

**T017-02**：隐藏敌人不会因小地图直接暴露；已过期标记正确消失。

**T017-03**：1280×720及1920×1080主要元素不重叠；长文本使用容器布局而非固定坐标。

**T017-04**：Tab/暂停/回放开关不影响射击规则或残留鼠标捕获。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U017-01**：新玩家只看界面能说明自己为什么开不了炮、去哪占点、何时重生。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 完整战斗HUD
- 地图与情报显示规则
- 布局及输入回归
- docs/DELIVERY_017.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不照搬现成游戏图标/布局资产，不用颜色作为唯一状态指示。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 018：第一张战斗地图：丘陵村庄

阶段：D｜计划初始状态：未执行｜前置：017已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

制作一张支撑正面交战与绕侧的原创地图，而非随机堆掩体。

## 2. 实现步骤

1. 以约400×400米灰盒图为基础：中央据点、两条主进攻路线、一条较长侧翼路线，出生区用地形或实体遮挡隔开。尺寸是设计起点，可按驾驶速度调整。

2. 典型交火距离80—250米；设置可利用坡顶、低墙与村舍，不让所有交战都变成长走廊正面对射。

3. 给每种地物明确视觉与碰撞：建筑和石墙挡弹，首版薄围栏也可挡弹但外观必须清楚；草不挡弹，灌木感知规则统一给玩家与AI。

4. 完成路点图、出生点集合、维修/补给区域的预留；补给在022统一接入，不在地图脚本私自赠送弹药。

5. 检查四种未来车型所需的最大宽度与炮塔空间，狭窄区域提供绕行，避免地图只适配初始中型车。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T018-01**：两侧出生点之间没有直接主炮射界，据点到各方主路线的初始行驶时间差记录清楚。

**T018-02**：每侧至少两条独立到据点的可行路径，任一路被车堵住仍有替代路线。

**T018-03**：固定路径探测覆盖每个出生点到据点；最大车宽测试模型能通过声明可通行道路。

**T018-04**：地图外边界与视觉一致，无看似可走却无形阻挡的主要路线。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U018-01**：用户正面推进一局，再主动走侧翼一局，能感到不同战术选择。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- map_hill_village场景/布局数据
- 路点和出生点
- 地图设计说明与通行测试
- docs/DELIVERY_018.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不做开放世界、随机生成地图或逐块破坏地形。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 019：4对4整合、可玩性验收与早期导出探路

阶段：D｜计划初始状态：未执行｜前置：018已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

拿到一份小规模完整战斗版本，并尽早验证不是只能在编辑器里运行。

## 2. 实现步骤

1. 把8辆车、占点、模块、维修、AI、残骸和回放整合，确保运行预算与生命周期明确。

2. 建立固定种子比赛批测：至少10场AI对AI逻辑测试，记录卡住、结束原因、射击/命中和最大对象数；逻辑测试不替代渲染性能和人工手感。

3. 用户进行至少3场有玩家参与的完整比赛；区分“AI不会去目标”“炮弹判定错误”“反馈难读”三类，不把它们统一归为平衡问题。

4. 检查本机对应Godot版本的导出模板；已具备时做首个Windows测试包，在独立临时目录运行；缺模板仅报告所需最小官方安装步骤，不能伪造exe。正式发行流程留到031。

5. 整理阶段缺陷清单：所有崩溃、无法完成对局、隔墙伤害、状态污染属于阻塞。没有通过则回补原工作单，不用添加功能掩盖。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T019-01**：完整对局能按票数或时间自然结束；AI不应全体停在出生点或卡死等待。

**T019-02**：8辆车和多次重生不会错误绑定玩家相机、清空其他车辆或重复收到输入。

**T019-03**：连续10场重开后活动节点/弹丸/残骸不无限增长，错误日志无未解释异常。

**T019-04**：若已导出：从独立目录启动，资源、弹种和地图均存在；无模板时状态明确为“导出待验证”。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U019-01**：用户完成3场：至少一场死亡重生、一场维修、一场通过绕侧取得击毁。

**U019-02**：签收四对四核心玩法后，才进入多车型与第二张地图。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- v0.2 4对4候选
- docs/GATE_C.md
- 固定种子批测记录
- 早期导出结果或明确阻塞
- docs/DELIVERY_019.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不能把AI自打通过等同用户体验通过，也不能用开发电脑性能推断最低配置。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 020：四款正式车型与差异化验证

阶段：E｜计划初始状态：未执行｜前置：019已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

把一套可复用系统扩展成四辆有不同打法的原创坦克。

## 2. 实现步骤

1. 以已完成中型车为模板新增轻型侦察车、重型突击车、固定战斗室歼击车；共4款，而不是再新增4款。名称和外观原创、性能全部标为架空游戏值。

2. 按 VEHICLE_SEEDS.json 的初始方向配置：轻车机动好但薄甲，重车防护好但转向慢，歼击车前向火力好但射界窄，中型均衡；具体平衡留032实测。

3. 每车必须配齐外观、行驶碰撞、装甲面、真实内部布局、炮口/旋转轴、弹药架和配置校验，不允许只换颜色或整车血量。

4. 歼击车的主炮限制左右射界，超过射界由车体转向完成；玩家和AI均使用同一限制，不为它复制另一套射击系统。

5. 已有车型不应因新增数据而被硬编码判断车型名称。确需接口扩展时写迁移测试，旧车型行为保持或明确批准修改。

6. 每车执行同一套射击、模块、驾驶、炮镜与出生通行测试。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T020-01**：四车均从注册表载入并独立生成；缺配置时明确阻断而不退回错误车型。

**T020-02**：轻车速度高于中型、重车低于中型，且受相同单位/物理步控制。

**T020-03**：歼击车炮管不能转出指定射界，AI也不能越界发射。

**T020-04**：各车模块与炮塔父子变换匹配；死亡/重生不存在共享数据污染。

**T020-05**：新增三款车主要由场景和配置完成，不复制三个版本的伤害解析器。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U020-01**：用户各开一轮，能不用看名字说出轻、中、重、歼击车的主要差别。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 4款可玩车型资源
- 车辆参数表
- 车型全覆盖回归
- 模型/内构/碰撞对照图
- docs/DELIVERY_020.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不扩大到科技树几十辆车，不使用现实型号性能作为未经核实的宣传。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 021：第二弹种与装填、弹药架联动

阶段：E｜计划初始状态：未执行｜前置：020已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

加入有明确取舍的APHE游戏化弹种，而不是给普通穿甲弹换皮增伤。

## 2. 实现步骤

1. 保留AP稳定穿透、较少车内扩散的特征；APHE设为较低穿深、成功进入车内后按游戏化行程触发内部爆发。不是工程级引信或爆药仿真。

2. 只使用可配置的玩具规则：击穿合格表面后起算车内路程，触发时有限条内部碎片射线沿生产查询服务结算；对装甲/模块的消耗规则统一，禁止穿墙全车广播伤害。

3. 碎片方向由种子决定，每枚最多12条，每条有距离/迭代上限，不递归产生碎片；已终结车辆不重复死亡。

4. 按键1/2选弹。当前装填结束前切换只影响下一次装填，已在膛内弹丸不会瞬间变种；弹药计数和弹药架实际分配同步。

5. 把有限备弹接入所有正式车辆；训练无限弹是单独规则，不能泄漏到正式比赛。无弹时开火不生成弹丸，也不进入虚假装填。

6. 回放显示实际记录的引爆点与命中碎片，不能随机画满整辆坦克。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T021-01**：APHE未穿外甲时不产生车内碎片；击穿但达到行程前离开目标时按已定义规则处理，不凭空炸车。

**T021-02**：碎片遇到不可穿装甲停止，空舱不命中乘员；每发上限严格执行。

**T021-03**：切弹/装填/重生/补给循环不复制弹药，所有弹药架总数+膛内计数与车上总弹数一致。

**T021-04**：同记录重播碎片位置不变；显示开关不改变真实受损模块。

**T021-05**：普通AP和APHE在规定装甲/布局场景表现出取舍，不是一种全面更强。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U021-01**：用户选择两种弹击打同一厚甲和同一薄甲车，能观察到穿透能力与车内影响的取舍。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- AP/APHE数据与共同结算入口
- 选弹/弹药架/装填联动
- 碎片预算与弹药守恒测试
- docs/DELIVERY_021.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不加真实爆药参数、破片物理实体海、现代弹种或范围内无视装甲伤害。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 022：车库、装甲查看与战前配置

阶段：E｜计划初始状态：未执行｜前置：021已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

玩家能选车、检查弱点和选择备弹，再进入同一套战斗。

## 2. 实现步骤

1. 车库列出4款已解锁车辆，展示机动、防护、火力的真实配置摘要；不以未经实测的综合战力分数制造假精确。

2. 提供原创车辆旋转预览、装甲厚度模式和内构模式；预览使用无战斗行为的显示对象，避免车库也运行AI/装填/伤害。

3. 可选择AP/APHE备弹分配，总数不超过车型容量；训练可自由设置，正式战斗默认满备弹，也允许少带降低弹药架风险。

4. 将选车、弹种分配、地图和难度组成 MatchConfig，启动比赛时创建独立快照；预览调参不能实时改变正在运行的车辆。

5. 补给仅在自方指定补给区驻车满足条件后缓慢进行，按容量增加指定弹种并更新弹药架；敌方不能使用，自方火灾时不可补给。

6. 首版全部车辆直接可用，不加入银币维修费、氪金或长期科技树；挑战星级/战绩提供轻量目标。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T022-01**：备弹选择为零、超容量、负数或非法弹种时校验正确；不能通过读档绕过上限。

**T022-02**：空弹药架预览和正式车分配一致；改变备弹确实影响剩余可殉爆位置。

**T022-03**：车库选择B车后开局为B车，重生仍按既定配置，不意外退回初始中型。

**T022-04**：补给不超过容量、不在移动/起火/敌方区域生效，返回菜单不能复制补给。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U022-01**：用户不读代码，能选中重型车、查看侧甲和乘员位置、配弹、进入一局。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 车库与配置界面
- MatchConfig
- 补给规则联动
- 配弹边界测试
- docs/DELIVERY_022.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不做复杂经济、联网账户、商店或付费解锁。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 023：第二张战斗地图：工业边缘

阶段：E｜计划初始状态：未执行｜前置：022已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

新增不同交战节奏的地图，而不是第一张图换材质。

## 2. 实现步骤

1. 设计约350×450米工业边缘地图：仓库街道形成近中距离交火，外围道路支持绕侧，中间保留一个据点。与丘陵图使用相同模式规则。

2. 采用原创低模建筑、轨道/道路等可重复件；重要掩体完全阻弹，装饰物规则与第一张地图一致。

3. 提供重车可通行主路、轻车侧路和歼击车有反制路线的射界；任何固定火位都应有可接近的侧翼或遮挡。

4. 完成4车适配、电脑路点、备用出生点、补给区和小地图；地图配置从注册表加载，不写死到主场景。

5. 检查出生时间优势和长期堵点，必要时调整布局而不是偷偷给某队加强属性。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T023-01**：两张地图切换、重开及返回车库后均不残留上张地图的据点/车辆/路径。

**T023-02**：每种车型能从全部出生点抵达据点/补给区，失败点明确整改。

**T023-03**：从出生点无直接有效射界秒杀对方新生车辆。

**T023-04**：相同物体类别在两图的挡弹/挡车/感知规则一致。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U023-01**：两张地图各打一局，用户能感到丘陵图偏利用坡地、工业图偏街角和绕行。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 第二地图
- 地图注册与切换
- 4车通行和出生安全测试
- docs/DELIVERY_023.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不增加第三战斗地图，不引入全建筑可摧毁系统。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 024：三个固定挑战任务与轻量重玩目标

阶段：E｜计划初始状态：未执行｜前置：023已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

在自由对战之外，给单机玩家明确的练习目标和完成记录。

## 2. 实现步骤

1. 复用现有地图和敌人配置，不建战役系统。任务一“侧翼猎手”：指定中型车对付正面难穿的守卫，要求绕侧击毁。

2. 任务二“阵地坚守”：有限时间守住据点并击退有限波次；波次使用现有出生与AI，最大活动车辆数仍不超过预算。

3. 任务三“猎歼训练”：使用歼击车在有限弹药下完成目标；用射界和备弹的限制产生差异，而非提高敌车血量。

4. 每任务设置明确胜负、可重开和三星目标。星级按可核验条件如完成/用时/剩余车次评定，阈值配置化，不能靠看不见的评分。

5. 记录最佳星级与最佳成绩的数据入口；真正持久化在029接入，当前内存记录必须明确标注。

6. 脚本化任务只安排目标/波次/判定，不直接改模块损伤来强行演剧情。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T024-01**：每任务完成、失败、超时、主动退出与重开都有独立测试；结算事件只触发一次。

**T024-02**：守点任务波次不会无穷生成或超出活动车辆预算。

**T024-03**：三星规则边界明确，读同一结果多次不重复发星级或解锁。

**T024-04**：任务车与自由对战车使用相同穿甲/损伤/移动逻辑。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U024-01**：用户完成至少一项任务，能理解为什么拿到当前星级，以及怎样提高。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 3项任务配置
- 星级规则
- 任务生命周期测试
- 持久化待接入标记
- docs/DELIVERY_024.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不写长篇战役、过场动画、开放世界任务生成或特殊作弊伤害。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 025：低模方块美术统一与资产生产线

阶段：F｜计划初始状态：未执行｜前置：024已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

让四辆车和两张图看起来属于同一款游戏，且外观不破坏命中可信度。

## 2. 实现步骤

1. 制定小型美术规范：方块化但保留斜甲和车型轮廓；统一材质粗糙度、调色范围、细节尺寸、光照和地面比例。名称与标志使用原创表达。

2. 优先复用程序生成/基础几何模型；确需Blender则仅引入可重复的建模导出脚本，并写工具版本和执行方式，不让用户手工摆几百个部件。

3. 四车型分别检查车体、炮塔、炮管、履带、炮口、后坐部件的命名和轴；显示模型、行驶碰撞、装甲面与内构采用同一尺寸基准。

4. 做少量可复用场景件：房屋、仓库、墙、石堆、树和道路；重要轮廓清楚，避免靠高频碎纹理掩盖方块风格。

5. 记录每个素材的作者/来源/生成方法/许可状态/用途；没有可说明来源的现成游戏资产不得进入发行包。

6. 每类模型先做一件确认再批量替换，替换过程中保留原代理形体作为调试参照，不同时大改伤害数值。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T025-01**：替换外观后重新跑装甲表面覆盖和模块变换测试；不存在明显可见车体与判定错位。

**T025-02**：资源路径不含本地绝对路径；新克隆或清缓存后能重新导入。

**T025-03**：两图四车均无缺材质、丢贴图、倒法线导致主要面消失等问题。

**T025-04**：模型复杂度/材质数有记录，并在当前8车场景采样，不以单模型截图推断整场性能。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U025-01**：比较四车正侧轮廓，用户能辨别车型；在远中近距离都能识别敌车与掩体。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- ART_STYLE.md
- 四车正式低模外观
- 通用场景件
- 资产登记与导入流程
- docs/DELIVERY_025.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不提取战争雷霆、MC或Unturned的模型、音效、贴图；不追求逐螺丝精细度。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 026：音效、特效与重量感

阶段：F｜计划初始状态：未执行｜前置：025已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

让开车和交火有力度，同时确保反馈真实、可关闭且不遮住战斗。

## 2. 实现步骤

1. 补发动机怠速/加速、履带、炮塔、开炮、装填完成、命中未穿、跳弹、击穿、起火、殉爆等声音；使用可记录来源的资源或原创合成音。

2. 按实际车速和状态驱动发动机/履带，按距离衰减远处声音；设同时声音数量与优先级，避免8车场景削波和音源堆积。

3. 炮口闪光、后坐、烟尘、火花、火灾、残骸均订阅战斗事件；不能通过视觉粒子碰撞来另算伤害。

4. 镜头后坐与摇晃可调/可关；炮镜中保留清楚目标，不让持续烟尘完全挡住主要瞄准区域。

5. 区分击穿但未毁与真实击毁，禁止每次命中都播放毁车音。低画质可减少粒子，不改变命中和碎片射线预算。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T026-01**：关闭声音、粒子、回放或镜头摇晃，固定测试伤害与胜负保持一致。

**T026-02**：连续开火100次和车辆重生后，音源/特效节点数量返回定义上限内。

**T026-03**：暂停、失焦、离开比赛后长音循环正确停下，无菜单里仍有引擎声。

**T026-04**：8车交火同时音量不过载，关键警报不会被低优先级环境音完全淹没。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U026-01**：用户能不看调试文字分辨开炮、未穿、跳弹和击毁，并可关闭不舒服的摇晃。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 音效与特效包及登记
- 音频总线/优先级
- 效果独立性与回收测试
- docs/DELIVERY_026.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不让特效改变穿深、不用盗录其他游戏声音，不因画面热闹取消关键信息。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 027：中文界面、输入改绑与基础易用性

阶段：F｜计划初始状态：未执行｜前置：026已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

把开发界面升级成中文玩家能直接操作的产品界面。

## 2. 实现步骤

1. 收集全部玩家可见文本到统一文本资源：主菜单、HUD、车库、任务、教学、设置、错误与结算；首发简体中文为必需，英文可作为回退。

2. 核验可合法随游戏再分发的中文字库来源；需要下载时仅通过可信官方来源并记录许可，不从助手环境拷贝字体或打包系统字体。本工作计划不附字体。缺资源则明确阻塞，不能宣称中文完成。

3. 使用主题和容器统一字号/间距/按钮，支持至少1280×720与1920×1080，提供UI缩放或可读字号；长中文和英文回退不溢出。

4. 做键鼠按键改绑：冲突提示、取消、恢复默认；保留可退出界面的固定后备操作，不能把所有返回键改没。图标/教学提示读取实际映射。

5. 支持鼠标灵敏度、反转Y轴、镜头震动关闭、回放关闭、色彩之外的友敌符号；音量不只一个总开关。

6. 正式比赛和训练中的R含义不混淆；按键映射按上下文注册，菜单打开时不转炮/不开火。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T027-01**：全部必要中文字符在真实运行和独立导出中显示，不以字体文件存在代替显示检查。

**T027-02**：改绑开火键后原键失效、新键生效；冲突/取消/恢复默认均可用。

**T027-03**：极长车辆/任务名称、中文错误提示和高DPI窗口下不遮挡主要按钮。

**T027-04**：仅靠键盘和鼠标可以进入、退出每个界面，不出现焦点陷阱。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U027-01**：用户用中文界面完成选车、查看按键、改灵敏度、开局和退出，不借助开发者说明。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 完整简体中文文本
- 统一UI主题
- 改绑和辅助选项
- 字库来源/许可记录与实测清单
- docs/DELIVERY_027.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不把英文占位永久当作中文完成，不分发来源不明或系统自带字体。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 028：交互教学与失败提示

阶段：F｜计划初始状态：未执行｜前置：027已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

让首次打开游戏的人能够理解驾驶、穿甲和模块损伤，而不是阅读一大段说明。

## 2. 实现步骤

1. 在既有靶场实现可重玩的分步教学：开车到点→转炮/炮镜→命中薄靶→换角度攻击→击毁指定模块→维修/灭火→进入占点。

2. 教学步骤订阅实际移动、射击、伤害和占领事件；不能玩家随便点“下一步”就伪装学会。允许跳过并在菜单重进。

3. 每步只显示当前必要动作，按实际改绑显示键位；长时间未进展时给具体提示，不羞辱或自动替玩家完成。

4. 用真实结果解释“没打穿”“发动机坏了但炮还能打”“成员不足出局”；训练无限弹、重置和X光只在训练上下文生效。

5. 教学检查点只保存到章节级，重进时恢复该步骤的预设场景；首版不保存任意时刻的炮弹/物理状态。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T028-01**：按错误部位射击不误完成指定模块步骤；未击穿不能算作击穿教学成功。

**T028-02**：跳过、重开、死亡、失焦和返回菜单不锁死教程进度。

**T028-03**：更改按键后提示即时更新；无弹/损伤状态下有正确补救入口。

**T028-04**：训练无限弹和敌人X光在正式比赛完全关闭。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U028-01**：找未参与制作的人从首次启动开始教学；记录卡住位置和所用帮助，不能由Codex替其填写通过。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 可跳过/重玩的交互教学
- 提示与检查点数据
- 教程状态回归
- 首次用户观察表
- docs/DELIVERY_028.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不以长视频或一页说明代替交互教学，不为了演示绕过伤害系统。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 029：设置与战绩存档、损坏恢复

阶段：F｜计划初始状态：未执行｜前置：028已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

退出再打开后记住设置和挑战成绩，存档出错不会让游戏打不开。

## 2. 实现步骤

1. 用用户可写目录保存数据：设置与个人战绩分开；设置可用ConfigFile，成绩使用有schema_version的简单结构。不要写到只读安装目录。

2. 保存音量、窗口模式、灵敏度、改绑、语言/字号、辅助选项；成绩只存教学章节、三项挑战星级、最佳成绩和有限场次统计。首版不存局中物理快照。

3. 采用临时文件写入、检查成功后替换并保留一份已知良好备份；针对实际平台验证替换行为，失败时不先删唯一旧存档。

4. 校验版本、类型、范围和ID白名单；损坏、缺字段、旧版本或未来版本使用明确回退/迁移，不从存档任意路径加载脚本/资源。

5. 设置改变后显示模式先预览，默认10秒未确认回退；防止选择无效分辨率后无法操作。

6. 提供重置设置与重置成绩的分开按钮，危险重置需确认；日志不记录私人文件路径、账号令牌等。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T029-01**：改音量/按键、完成任务后重启，设置与星级保持；未完成比赛不发完成奖励。

**T029-02**：空文件、截断JSON、缺字段、越界值和不支持版本均不会导致启动崩溃。

**T029-03**：模拟写入失败/替换失败，原有良好存档仍可读取；异常路径不被当成可执行资源。

**T029-04**：显示模式不确认能回退；恢复默认按键后教程/HUD同步更新。

**T029-05**：同一个match_id结算重复进入，累计比赛数只增加一次。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U029-01**：用户完成一项挑战、调音量、关闭游戏再开，检查记录仍在；损坏副本测试不能动用户真实存档。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 配置/成绩存储服务
- 迁移与备份恢复
- 独立测试档案
- 中文数据位置/恢复说明
- docs/DELIVERY_029.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不加云存档、账号、联网遥测或局中全状态存档；不读取任意存档指定脚本。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 030：主菜单到结算的完整流程

阶段：F｜计划初始状态：未执行｜前置：029已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

所有功能从一个一致入口串起来，陌生玩家不需要知道场景文件在哪里。

## 2. 实现步骤

1. 主菜单包含继续教学/开始游戏、车库、挑战、靶场、设置、制作与许可信息、退出；去掉不可用的假按钮和“敬请期待”占位主流程。

2. 自由对战配置为选车、配弹、地图、难度→加载→准备→比赛→结算→再来一局/返回车库；加载失败给具体原因和返回入口。

3. 结算显示胜负、票数、命中/击穿/击毁、占点贡献、关键死亡原因；统计来自同一事件记录，反复进入结算不重复累加。

4. 全流程统一管理场景切换、鼠标捕获、暂停、音频与运行实例，避免每个按钮自行操作全局树。

5. 在加载/存档动作期间避免重复点击多次开局；退出比赛有确认，退出整个游戏不损坏已提交成绩。

6. 主菜单显示游戏版本和规则版本，便于用户反馈；开发调试入口默认隐藏但不删除测试能力。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T030-01**：从启动到每个菜单再返回的主要路径全部可达，无死路。

**T030-02**：连续快速点击开始、重开、返回时最多创建一场比赛，无重叠音乐或多个鼠标控制器。

**T030-03**：暂停退出、阵亡退出、结算再开和切地图各重复10次，无旧对象持续更新。

**T030-04**：同局结算统计与事件摘要一致，重复查看不重复计成绩。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U030-01**：用户不打开Godot、不切场景，独立完成教学、配车、对战、挑战、存档和退出。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 完整应用流程
- 状态切换与统计回归
- v0.5 功能完整候选
- docs/GATE_D.md
- docs/DELIVERY_030.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不以菜单能打开代替所有按钮后的流程实际可用。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 031：Windows独立构建与可复现打包

阶段：G｜计划初始状态：未执行｜前置：030已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

生成离开开发工程也能独立运行的Windows x64游戏包。

## 2. 实现步骤

1. 核对固定Godot版本及匹配导出模板，建立Windows Release预设。缺模板是环境阻塞，不把源工程或引擎编辑器改名冒充游戏。

2. 提供本地构建脚本：检查版本→资源导入→自动回归→导出→生成构建清单和校验值；任何一步失败明确返回非零且不发布坏包。

3. 导出内容白名单或排除规则覆盖tools、.git、缓存、备份、开发日志、规划文档、测试场景及凭据；动态加载的车型/地图/文本资源需要显式保证在包内。

4. 把exe及实际需要的pck/许可文件一起放进独立builds目录，再压缩发行ZIP；导出的exe是游戏，不要求收件人安装Godot。

5. 构建清单记录源代码SHA、Godot版本、规则/数据版本、构建配置、测试结果、包SHA256；源码提交与构建产物分开管理，不把大包塞进每次源码提交。

6. 在与开发目录无关的路径从发行包启动，至少测试含空格和中文目录；断网可玩，存档走用户目录。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T031-01**：新目录解压后直接启动exe，4车2图3任务与中文资源全部可用。

**T031-02**：发行包内不存在令牌、开发机绝对路径依赖、Godot编辑器或tools安装包。

**T031-03**：构建脚本遇到失败测试或缺模板时不输出“成功发行”。

**T031-04**：通过清单能关联到唯一源码版本；SHA256可复算一致。

**T031-05**：至少一次清理导入缓存后的重建成功；不要求二进制必然逐字节一致，但构建过程可复做。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U031-01**：用户从单独解压的发行目录开玩，而不是点原项目START_GAME.bat；记录实际Windows和硬件。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 构建脚本与export_presets.cfg
- Windows候选ZIP
- BUILD_MANIFEST.json
- 独立启动说明
- docs/DELIVERY_031.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不自动购买证书或商店服务，不隐藏系统安全警告，也不让用户关闭系统防护。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 032：车辆、弹种与地图平衡

阶段：G｜计划初始状态：未执行｜前置：031已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

用可重复对照和实际试玩修参数，避免“后做的车永远最强”。

## 2. 实现步骤

1. 建立四车×两弹种×主要距离/方向的穿透与失能测试矩阵，记录哪些搭配能打穿哪些区域。数据来自实际解析器，不手填好看的结果。

2. 使用对称出生/交换队伍的固定种子AI比赛作为筛查，再结合玩家试玩；小样本胜率不能宣称统计定论。

3. 分别检查车辆定位、地图阵营优势、单种弹是否全面优于另一种、固定火位是否无反制；先改布局和数据，不给特定玩家暗中加成。

4. 一次只改一类关键变量，记录前后参数、原因、受影响黄金测试和体验；不能为了通过旧结果保留错误断言，也不能无审批随意改验收标准。

5. 调整挑战三星阈值和普通难度，目标是可以学习和改进，不用漫长刷取拖时长。

6. 完成后冻结1.0规则版本；仍存在争议的非阻塞参数写进已知限制，后续更改需独立补丁。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T032-01**：每次改参后穿甲/状态/弹药/对局回归全部重跑。

**T032-02**：不允许有车型因配置漏填获得0秒装填、无限炮塔转速或空装甲。

**T032-03**：AP与APHE在至少一个标准场景各有优势，取舍不是纯文案。

**T032-04**：交换阵营的筛查报告包含样本数、种子、AI难度与局限，不伪称证明公平。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U032-01**：用户四车各打一场，两图各试玩，并明确最不愿使用的车及原因；按具体反馈调整。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- BALANCE_REPORT.md
- 规则参数变更记录
- 冻结的1.0数据版本
- 车辆/弹种测试矩阵
- docs/DELIVERY_032.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不把平衡问题变成无限加车、科技树或难度作弊。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 033：性能、长局稳定性与资源预算

阶段：G｜计划初始状态：未执行｜前置：032已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

确认8车、炮弹、碎片和回放同时存在时稳定，并记录实测硬件范围。

## 2. 实现步骤

1. 选定真实测试电脑并记录CPU/GPU/内存/系统/驱动/分辨率/画质。已有交付报告里的4070 SUPER不是自动确认的最低配置。

2. 在Release有画面构建测两图高负载：8辆车、多发弹丸、火灾、残骸、回放；分别记录平均帧率、帧时间p95/p99与尖峰场景。60FPS是目标，不是无条件承诺。

3. 固定活动预算起点：车辆8、飞弹64、每弹碎片12且不递归、回放队列2、残骸12；根据测量调整上限，达到上限时行为有明确定义且不改变已结算伤害。

4. 进行30分钟真实运行稳定性检查和至少20局连续生命周期测试，统计节点、内存和活动对象趋势；预热/缓存导致的有限增长与泄漏区分。

5. 先定位热点再优化，优先限制重复查询、感知频率、粒子/影子与无界列表；不要凭感觉重写整个物理或降低测试覆盖。

6. 低画质只降视觉开销，不能改变装甲厚度、弹种规则、AI作弊程度或战斗时间步。日志限制大小并轮转，禁止无限写盘。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T033-01**：无窗口逻辑压测和真实渲染测量分开报告，不拿headless帧率冒充游戏FPS。

**T033-02**：20局循环后无持续增长的活动车辆/弹丸/音源，内存趋势有数据及解释。

**T033-03**：预算上限用例不崩溃、不生成虚假伤害、不丢失必须结算的已有命中。

**T033-04**：优化前后相同输入的黄金战斗结果一致。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U033-01**：用户以自己常用分辨率完成一整局，标记开炮、殉爆、转镜头和重生时的明显卡顿。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- PERFORMANCE_REPORT.md
- 帧时间与对象/内存记录
- 性能修复与规则一致性回归
- docs/DELIVERY_033.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不宣称未测试的核显/低端电脑可60帧，不靠吞错误或关闭核心规则优化。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 034：全量回归、陌生玩家测试与发布候选

阶段：G｜计划初始状态：未执行｜前置：033已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

用最终导出包逐项检查完整游戏，而不是仅检查最后新增功能。

## 2. 实现步骤

1. 执行TEST_MATRIX.md及每张单积累的实际自动测试，覆盖车型、地图、弹种、难度、任务、教学、存档和主要菜单路径。所有结果关联同一构建SHA。

2. 检查中文/空格路径、只读安装目录、缺/坏存档、失焦、不同窗口尺寸、高DPI、断网、输入改绑、重复重生和重开。不同机器未测就标待验证。

3. 让至少一位未参与制作的人从发行包开始，不提供口头路线图；记录卡住环节、误解、崩溃和反馈，不替用户评价完成。

4. 按P0/P1/P2/P3分级缺陷；崩溃/丢档、不能开始或结束比赛、穿墙伤害、回放造假、核心资源缺失必须修完。

5. 修复必须复现→补测试→最小修复→全回归；修复改变核心规则时先回到相关验收闸门，不偷渡新功能。

6. 输出v0.9 Release Candidate及明确未覆盖环境列表；候选通过不代表用户已经允许公开发布。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T034-01**：所有必须测试有实际结果或阻塞，不允许用“应该可以”填PASS。

**T034-02**：P0/P1数量为0，且无仍未定位的严重间歇性问题被降级掩盖。

**T034-03**：从主菜单到完整对局和挑战存档均在最终导出包验证。

**T034-04**：测试报告的SHA与待发行构建清单一致，不拼接多个版本的通过证据。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U034-01**：陌生试玩者能自行开始、理解、完成并重玩；有明确观察记录而不是开发者推测。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- v0.9候选包
- QA_REPORT_RC.md
- 缺陷清单与复测证据
- docs/GATE_E.md
- docs/DELIVERY_034.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不在候选阶段加新地图/新车/联机；没有真实Windows测试不能签为Windows验证通过。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 035：发行资料、素材来源与诚实展示

阶段：G｜计划初始状态：未执行｜前置：034已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

把游戏交付成用户能理解和传播的完整作品，但不越权提交商店。

## 2. 实现步骤

1. 清点模型、音效、纹理、图标、文本和实际使用字库的来源及许可记录；移除来源不明资产，按适用许可准备鸣谢。Godot相关许可说明单独核验。

2. 准备游戏说明、操作指南、已知限制、实测配置范围、离线存档位置和故障反馈方法；玩法宣传明确是简化的架空坦克仿真。

3. 从最终候选实机截取主菜单、车库、两地图战斗、穿透回放等画面；视频也必须是实际运行录制，不用AI概念图冒充游戏截图。

4. 准备发行ZIP中的README、许可/鸣谢与版本说明，生成文件列表和SHA256；日志/崩溃反馈默认本地，上传需用户主动。

5. 区分“可独立分发的成品包”和“已在商店上架”。商店账号、费用、条款、地区要求和当前审核政策到正式决定上架时再核实，不能本单假定完成。

6. 不给作品贴官方授权、历史精确仿真或支持未实现联网等标签；项目仓库名mcthunder不自动决定最终公开商标。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T035-01**：素材清单中每一发行资源都有可追溯来源/处理状态，无“未知但先发”。

**T035-02**：截图/视频可对应候选版本，宣传所列功能在该包中可打开。

**T035-03**：发行包不含密钥、个人日志、无关文件或开发工具安装包。

**T035-04**：下载/启动/按键/退出/存档恢复说明与实际包一致。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U035-01**：用户核对作品名称、公开介绍和真实画面；没有用户明确动作，不替用户公开发布或购买服务。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- 发行README/更新说明
- 真实实机素材
- ASSET_REGISTER.md最终版
- 许可与缺失项核验表
- docs/DELIVERY_035.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

不将技术清单当作法律结论；不代买服务、代签条款、擅自公开发布。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。


---

# 工作单 036：v1.0签收、版本冻结与补丁流程

阶段：G｜计划初始状态：未执行｜前置：035已签收；前置阻塞时先修前置，不能自行跳过。

## 0. 开始前

在现有工程实现，不新建替代工程。读取根AGENTS.md、本单、docs/planning/EXECUTION_PROTOCOL.md以及相关的GAME_RULES/ARCHITECTURE_CONTRACTS。
记录当前实际分支、源码SHA、Godot版本与未提交改动。若当前工程不同于计划基线，先对照接口做最小适配，不强行覆盖。
本单引用的参数属于游戏初值；不能把它们宣称为现实车辆性能。

## 1. 本单必须留下的可观察结果

把通过验证的具体版本冻结为可交付成品，后续修复有回退路径。

## 2. 实现步骤

1. 核对最终源代码SHA、构建包SHA256、引擎版本、规则版本、资产登记和完整测试报告对应同一候选。必要时从该SHA重新构建并复测。

2. 按RELEASE_CHECKLIST逐项签收：4车、2图、靶场、2弹种、4对4占点、3挑战、教学、中文设置、车库、存档、结算、独立运行和已知限制。

3. Codex填写实现与自测证据；审核者填写技术检查意见；只有用户能填写“实机试玩接受”。任何角色不冒充另一个角色。

4. 默认只准备本地1.0包与标签建议；未经明确发布授权不创建公开Release、不上传发行物、不提交商店。已有源码推送授权按约定分支执行，不强推。

5. 记录后续修补流程：反馈→关联构建→复现→P级→小补丁→回归→候选包→用户接受→发布。保留前一可用包，存档迁移必须兼容或提示。

6. 把联机、飞机、海战、步兵、逐体素破坏、真实履带链和大科技树移到1.0以后独立提案，不为“完善”无限追加首发范围。

## 3. 自动检查与集成验收

下面是要实际实现和运行的验收案例，不是已有测试通过记录。每个测试调用真实生产逻辑，记录源码SHA、命令、种子和结果。

**T036-01**：清单不允许空白必需项；待人工项目不能自动变为通过。

**T036-02**：最终归档包与验收测试包一致，校验值匹配。

**T036-03**：已有存档在最终包能读取；新开档同样通过教学与首场战斗。

**T036-04**：可回退位置、构建步骤和实际启动方法由另一人按说明核对。

还必须重跑此前相关回归、资源导入/脚本检查与有限帧启动。调整旧测试只能因获批规则变化，不得删断言掩盖问题。

## 4. 用户或真实图形验收

**U036-01**：用户运行最终独立包并确认“接受此版本作为1.0”；否则保持候选状态，按缺陷单修复。

真正没有做过的图形/手感检查标“待用户验收”，不得拿headless检查或自动截图代替用户的实际意见。

## 5. 交付物

- SIGNOFF_1.0.md
- 最终构建清单/校验值
- 1.0独立发行包
- 补丁与回退说明
- docs/DELIVERY_036.md：实际实现、被测SHA、命令与日志、未验证、已知问题、运行入口和回退位置。
- 新增自动回归及可复现场景/配置；必需证据不能只有一句“全部通过”。
- 按已授权仓库分支流程提交，回复实际分支与提交SHA；不强推、不擅自公开发行。

## 6. 禁止扩展

Codex不得自行签收、伪造用户同意或把完成计划等同完成游戏。

不得自动开始下一工作单；本单完成仅意味着待签收。

## 7. 何时停止/返工

缺引擎/权限/必需资源/模板等真实环境阻塞就报告最小必要操作，不伪造运行。
新引入的P0/P1、核心验收失败、生产逻辑与测试/回放不一致时，保持本单并给出复现和修复，不以扩大范围回避。
部分完成可交付当前可运行部分，明确未完成；技术审查和用户试玩接受后才更新为accepted。
