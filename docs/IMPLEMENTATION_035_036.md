# 035—036 本地候选交付方案

固定运行源码 `9998f4483637ad5d0b73ca23060e235b46f908c2`，版本 `1.0.0-rc.1`。先完成 031 全新导入/回归/独立运行、032 规则冻结、033 实机长测与 034 实际玩家入口，再冻结本地包。文档补充提交与运行源码分开记录，不能把后补说明的提交当成已测试二进制来源。

035 整理实际功能、按键、存档恢复、已知限制、真实窗口图片和素材许可登记。最终包保留 Godot 及字体许可，四车模型/图集/合成音频/原创场景按清单审查。参考图对外权利保持 PENDING_HOLDER_REVIEW；跟踪文本的敏感模式扫描记录范围和局限。没有上传、商店提交或付费操作。

036 将最终 ZIP、原始构建清单、源代码定位、规则内容哈希、验收记录和补丁流程放在可定位的交付目录。复算每个随包文件与 ZIP 的 SHA256，保留成功包和失败构建。技术检查、真人上手、历史内容证据和素材公开权利使用不同字段；可以交付本地 RC，不能由模型替用户宣布 1.0 接受。

陌生玩家验收采用普通入口：解压后启动、初训、配弹/装甲查看、挑战、两图对局、结算/回放、退出重进确认存档。记录具体阻碍及复现，不以自动键鼠代替真人反馈。跨机器、DPI、只读 ACL、断网环境中未运行的项目保留 NOT_RUN。

后续修复按 PATCH_PROTOCOL_RC.md 新版本、新目录、回归验证，不覆盖旧包。车体方向性后坐、悬挂、炮口烟尘和 99A 细化保留在 GAP_REGISTER_RC.md，按用户指定顺序在主线交付之后实施。
