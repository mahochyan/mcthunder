# 029 设置与进度恢复交付

基于 `612170e03cfc5ba943fa44bc25888502ea33aad5`，分支 `codex/mainline-027-continuation`，Godot 4.7.2 Compatibility，版本 0.3.2。实现源码 SHA 在下一份进度清单登记；本报告与代码一起提交，以下 WIP 检查不冒充最终提交重测。

设置 schema 2 新增显示模式/窗口尺寸和简体中文，显式迁移 schema 1。按键、选项、类型、范围和 ID 白名单校验；先写临时文件并核验字节及语义，备份有效旧文件，再替换主文件。锁目录防并发写入。损坏主文件恢复有效备份并提示，替换前保留原始坏文件。任一设置文件含未来版本则禁止覆盖或重置。

显示模式由真实 DisplayServer 预览；10 秒未确认、Esc、关闭面板或保存失败恢复原模式/尺寸/位置。确认成功才保存偏好。全屏使用桌面分辨率；选择尺寸用于窗口。启动尊重显式命令行窗口参数。

设置重置与全部进度重置分开，均要求确认。设置重置不动进度；进度重置不动设置。档案保留 identity/revision 与递增比赛序号，旧 pending 结果不能重复获奖。原双槽迁移、挑战成绩、教学检查点与比赛收据继续使用唯一 ProfileStore；修复“未来版本槽旁有旧有效槽仍可写”的保护缺口。

数据与手动恢复说明：`DATA_RECOVERY_029.md`，游戏设置面板也提供简版说明。所有破坏性测试只操作隔离测试目录。

## 实际检查

- WIP 回归 `logs/029-wip/612170e03cfc5ba943fa44bc25888502ea33aad5/20260910-152922/RESULTS.json`：设置 36、改绑 16、车库 151、应用流程 127、挑战 140，共 **470** 项，通过且无意外错误，退出 0。包括 schema 3 的挑战成绩迁移回归。
- 真窗口 `logs/030/settings-window-r1.log`：**16/16**，退出 0；执行 `--resolution 1280x720 -s res://tests/run_settings_window.gd -- --settings-window-check`。实际切换到 1920×1080 桌面全屏再恢复，键盘确认/取消/超时/写失败/销毁/双重置均覆盖。截图 `docs/evidence/029/window` 已目视。
- 早期故障证据 `logs/030/settings-checks-r1` 至 `r4` 留存。修复了 JSON schema 浮点值与整数列表严格比较的问题；有效临时文件先校验语义再逐字节比较，避免 JSON 数值表示差异被误当写失败。
- 原改绑末条损坏用例由“无备份默认值”更新为新增规则“恢复上次有效备份 K 键”；新增 14 个无有效备份损坏夹具仍验证默认值回退，未删去负例。

工程检查通过；真人重启/手动备份恢复为 PENDING。启动 `E:/AIprogram/mcthunder-mainline/START_GAME.bat`。下一步 031 独立 Windows 包，未合并 main、未公开发布。后坐/悬挂/火光细节仍延期至主线完成后。
