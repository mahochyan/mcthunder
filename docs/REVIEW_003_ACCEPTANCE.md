# REVIEW_003_ACCEPTANCE — 003 阶段签收记录

- 复审人：GPT（源码与执行端证据复审；未在本环境运行 Godot/PowerShell，未目视 PNG）
- 签收时间：本轮（GPT 正文决定，直接作为签收依据，不依赖附件）
- 结论：**status = accepted**
- acceptance_scope = development_baseline_with_carryover（开发基线、带明确保留项）
- accepted_sha = `eb63540e399e5ce3d5c46224e2e19bcd5b6464bd`
- reviewer_acceptance = source_and_evidence_review_passed
- user_acceptance = **NOT_RUN**（不代签，人工项延期，最迟 011 核心体验里程碑前完成对应检查）

## 签收范围

已实现的多车辆基础、配置驱动、统一命令、试射任务及启动安全处理。
不是完整游戏、历史车型或最终 UI 品质验收；人工未做的项目继续保留，不改成 PASS。
003-R2 修订循环到此结束；不再主动修改已关闭的车辆、命令、任务或启动守卫。

## GPT 复审确认的关闭项（3d410a6 复审 + 本轮收尾）

1. **命令单一物理消费**：submit 只验证/复制/暂存；车辆物理回调统一消费；
   合并策略接入；60 物理步对应 60 次驾驶执行（drive_call_count）。
2. **发射时轮次/生命周期身份**：开火时生成编号与轮次上下文；命中对象提供
   目标身份；任务接收器统一处理轮次、双方身份与去重（TrialHitGate 单一来源）。
3. **自然瞄准演示**：移除强制对齐后的自然追赶记录 + 三次输入开火、自然装填、
   任务完成与 R 重开记录。
4. **启动失败短路（收尾轮关闭）**：
   - 测试不再修改原工程配置：旧 `abort_check.gd` 已删除；新 PowerShell 运行器
     `git archive` 导出提交、在临时工程内修改配置；遗留备份只报告，不自动恢复覆盖。
     负例记录含被测候选 `b2784ca`、临时工程位置、正常副本启动成功、坏配置子进程
     `exit=1 / abort_log=True / unexpected_script_error=False / PASS`。
   - 初始化失败后不再进入正常暂停流程：`_initialized` + `_can_use_gameplay()`
     守卫接入失焦通知/暂停/恢复/整场重置；`_abort_initialization` 幂等
     （先设终止状态再清理，错误画面不重复创建）。T003-10 分别验证终止态/
     未初始化态/正常态（前两者不进入暂停，正常态保持自动暂停）。
     回归 208 项 0 失败 CHECKS_PASS。

## 签收版本对照（GPT 比较过 Git 树，对象一致）

| 用途 | SHA |
|---|---|
| 隔离负例所测候选 | `b2784ca86df2741340f343fd5c1070d1c5b144b2` |
| 003 签收交付 | `eb63540e399e5ce3d5c46224e2e19bcd5b6464bd` |

远程 `work/003-vehicle-foundation` 指向签收交付；main 未变；复审只读取仓库。

## 保留项（carryover，不代签）

| 项目 | 状态与处理 |
|---|---|
| U003-01：两车同场、仅 A 响应玩家输入、炮镜中 B 可见 | NOT_RUN，并入后续可视化版本验收 |
| 真人完成试射目标并重开 | NOT_RUN，由实际试玩确认 |
| 操作系统级 Alt+Tab、鼠标与驾驶手感 | NOT_RUN，不能用通知方法测试替代 |
| UI 实际可读性和画面观感 | 未做人工确认，不因自动截图生成而通过 |
| TOOL-003-01：运行器原始输出保存与全阶段超时 | 非阻断工具保留项——下一次使用运行器前维护：三个阶段（import/clean_boot/invalid_config）统一走 Invoke-GodotStep 执行函数，原始输出先按被测 SHA 归档再清理临时工程 |

人工项最迟 011 核心体验里程碑前完成；RC-001 继续有效（完整 UI、历史车型资料、
装甲与内构、完整战斗体验仍是后续目标——签收基础工程不意味着把目标游戏降级）。

## 下一步

- recommended_next_order = **004**（仅登记；不自动执行旧 WO004）
- 004 开始前需按 RC-001 把具体车型、数据结构、关键函数与工具流程设计清楚
  （第一辆有明确资料依据的坦克 + 能实际操作的外观/装甲/内构查看界面）
- 不合并 main、不强推、不重写历史