# 025 低面数贴图修订与复刻流程记录

用户于 2026-09-09 看过实际 985/2489/4085 三角面 M4 预览后选择约 1000 面，要求机械细节用贴图、轮廓附件保留几何，并提供四张概念图。随后要求一份完整、可复用和可移植的制作流程。

正式四车按原始装甲种子、相同局部轴心和运动层级重建：M4 1016、M24 908、M26 1018、M36 970 三角面，包含左右履带。未修改车型性能、装甲或内构配置。Blender/GLB 所有装甲片仍通过逐顶点/三角形对应检查，运行时装甲直接使用同一布局并附加 manifest 的 UV。

内置 ImageGen 第一次请求网络失败，第二次成功生成 1254×1254、4×4 分区纹理。固定 PNG、完整提示词、Blender 源、GLB、种子和脚本全部保留。几何为项目原创脚本构建；纹理由用户 M4 概念参考派生。没有下载第三方游戏模型、Blender 插件或调用运行时外部 API。

连续履带加独立左右纹理滚动替代原有逐片几何；实际位移、转角、暂停和重置驱动视觉相位。完整模型上限 1100 三角面；预分配烟火另计，完整实体上限 3000。两者由运行时分别统计，避免把暂时隐藏的特效当作模型面数或遗漏两侧履带。

普通窗口试玩暴露了同帧 UI 按下/释放可能在下一渲染帧留下 fire 边沿。`PlayerController.require_fire_release` 现在等待输入边界并清除旧边沿；新的 `run_menu_fire_handoff_checks.gd` 使用实际挑战、输入动作和射击代码验证初始六发、菜单边沿不耗弹、下一次新点击正常耗一发。修订前该检查实际失败，修订后通过；未通过的 WIP 日志保留，不冒充已通过证据。

两个旧工业地图断言与 025 行为同步：铁轨从四个网格改成一个合批网格，因此测试改为核对四条实际可见条带及穿过条带高度的世界射线无碰撞；完整 AI 对战按真实 team_id 统计每队至少三个不同槽位到达中央，避免把固定 A/B 槽位一定生存到中央当作寻路要求。原有全部槽位路径检查、至少六槽实际开火、真实比赛结束、90 秒不静止等约束保留。

制作流程见 `TANK_MODEL_WORKFLOW.md`。移植包 `deliverables/tank-model-workflow-v1.zip` 包含源文件、参考图、脚本、纹理、参数和验证日志；在新目录中实际重建四车后，`repro_report.py` 对规范化几何、UV、局部变换、层级、种子、纹理和工具版本逐项比对基线，结果 `REPRO_BASELINE_MATCH_PASS`。不保证不同渲染设置的截图逐像素相同，也不把重新调用图像生成等同于确定性复刻。

本记录不是工作单 025 的完整签收。完整回归、最终源提交对应的窗口证据、打包与交付登记另行记录，真人验收不代签。
