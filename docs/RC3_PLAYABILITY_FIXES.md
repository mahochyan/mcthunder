# RC3 可玩性修复记录（2026-09-11）

工作树：E:/AIprogram/mcthunder-mainline；分支 codex/rc3-playability-fixes；基线 664e1c82514ca3b3736ea17d2b2913defd51cdbe。这是源码修复批次，旧 RC2 安装包与 main 保持原样，未导出、推送或发布 RC3。

## 本轮变化

- 存档：现存锁一律拒绝抢占，包括同 PID 的另一实例；每次取得锁写入随机 nonce，释放时核对磁盘完整令牌。失败不扣除内存积分、不删除他人的锁。
- AI：原始任务保留，每个任务最多尝试四个互不重复的导航跳点，避免 A/B 往返。真正不可达仍报告阻塞，不伪装到达。修理测试使用真实物理更新、修理记录及实际位移。
- 交通统计：规划计数不依赖 64 条环形历史，按驱动实例读取累计计数；开放等待计入总时长和最长等待；只有实际补给区域才能归为补给等待。
- 界面：健康模块默认收起，损伤出现即显示；战况打开时恢复完整列表。补齐副驾驶/前机枪手角色中文映射。暂停及结算清空战斗字幕，避免残留遮挡。
- 性能采样：修复首帧永远无法入列的循环；空样本或无快照失败；允许 2–45 秒短采样。将结束弹体数明确命名为 projectiles_finished，修复闭包标量计数，并与实际发射次数区分。

## 验证与失败保留

固定引擎 tools/godot/Godot_v4.7.2-stable_win64_console.exe。自动逻辑检查 headless；需要物理推进的检查使用 --fixed-fps 60。窗口采样未使用固定 FPS 仿真，只使用 --max-fps 60。

- logs/rc3-ai-recovery-r3.log：16 项通过。真实修理 721 帧、履带完整度 0→50，随后四秒移动 13.119 米，目标仍为撤退点。此前 r1/r2 失败保留：原测试撤退点在导航图外，不能作为可达撤退夹具。修正为图上节点，未放宽位移要求。
- logs/rc3-lock.log：8 项隔离锁测试通过；不使用真实玩家档案。
- logs/rc3-telemetry-final.log：7 项通过，覆盖 100 次规划超过 64 条历史、开放等待汇总、快照不重复累计、补给误归类。
- logs/rc3/wip-r2/：车库及真实外部进程持锁、AI 驾驶/战斗、团队、HUD、音画反馈、AppFlow 回归；各命令、退出码和检查数由 RESULTS.json 保存。初次 wip-uncommitted 失败保留：旧车库断言仍要求同 PID 抢锁，并遗留夹具锁，已修正断言与夹具清理，真实子进程持锁检查增加 PID 核验。
- logs/rc3-window-village/：5 秒真实窗口，295 帧，均值 16.998 ms、p99 19.070 ms、最大 135.332 ms。
- logs/rc3-window-industrial/：5 秒真实窗口，298 帧，均值 16.817 ms、p99 19.870 ms、最大 86.264 ms；PNG 已人工式读图检查，健康模块收拢后左下方面板变矮。

上述证据为开发工作树验证，不能冒充 RC2 固定源码或新发布包验证。短窗口没有覆盖炮战高负载、整局和村庄帧尖峰；不据此宣称稳定 60 FPS。AI 实验室检查部分运行仍报告退出时 ObjectDB 警告，未将其描述为零泄漏。

## 仍待完成

整局两地图性能与交通复测、村庄尖峰定位、真人视角与高 DPI 布局验收；车体方向性后坐/悬挂等手感工作尚未实现。当前修复不代表游戏整体签收，历史 GAP_REGISTER_RC.md 中未完成项继续保留。
