# 022：车库配装、编成与小型研发闭环

工程自审通过，真人试玩NOT_RUN。正式车库可检视四车的装甲面、部件、乘员及来源；两弹种数量、首发、最多三车编成与难度形成独立MatchConfig，出生和再出击均使用确认清单。

分支work/022-garage-progression，基线ba92ba5。完整回归源码35f3663f70a7e848ae293f306c02a1e4dfa64f18；最终源码fae2fb8a45517ff242bb0110a9c7c3890a4207b3只修正装甲区域显示名称及窗口脚本的侧面选择标识。固定Godot4.7.2.stable.official.ed1daf0bf / Compatibility，版本0.2.2。

## 实际实现

GarageService通过真实AmmoInventory校验数量、容量与首发。零弹架在内构预览隐藏，出战后架位与预览一致。Lineup只接受1—3个不重复合法车型；MatchConfig冻结车型、配弹、地图、难度和模式，UI不直接写战斗状态。旧工程靶场入口与历史回归保持兼容。预览只有视觉节点，不生成战斗Actor；装甲显示名义厚度及verified/estimated/unknown，未知不显示为零厚。

训练/自由对战全车可用、不计收益。正式模式初始M4与100研发点，M24需80点，M36需120点，M26需先有M36再花180点。胜60、负30、平40、主动退出0；研发只解锁车辆，不改变历史核心性能。这些均为明确游戏规则。

ProfileStore校验完整档案并使用两个交替JSON槽、临时文件、复读与重命名；较新槽损坏时恢复旧槽，两槽均坏则保留文件并禁止覆盖。目录锁和revision检查拒绝并发旧实例写入。研发扣点、比赛登记和奖励分别事务提交，失败不改变内存余额。比赛令牌先保存，结算绑定真实Director结束结果；重复返回、重试及重启不能重复领奖。失败结算保留可信结果，释放战场后仍可重试。自动检查使用独立内存或user://tests路径，不接触玩家档案。

## 验证证据

| 实际命令/流程 | 源码 | 结果 | 日志目录（logs/022/下） |
|---|---|---|---|
| tests/run_suite_checks.ps1 -Order 022 | 35f3663 | 23套1,790项PASS，导入及所有进程exit0 | 35f3663f70a7e848ae293f306c02a1e4dfa64f18/20260909-062759 |
| 同运行器 -Suites @('run_core_checks','run_historical_checks','run_garage_checks') | fae2fb8 | 392项PASS，exit0 | fae2fb8a45517ff242bb0110a9c7c3890a4207b3/20260909-063823 |
| tests/run_garage_player_checks.ps1 | fae2fb8 | 68项PASS，15张抓帧，exit0 | fae2fb8a45517ff242bb0110a9c7c3890a4207b3/player-20260909-063930 |
| logs/022/run_historical_evidence.ps1 | fae2fb8 | 80+4项PASS，28张抓帧，两进程exit0 | fae2fb8a45517ff242bb0110a9c7c3890a4207b3/historical-player-20260909-064928 |
| logs/022/run_shell_evidence.ps1 | fae2fb8 | 50项PASS，12张抓帧，exit0 | fae2fb8a45517ff242bb0110a9c7c3890a4207b3/shell-player-20260909-065152 |

新145项检查覆盖四车实际配弹、非法清单、快照隔离、全部M4装甲面检视、空架显示、研发依赖、损坏恢复、并发旧存档拒绝、写入失败、可信结算及重复奖励拒绝，以及真实AppFlow内M24出战和M4再出击。完整命令、退出码、引擎版本、PASS/FAIL原文见各RESULTS.json与stdout/stderr；旧布局三个精确反例错误仍是唯一白名单。

正常窗口脚本通过真实鼠标、滚轮和键盘操作：研发M24、配2AP/3APHE首发APHE、M4只带一发、查阅档案、M24实射、自然等待8秒后M4再出击、退出不领奖。第二局玩家驻车观察，AI未经属性/计分改写自然战斗262.6秒，以0:270票战败，获得30点、余额50，返回车库不重复奖励。固定物理帧用于可复现输入，不是FPS或真人体验结论。

最终55张真实1280×720截图全部逐张读图，位于docs/evidence/022/fae2fb8a45517ff242bb0110a9c7c3890a4207b3的上述三个同名会话目录。旧四车射击演示仍打到红靶后背墙，不据此声称命中红靶。双弹种内部影响与补给由独立实际射击流程覆盖。

失败保留：35f3663的player-20260909-062916有一项侧甲检视失败，脚本查找旧hull_left标识而当前历史车采用分片ID；其余窗口流程通过。fae2fb8以实际plate_group及法向选择侧面，68项全过。失败原日志与15张抓帧保留，其中仅00/01/02/02b曾读图，不标全量复核。更早WIP原日志本地保留；曾发现JSON反例额外错误输出和车库横向裁切，修复后再形成候选。

## 运行与限制

双击START_GAME.bat，选择车辆卡片；左栏展开“配弹与编成”，设数量和首发，再从底部进入训练或4对4。正式模式可研发车辆，车型档案仍保留020/021的估算与unknown边界。个人档案在Godot user://profiles/commander.0.json和.1.json。

真人手感/历史复核NOT_RUN。少量旧实验室英文和返回车库“上次课目”措辞留027统一；异常崩溃遗留commander.lock目录时需关闭游戏后按提示移除该锁，029继续改善恢复。019长帧/动态拥堵P2继续留后续性能工作；本单不宣称新八车APHE场景稳定60FPS。正常进行中的比赛中断不伪造胜负收益。

回退到021登记ba92ba5；原main及用户文件保持不变。按连续开发授权进入023工业地图。
