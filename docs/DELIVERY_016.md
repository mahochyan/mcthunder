# 016：4对4占点、票数与安全再出击
implementation=implemented；engineering=passed_self_review；human=not_run。
分支work/016-team-match；运行源码645bde56b873214ae4151aa163b1e1892cc88555；窗口检查增补54c44f82bb6b7f384ee7402141d4ec4dd62d4f22（只增加正常时间扣票断言，游戏源码相同）。基线015登记87b662b41a9248bf9dd9d41982709a91a1bc6a2c。版本0.1.6。

中文车库“4 对 4 占点”进入一名玩家+三辆友军AI对四辆敌军AI。灰盒地图有中央半径12米据点、两侧通路和出生/中途掩体。300票、阵亡-30、占领后每秒扣敌方1票、600秒按票数判胜负；全部是游戏设计初值。车型外形与装甲/模块设计沿用M4工程车，双方同车同弹。

TeamMatchState集中保存票数、占点和各槽位生命身份。CapturePoint按有符号进度精确推进，争夺或无人时冻结、多车不加速；敌占先中立化再占领，返回每个物理步的实际归属持续时间。TicketLedger把同帧所有阵亡与持续扣票提交完再统一判终局，避免回调先后决定胜方；重复死亡和旧life/generation不再扣票。

阵亡先扣票，AI至少8秒后自动再出击；玩家至少8秒后在编成面板选择唯一现有工程车并点击“再出击”。实际BoxShape世界/车辆碰撞查询加同帧位置预留，出生点堵塞选备选或继续等待。每次生成全新life、独立完好部件/乘员、30发与两次灭火，旧实体按残骸预算保留。3秒保护有地面环和文字；驾驶/转向/开火请求先取消，转炮观察不取消，保护期间不占点。

友伤关闭但实弹在友车最近外甲或外置履带处停止；保护敌车与残骸也截停实际弹道，不能穿过打后面的敌人。结束关闭新发射、取消在途弹，冻结车辆、火灾和残骸清理时钟，取消再出击队列；冻结回放记录保留。旧玩家残骸改为普通视觉层，在新一条命的炮镜里也可见。正式R不能重置；暂停菜单“放弃当前车”只按一次阵亡扣票。Tab阵亡观察友军，始终只有一个本地控制器。

八车集成暴露并修复了AI近距离瞄点低于炮管俯角、失去路径后永久停留、炮塔传动损坏却不维修和只按实体ID优先选目标的问题。现在只在已看见的外表中选择可达瞄点，根据可见距离带迟滞选择敌车；占点命令每10秒最多重试一次失败/维修后的路线，每次避障仍有上限。双方据点驻留目标与出生间距分开，避免对冲同一个停车位。这些修改不读取敌方内部状态或隐藏实时坐标，不改任何一方物理性能。

验证：
- `tests/run_suite_checks.ps1 -Order 016`，运行源码645bde5：14套1,132项PASS（216/123/140/144/81/57/64/67/55/25/40/34/27/59），全部exit0，无非预期ERROR/SCRIPT ERROR。完整日志`logs/016/645bde56b873214ae4151aa163b1e1892cc88555/20260908-195411/`。
- 59项团队专项覆盖捕获/争夺/换手精确计时、保护期间入圈、实际命令取消保护、八正常运行AI/车辆暂停冻结、同帧归零、唯一死亡/新life、手动与AI再出击、真实世界阻塞/预留、阻塞等待后续出、友车外甲/外置履带/保护/残骸实弹阻挡、迟到伤害、结束不再生成、旧残骸炮镜可见、20次AppFlow开退节点/回调稳定、近距离可达瞄点和炮塔修复。数学时间/死亡与几何夹具明确用于自动边界检查，正常窗口另记。
- `tests/run_window_checks.ps1 -Order 016 -Script run_team_player_checks -TimeoutSeconds 270`，54c44f8：17PASS、8张真实窗口帧、exit0；`logs/016/54c44f82bb6b7f384ee7402141d4ec4dd62d4f22/window-20260908-195623/`。从正常车库点击，暂停菜单放弃扣30票、8秒等待、Tab友军视角、编成再出击、新life保护、W驾驶取消保护全部正常；实际AI完成友方据点占领，继续正常游戏时间后敌方从300降到298票，28.75秒时友方240/敌方298；正常返回车库成功。脚本只发普通输入并读状态，未改位置/损伤/冷却/时钟/票数。目视了等待编成与占点票数截图；末尾FPS快照29，非平均帧率。
- `tests/package_candidate.ps1 -Order 016`：54c44f8独立候选导入/默认启动exit0。入口`backups/candidates/016/54c44f82bb6b7f384ee7402141d4ec4dd62d4f22/PixelArmor/START_GAME.bat`；完整候选记录见同SHA的`candidate/RESULTS.json`。不是导出exe，未公开发布或合并main。

早期正常窗口证明了放弃、等待、编成、再出击，但据点曾持续争夺；保留wip日志。后续窗口实际占领成功，又暴露低帧率下自动点击暂停菜单时GUI布局/鼠标状态未完成的问题；鼠标测试改为按实际渲染帧等待布局与按下/释放，不按追赶中的物理帧猜测GUI已处理。正式界面暂停入口同时收掉未接入的检视按钮并改为中文。

已知边界：AI战术与可玩性未经真人认可，未增加补给/其他车型/小地图，相关内容按后续工作单推进。窗口帧率较低且波动，不能宣称达到60FPS；033需对渲染与查询分别采样并处理性能。剩余继承训练HUD文字和完整信息权限将在017收口。
