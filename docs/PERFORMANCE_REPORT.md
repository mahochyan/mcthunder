# RC2 性能与稳定性报告

**本次连续运行和资源生命周期检查通过；60 FPS 目标未达到。** 运行源码 `b90511496fe84195ee8421bd3b4c6bfce2768b7a`，最终 ZIP 与 EXE/PCK 身份见 DELIVERY_031.md。工程状态为 `passed_stability_with_performance_gap`，不写成所有性能目标通过。

使用最终独立 Release 程序，Windows 11 10.0.26200、i5-13400、约32 GB内存、RTX 4070 SUPER、NVIDIA 591.86、1280×720、Compatibility/OpenGL 3.3。开跑前和结束时没有其它 Godot/Blender 进程，本次没有并行游戏测试；不宣称是隔离实验室或其它硬件的基准。

实际命令：`tests/run_performance_checks.ps1 -Executable 'E:/AIprogram/PixelArmor交付/1.0.0-rc.2-b9051149/游戏/PixelArmor.exe' -SourceSha b90511496fe84195ee8421bd3b4c6bfce2768b7a`。2026-09-10 18:56:38启动，**1876.26秒、20次场景生命周期、67项检查、退出0**。日志/原始JSON/进程内存/硬件/截图在 `logs/033/b90511496fe84195ee8421bd3b4c6bfce2768b7a/20260910-185638/`；分析表与输入哈希在 `docs/evidence/033/rc2/`。

场景使用生产四车配置和八AI，交替两图与三档特效。前六轮为主要战斗负载，第七轮补足1800秒，其余13轮约5秒，只测创建、倒计时、正常退出和释放。共打开7次实际记录回放，均保持冻结结算不变；**不是20场完整自然比赛，也不是玩家亲自驾驶的帧率验收**。完整自然比赛证据另见032/034。诊断单独跳过失焦暂停，普通游戏保持原行为。

93,281个真实渲染帧，累计平均 **51.86 FPS**，p95 **52.91 ms**，p99 **103.77 ms**，最大帧 **2653.72 ms**。逐组数据如下；分组种子、交战阶段与样本数不同，不能把差异当作严格特效A/B测试。

| 地图/特效 | 平均FPS | p95 ms | p99 ms |
|---|---:|---:|---:|
| 村庄/高 | 43.38 | 82.78 | 157.62 |
| 村庄/中 | 48.35 | 68.44 | 114.07 |
| 村庄/低 | 44.74 | 71.04 | 113.81 |
| 工业/高 | 59.65 | 35.00 | 58.72 |
| 工业/中 | 57.23 | 46.74 | 76.53 |
| 工业/低 | 58.33 | 42.70 | 69.77 |

村庄卡顿列为 **P2 / PERF-RC2-01**。最大2.65秒尖峰属于村庄低特效采样组，当前记录没有逐帧时间戳，不能定位为某次射击、操作系统调度、资源加载或诊断序列化，也不从报告里删除。帧记录包含场景进入和诊断开销，不能将全部尖峰直接归因普通玩家游戏。后续优先做带时间戳的调用分析，再优化查询/AI感知和资源加载；不降低装甲、弹药、伤害或模拟精度来掩盖耗时。

查询函数累计678,552次、入口/出口计时533.30秒。原字段名为 `query_cpu_ms_total`，实现采用单次函数墙钟差，可能包含调度延迟，不能等同系统CPU采样。渲染GPU计时可用，采样中位数约3.72ms；引擎物理监视器与渲染计时口径不同，不相加成每帧预算。数据支持继续分析查询和物理阶段，但不证明它们解释了所有尖峰。

Windows实际进程内存共929个约2秒样本：私有字节峰值 **513.32 MiB**，工作集峰值 **355.32 MiB**。前10分钟有预热增长；600秒后的5分钟私有内存中位数依次约487.3、485.9、491.5、485.9 MiB，未单调增加，600—1800秒线性斜率约0.112 MiB/分钟。最后短周期中位数492.1 MiB。诊断保留有界帧数组/快照，序列化与分配器缓存可能贡献有限增长；未做堆分配归因，因此不声称证明没有任何泄漏。

预热四周期后，第5—20轮每次释放均为 **397节点、3411对象、157资源、0孤儿节点**，本次未观察到持续场景资源增长。Godot Release的 `static_memory=0` 是监视器不可用，内存判断采用上述Windows数据，绝非零内存占用。

真实采样峰值：8辆活车、5个残骸、2发活动弹、16条射击记录、26个声音槽、5组特效、2处火灾、96条保留破片路径；均未超过既有预算。2秒采样峰值不代替瞬时上限反例，活动弹/音源/特效的拒绝与清理语义由2796项全量回归共同覆盖。交通诊断仍为10槽、单文件2MiB上限，超限保留旧记录。

查询优化的固定姿态1536发JSON在旧实现、当前实现和最终干净构建中逐字节一致，SHA256 `3B90E05682868BDD67D0CDA8C680AD74BC1C4EC094D2BAB6EAE54A9A86A09940`。RC1长测失败保留，RC2正式第一轮再现“末条记录没有目标几何”，成功选择第13号有效记录，原回放检查意图得到实际验证。

其它硬件、DPI、玩家移动镜头与真实操控手感仍待验收。该报告支持本地RC稳定性，不支持“所有场景稳定60FPS”或最低硬件承诺。
