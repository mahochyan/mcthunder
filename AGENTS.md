# PixelArmor — AGENTS.md

## 项目目标
按获批工作单逐步把本工程做成"低模方块风、按装甲和内构结算的小规模坦克战"
（总计划 002→036，见 `docs/planning/MASTER_PLAN.md`）。当前状态：001（可驾驶、可瞄准、
可射击的 3D 坦克靶场 v0.0.1）已实现；002 已按"开发基线、带保留项"签收
（`docs/REVIEW_002_ACCEPTANCE.md`；人工逐项/截图等保留项延期至 011 核心体验验收，
不代签）；**当前获批：003 多车辆基础、数据配置与控制接口（执行中）**。
每张工作单完成即停、签收后才进入下一单；未获批的范围一律不实现。

## 技术栈
- Godot 4.7.2-stable（官方普通版，非 .NET）+ GDScript
- 渲染器 gl_compatibility；窗口 1280×720、可调整大小
- 引擎随工程放在 `tools/godot`（含 `.gdignore`；不入库、不打包）

## 运行
- 推荐：双击 `START_GAME.bat`（按脚本位置定位引擎与工程，正确处理空格路径；
  缺引擎时输出明确提示，不静默失败）
- 备用：Godot 项目管理器导入 `project.godot` 后 F5

## 测试方式
- 无窗口自动检查（78 项，002-R1 版）：
  `godot --headless --path <工程根> -s res://tests/run_checks.gd`
  覆盖：001 全部 52 项（主场景与必要节点加载、9 个输入动作、冷却与按住不绕过、
  遮挡不误命中、穿墙阻止开火、驾驶物理、重置、暂停宽限）+ 002 新增：
  空格路径启动（START_GAME.bat 实测）、三个遮挡场景（矮墙/炮口入墙/贴墙）、
  相机贴墙防穿、失焦通知自动暂停、暂停期间装填/驾驶冻结、恢复持火不补射、
  连续 20 次重置、瞄点后方过滤 + 002-R1 新增：相机前向与炮管指向俯仰一致、
  真实鼠标事件点击“继续”按钮（无冷却场景全链）、重置全量断言、装填冻结精确对比。
  测试全部调用游戏实际逻辑；变异验证（RELOAD=0、射线掩码=0）证明断言能精确变红。
  失败时退出码非 0。
- 截图自检：`godot --path <工程根> --resolution 1280x720|1920x1080 -- --autoshot`
  （有限帧、真实抓帧、含暂停菜单与鼠标重捕获证据、自动退出；
  必需截图失败时退出码非 0）
- 证据日志：`logs/002/`、`logs/002-R1/`（真实命令、退出码、完整输出；无 SCRIPT ERROR 扫描记录）

## 约定
- 1 单位 ≈ 1 米；Y 轴向上；车辆前方为 -Z
- 碰撞层 / 视觉层 / 射线自身排除集中在 `scripts/game_config.gd` 头部说明：
  LAYER_WORLD=1（地面/围墙/箱子/靶板）、LAYER_VEHICLE=2（本车）、
  VIS_LAYER_VEHICLE=2（本车网格视觉层，炮镜相机 cull_mask 剔除该位）
- 所有速度 / 加速度 / 转速 / 冷却参数集中在 GameConfig

## 工作单边界（002 修订，替代原"仅限 001"条款）
- 按“当前获批工作单”逐步推进；当前获批：**003 多车辆基础、数据配置与控制接口**
  （002 已按开发基线带保留项签收，见 `docs/REVIEW_002_ACCEPTANCE.md`）。
- 003 范围：多车辆（A 玩家 / B 测试目标）、VehicleDefinition/WeaponDefinition/
  ShellDefinition 数据定义与校验、独立 VehicleRuntimeState、统一 VehicleCommand、
  本地玩家控制器（单车驱动与武器不再直接读全局键鼠）、统一实体生成/销毁/控制者设置、
  自身命中排除按实体（A 可命中 B、墙挡不可命中、炮镜不隐藏 B）、reset_vehicle 与
  reset_range 分离、HUD 扩展（控制车/装填/射击结果/试射目标 0/3）、保留稳定瞄准/
  有限转速/俯仰限制/真实炮口结算/遮挡/自然装填/暂停/重置。B 无 AI/巡逻/反击；
  无装甲/伤害判定（命中反馈测试，无整车血条）；不提前做内构/穿甲/科技树/正式菜单/网络/大量美术。
- 分阶段功能解禁（未获批前仍是禁令；不跳步、不并行扩权、不一次性全做）：
  004 装甲/内构数据与可见调试模型 → 005 统一命中查询 →
  006 有限速度炮弹 → 007 角度穿甲 → 008 模块/乘员 → 009 维修/起火/殉爆 → 010 回放 →
  011 核心体验闸门 → 012–015 坡地驾驶/电脑对手/1v1 → 016–019 4v4/占点/第一地图/导出探路 →
  020–024 四车/第二弹种/车库/第二地图/挑战 → 025–030 美术/音效/中文/教学/存档/主菜单 →
  031–036 打包/平衡/性能/回归/发行资料/签收。总计划：`docs/planning/MASTER_PLAN.md`。
- 永久约束（任何工作单都不得违反）：
  · 不接入外部 API、不联网对战、不建空壳网络架构；
  · 不虚构测试/试玩/截图；不能执行的检查必须标 NOT_RUN/待人工；
  · 不强推、不擅自公开发行、不覆盖 main 上的用户修改；
  · 不更换引擎技术栈（Godot 4.7.2-stable 普通版 + GDScript + gl_compatibility）；
  · 不安装无关工具；不上传工程外私人文件；工程内 `tools/` 引擎与 `.godot/` 缓存不入库；
  · 所有数值为游戏设计初值，不得冒充真实车辆/装甲性能。
- HUD 中文依赖引擎默认字体 CJK 能力（当前检测=false）→ 游戏内英文 UI，
  中文说明由 `开始试玩.txt` 与本文件承担。